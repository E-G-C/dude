# Dude Context

Durable domain knowledge, project facts, and important background context.

## Entries

- This repository delivers the Dude bundle. Edit core in `src/`, packs in `library/packs/`, and docs directly. `.github/` contains the generated, committed dogfood core. For Canvas JSX, first run `node scripts/dude-canvas-ui/build.mjs`; `node scripts/build-dev.mjs` projects the already-built runtime and other core files without rebundling. Do not hand-edit generated core or include local pack-model overrides and machine-specific pack paths in an unrelated delivery.
- `scripts/build-release.mjs` produces core-only `dist/` output, excluding packs, tests, and project ideas/specs/memory. It emits a generic project skill and only the bundle manifest and profile under `.dude/`. The committed upstream manifest stays `source_ref: main` / `installed_ref: main`. `build-release --tag <tag>` changes only the staged manifest to `source_ref: latest` / `installed_ref: <tag>`. There is no package-version write-back in this repository; hand-bumping the source manifest would misrepresent the development channel.
- Empty or missing `.dude/ideas/` and `.dude/specs/` directories are valid; idea ledgers and feature directories are created when brainstorm and definition work starts.
- Beads commands should use `--json` whenever output will be parsed or used for coordination.
- Historical reader diagnosis, superseded as a blanket current claim: the July 2026 report said `incident-supersession` was unknown and blocked Feature 007. The current `LANE_EVENT_TYPES` registry in `src/skills/dude-work/recovery.mjs` explicitly validates it as audit-only. Do not repeat the old unknown-event explanation or alter append-only history; a fresh Feature 007 execution was not performed during this memory review.
- Work recovery gaps remain separate deferred records: 056 covers orphan cleanup authority, 060 covers recoverable handoff validation, and 061 covers inventory-plus-evidence capacity. A valid completion was rejected by the 64-source budget; an earlier handoff also exceeded the 16-check limit and omitted an authorized file. Memory guidance does not change those limits or authorize automatic cleanup, wider scope, or post-hard-stop continuation.
- Canvas rediscovery provenance stays in `.dude/ideas/057-dude-canvas-needs-you.md` under Session Scenarios and Cross-Feature Rediscovery: acknowledgment/stale input, operation-specific consent, exact preview revisions, avoidable prompts, and regression reuse. Blank answer slots and historical dispositions do not prove a current human request. These bookmarks do not create scope, dependencies, or blockers.
- Preserve the UX intent behind the early Canvas flags: complete discovery without a permanent oversized navigation list, balanced full-instruction typography, and no prototype controls mixed into real work. Feature 057's approved single finder and ordinary-chat refresh supersede those September 5 implementation diagnoses; their original evidence remains in the feature ledger/design history. Query, selection, and unsent input remain separate. Draft editing and direct manipulation are not implied by those fixes; see 058 and 059.
- Learning-synthesis follow-up (user flag, 2026-09-13): ensure the existing learning process is used consistently across long sessions, including later review, closure, and delivery discoveries that a scoped retrospective did not see. Manual consolidation and reusable guidance address today's records; they do not establish an automatic session-wide trigger, rerun a retrospective, or implement deferred runtime fixes. Keep this application gap visible for future evaluation rather than treating a positive feature verdict as complete learning extraction.
