# Dude Context

Durable domain knowledge, project facts, and important background context.

## Entries

- This repository's primary deliverable is the Dude bundle itself. `src/` holds the product core source and is the edit surface; `.github/` is the built, committed dev bundle (dogfood) — run `node scripts/build-dev.mjs` after editing `src/` to rebuild it, and do not hand-edit `.github/` core files. `library/packs/` is the pack catalog and `scripts/` holds build tooling.
- Release bundles are produced by `scripts/build-release.mjs` into `dist/` and ship the core tier only: they exclude `library/` (packs) and all `*.test.mjs`, and emit a generic `project` skill stub plus exactly `.dude/metadata/bundle-manifest.md` and `.dude/metadata/profile.md`. This source/dogfood repository is itself upstream, so its committed manifest intentionally stays `source_ref: main` / `installed_ref: main`: those values are the live development channel and a branch-tracking install pin, not release-version fields. For a tagged release, `build-release --tag <tag>` reads but does not modify that source manifest and writes only the staged `dist/` manifest as `source_ref: latest` / `installed_ref: <tag>`; there are no package manifests or version write-back. Hand-bumping the committed fields for a release would pin upgrade/catalog resolution away from live `main` or create a false upgrade status. Released bundles do not ship project ideas, specs, memory, or a `.github/dudestuff/` compatibility endpoint.
- Empty or missing `.dude/ideas/` and `.dude/specs/` directories are valid; idea ledgers and feature directories are created when brainstorm and definition work starts.
- Beads commands should use `--json` whenever output will be parsed or used for coordination.
- Known live core defect (2026-07-28, unfixed): `@dude work --policy autonomous` hard-stops with `evidence-incomplete` / subject `occurrence-retention` on any feature whose `## Lightweight Execution History` holds a `type: incident-supersession` lane event. `src/skills/dude-work/recovery.mjs` writes that type and `validateEventCommitmentV1` accepts it, but the reader constant `V2_EVENT_TYPES` omits it, so `parseV2EventLines` throws `unknown prefixed event record`. Lane history is append-only, so an affected feature cannot be repaired by editing history and stays permanently non-autonomous until the reader is fixed. Guarded policy is unaffected. Feature 007 is affected; the event there was written by Feature 009's own T009 materialization. Classified as contract-mismatch; the fix is unassigned and is deliberately NOT owned by the `unattended-work-continuity` idea, which carries only the diagnosability and continuity requirements.
- Known live core workflow defect (2026-09-03, unfixed): explicit Ship can hard-stop with an orphaned Work claim/checkpoint and then require a separate human echo to authorize deterministic bounded cleanup and a fresh claim, even after the coordinator proves the supervisor is gone and no judgment remains. This contradicts Ship’s purpose of absorbing answerable continuation checkpoints. Classified as contract-mismatch; follow-up should preserve explicit confirmation for ambiguous or genuinely destructive actions while allowing Ship to authorize safe exact-pair cleanup and resume.
- Future Canvas rediscovery: .dude/ideas/057-dude-canvas-needs-you.md (Session Scenarios and Cross-Feature Rediscovery) retains the user-requested study of current-session evidence and other-session reports. Revisit it for I3 response acknowledgment/stale input, I4 operation-specific consent, Review/054 exact preview revisions, 013/039/040 avoidable human prompts, and 055 regression reuse. Blank Answer slots can coexist with definition dispositions; they do not alone prove current user work. These are investigation bookmarks, not approved scope, dependencies, or execution blockers. Feature 056 remains deferred.
- Canvas discovery follow-up (2026-09-05, user-reported): newly captured 057 did not appear in the already-open Dude canvas until manual Refresh. Source inspection: frontend/app.jsx checks freshness on focus/visibility but replaces projection only on initial load or explicit refresh; a freshness check does not update the navigation inventory. For Needs You discovery, cover successful idea capture/definition initiated through ordinary agent chat as well as Canvas-originated capture acknowledgments. Proposed outcome: refresh complete authoritative inventory after confirmed publication, preserving selection, query and unsent user input; never show partial publication or invent an auto-selected feature. This is a retained requirement gap, not a fix or automatic watcher/polling decision.
- Canvas typography flag (2026-09-05, user-reported; nonblocking): the full Next step instruction is disproportionately large and bold. FluentUI traced it to formatNextStep returning the entire description on its unsafe/un-condensed fallback, while FocalRegion always uses Title3 (frontend/app.jsx); the feature heading uses smaller Subtitle2, and focalHeadline in styles.js changes wrapping only. Retain for 057 T008 mockup review: choose a balanced long-description hierarchy using existing Fluent roles, preserve complete task instructions, and show both short and long real examples. No truncation or new typography policy is approved; no fix is claimed and T003 is unaffected. Screenshot: session 5c18f926-9e5b-4303-b236-cb926acfde82, files/needs-you-oversized-next-step-20260905.png.
- Canvas navigation feedback (2026-09-05, user): complete discovery must not become a long permanent scroll of feature entries in the left pane, described as a newspaper. The user also rejects the generic-looking clusters of filter and coverage-state buttons. Use purposeful Windows/Fluent selection controls and clear organization; keep preview-state simulation separate from real discovery. Complete inventory need not be the default navigation experience. No particular replacement control or hidden context is approved. Annotated screenshots are retained in session 5c18f926-9e5b-4303-b236-cb926acfde82, files/needs-you-feature-list-feedback-20260906.png and files/needs-you-filter-controls-feedback-20260906.png.
