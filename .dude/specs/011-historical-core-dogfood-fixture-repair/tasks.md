<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@66697874 [US1] Repair the historical valid-event construction in `scripts/current-format-contract.test.mjs` by explicitly reconstructing the accepted pre-materialization T009 repository identity and projection inside the disposable fixture; reuse immediate existing helpers, preserve all six exact named tests and their complete invalid mutation matrix plus ordinary/later-feature gates, and prove 6/6 focused passes against a later green-parity head while a discarded no-reconstruction or ambient-current control passes 0/6; add no waiver, skip, weakened assertion, persistent state, framework, production, generated, Feature 008, Feature 009, or Feature 010 write.
- T002@76616c69 [Shared] Complete ordinary final validation with the exact six-test 6/6 pass and disposable-control 0/6 proof, recursively discovered full suite, Dude lint, `git diff --check`, complete intended-scope inspection, fixture-residue check, and fresh independent review; require zero waiver or weakened gate, make no implementation write, and do not mutate or close Feature 010.

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/011-historical-core-dogfood-fixture-repair.md#coordinator-log -->

# Tasks: Historical Core Dogfood Fixture Repair

Two all-open canonical units implement `.dude/specs/011-historical-core-dogfood-fixture-repair/spec.md`.

No task carries `[P]`. T001 owns the sole implementation write path, `scripts/current-format-contract.test.mjs`. T002 is an ordinary test-only final validation task with no implementation write set. No `src/**` write is planned, so this package derives no Core Dogfood terminal and contains no `declared-src:` clause.

Execution may read but must not write this definition package. A required normative change stops with `contract-mismatch: redefine-required`. Feature 008, Feature 009, and Feature 010 artifacts and state remain read-only throughout execution.

Durable suffixes are the fixed lowercase hexadecimal encodings of `fixt` and `vali`.

## Phase 1: Historical Fixture Reconstruction

**Goal**: Make the six permanent Feature 008 T007 assertions evaluate the accepted historical pre-materialization T009 event from a later current head without weakening any gate.

**Implementation Write Set**:

- `scripts/current-format-contract.test.mjs`

**Execution Details**:

- Modify only `createTransientPacketRepositoryFixture()` or its immediate in-file test-local helpers.
- Reconstruct the historical pre-materialization repository identity and generated projection explicitly inside the disposable clone instead of inheriting ambient current head and green parity.
- Reuse existing Git, materializer, projection, mapping, identity, mutation, clone, and cleanup helpers.
- Preserve all six exact named tests, every existing invalid-case mutation, and strict ordinary and later-feature gates.
- Add no framework, module, command, persistent fixture, packet, state, source/generated production change, or expected-failure path.
- After a 6/6 focused pass, create a disposable control with reconstruction removed or ambient current head/parity substituted; the same exact selector must produce 0/6 passes. Discard the control without committing a bypass mode.

**Focused Verification**:

```bash
FOCUSED_PATTERN='^(?:T007 Core Dogfood valid live 20-path event passes every transient packet gate|T007 Core Dogfood requires exact main checkout baseline continuity and terminal readiness|T007 Core Dogfood derives current authority and rejects generic approval interruption and drift|T007 Core Dogfood compares the complete temp-materializer projection and cleanup inventory|T007 Core Dogfood pre-materialization verification isolates one exact parity delta|T007 Core Dogfood appends rechecks selects latest accepted evidence and blocks every close drift)$'
node --test --test-name-pattern="$FOCUSED_PATTERN" scripts/current-format-contract.test.mjs
```

**Independent Test**: The repaired later-head fixture discovers and passes exactly six tests; the disposable ambient-current control discovers the same six and passes none; all existing invalid mutations, ordinary-event checks, and later-feature checks retain their current rejection behavior.

- [x] T001@66697874 [US1] Repair the historical valid-event construction in `scripts/current-format-contract.test.mjs` by explicitly reconstructing the accepted pre-materialization T009 repository identity and projection inside the disposable fixture; reuse immediate existing helpers, preserve all six exact named tests and their complete invalid mutation matrix plus ordinary/later-feature gates, and prove 6/6 focused passes against a later green-parity head while a discarded no-reconstruction or ambient-current control passes 0/6; add no waiver, skip, weakened assertion, persistent state, framework, production, generated, Feature 008, Feature 009, or Feature 010 write.

## Phase 2: Ordinary Final Validation

**Goal**: Prove the bounded repair restores the repository verification baseline and is independently acceptable without exercising authority over Feature 010.

**Validation Write Set**: None.

**Execution Details**:

- Re-run the exact six-name focused verification and retain the 6/6 result plus the disposable control's 0/6 result.
- Run the recursively discovered full suite with `find . -path ./dist -prune -o -name '*.test.mjs' -print0 | xargs -0 node --test` and require zero unexpected failures.
- Run `node .github/skills/dude-lint/lint.mjs .` and require zero failures.
- Run `git diff --check`, inspect the complete diff, and require the feature implementation write set to be exactly `scripts/current-format-contract.test.mjs` with every prohibited boundary preserved.
- Obtain fresh independent review covering exact test-name preservation, invalid-case strictness, ordinary/later-feature non-leakage, transient cleanup, non-vacuity, scope, and absence of an expected-failure waiver.
- Do not mutate or close Feature 010. Report only that its separate terminal may resume under Feature 010 authority after the full suite is green.

**Independent Test**: Focused and full verification are green, lint and diff checks pass, the implementation diff is confined to the one test file, no fixture residue or protected-boundary mutation exists, and independent review accepts without changing Feature 010.

- [x] T002@76616c69 [Shared] Complete ordinary final validation with the exact six-test 6/6 pass and disposable-control 0/6 proof, recursively discovered full suite, Dude lint, `git diff --check`, complete intended-scope inspection, fixture-residue check, and fresh independent review; require zero waiver or weakened gate, make no implementation write, and do not mutate or close Feature 010.
    deps: T001@66697874

## Requirements And Success Traceability

| Specification coverage | Tasks |
|---|---|
| FR-001 through FR-009 / SC-001 through SC-003 | T001@66697874 |
| FR-004 through FR-008, FR-010 through FR-012 / SC-004 through SC-006 | T002@76616c69 |
