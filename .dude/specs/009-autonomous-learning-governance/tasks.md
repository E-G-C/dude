<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- T001@7365616c [US1] After the coordinator resolves T009@696e6369, records its Feature008 baseline, and immediately rechecks the source boundary, implement the fail-closed autonomous repeat seal in `src/skills/dude-work/recovery.mjs` and `src/skills/dude-work/recovery.test.mjs`; make `src/skills/dude-work/SKILL.md` the detailed owner and add concise autonomous-deferral pointers in `src/agents/dude.agent.md`, `src/instructions/dude.instructions.md`, `src/skills/dude-reviewer-protocol/SKILL.md`, and `src/skills/dude-receiving-code-review/SKILL.md`; prove through the public process CLI that an autonomous repeat cannot produce direct no-progress, escalation, block, close, controlled end, or another attempt, while guarded and non-Work behavior remains compatible; verify with `node --test src/skills/dude-work/recovery.test.mjs`.

### Done
- (none)

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/autonomous-learning-governance.md#coordinator-log -->

# Tasks: Autonomous Learning Governance

Nine all-open canonical units implement the approved immutable Feature009 definition contract at `.dude/specs/009-autonomous-learning-governance/spec.md`.

No task carries `[P]`. T001 through T007 contribute implementation or source-contract changes, T008 independently accepts the unchanged source revision without product writes, and T009 is the sole terminal Feature008 core-close task.

Execution may read but must not write any `.dude/specs/009-autonomous-learning-governance/**` artifact. This includes `spec.md`, `plan.md`, `research.md`, `data-model.md`, `contracts/schemas.md`, `quickstart.md`, `checklists/test.md`, `checklists/security.md`, and this `tasks.md`. A required normative change stops with `contract-mismatch: redefine-required`.

Durable suffixes remain the fixed lowercase hexadecimal encodings of `seal`, `iden`, `publ`, `halt`, `perm`, `lane`, `docs`, `proo`, and `inci`.

## Phase 1: Fail-Closed Autonomous Repeat Seal

**Goal**: Establish the Feature008 baseline before source mutation, then make repeated autonomous behavior enter learning-required before generic disposition or another affected-target transition.

**Implementation Write Set**:

- `src/skills/dude-work/recovery.mjs`
- `src/skills/dude-work/recovery.test.mjs`
- `src/skills/dude-work/SKILL.md`
- `src/agents/dude.agent.md`
- `src/instructions/dude.instructions.md`
- `src/skills/dude-reviewer-protocol/SKILL.md`
- `src/skills/dude-receiving-code-review/SKILL.md`

**Feature008 Baseline And Dependency Contract**:

- Before the first mutation under `src/**`, resolve the active open T009 authority from the live lane and validate its complete exact source declaration. In Lightweight execution, the canonical T009 header is authoritative; after tracked import, the corresponding Beads issue text is authoritative.
- Require immutable `HEAD`, clean `src/**`, clean base-owned generated core, no ignored entries in either owned boundary, and clean source/generated parity.
- After the clean preflight, the coordinator appends this bounded line to the unique Feature009 owner log:

  `- <UTC> - core-dogfood-baseline v1 terminal=T009@696e6369 head=<gitOid> src_tree=<gitTreeOid>`

- Immediately repeat the preflight before allowing T001's first source mutation.
- A missing or mismatched baseline, terminal declaration, clean boundary, or parity result blocks source mutation. Work waits for a clean boundary or uses an isolated worktree only after user approval.
- The baseline serializes all source work through T009. T001 does not materialize generated core or append accepted evidence; T009 exclusively owns those lifecycle steps.

**Execution Details**:

- Replace direct autonomous repeat-to-no-progress and generic escalation with fail-closed `learning-required`.
- Establish bounded pending-completion and learning-governance state needed to seal another affected-target attempt, generic escalation, no-progress, ordinary block, close, resolving status transition, and ordinary controlled end.
- Keep `src/skills/dude-work/SKILL.md` as the sole detailed operational authority.
- Keep coordinator, shared, reviewer, and receiving-review changes to concise authority, deferral, and evidence-preservation pointers.
- Preserve byte-compatible generic repeated-finding behavior for guarded and non-Work cycles.
- Add a process-level public CLI fixture proving that a repeated autonomous occurrence cannot directly produce no-progress or another disposition.
- End this phase fail-closed; continuation through a selected alternative is not yet enabled.

**Focused Verification**:

```bash
node --test src/skills/dude-work/recovery.test.mjs
```

**Independent Test**: Through serialized public CLI input, the first finding remains ordinarily recoverable, a second equivalent finding enters target-bound `learning-required`, all sealed transitions refuse, and corresponding guarded and non-Work fixtures remain compatible.

- [!] T001@7365616c [US1] After the coordinator resolves T009@696e6369, records its Feature008 baseline, and immediately rechecks the source boundary, implement the fail-closed autonomous repeat seal in `src/skills/dude-work/recovery.mjs` and `src/skills/dude-work/recovery.test.mjs`; make `src/skills/dude-work/SKILL.md` the detailed owner and add concise autonomous-deferral pointers in `src/agents/dude.agent.md`, `src/instructions/dude.instructions.md`, `src/skills/dude-reviewer-protocol/SKILL.md`, and `src/skills/dude-receiving-code-review/SKILL.md`; prove through the public process CLI that an autonomous repeat cannot produce direct no-progress, escalation, block, close, controlled end, or another attempt, while guarded and non-Work behavior remains compatible; verify with `node --test src/skills/dude-work/recovery.test.mjs`.
   blocked-by: external-dependency: Feature008 baseline requires a clean src and generated-core boundary with exact source/generated parity; existing unrelated boundary changes must be resolved or isolated first

## Phase 2: Trusted Occurrence And Governance Contracts

**Goal**: Retain trusted immutable occurrences before counting completion or deriving repetition, while keeping the approved definition contracts immutable.

**Implementation Write Set**:

- `src/skills/dude-work/recovery.mjs`
- `src/skills/dude-work/recovery.test.mjs`

**Execution Details**:

- Implement trusted VerificationEnvelopeV2 and IndependentReviewEnvelopeV2 normalization from authoritative captures.
- Accept caller identities only; reject caller-authored finding bodies and check states as transition authority.
- Implement immutable FindingOccurrenceEventV1 and ApproachOccurrenceEventV1 records.
- Split completion into capture and finalize so no occurrence is counted before fresh byte-equivalent retention on both authoritative surfaces.
- Implement closed finding and approach bases, chronology, replay handling, earliest qualifying Repeat Relationship derivation, and complete bounded failed-approach sets.
- Implement the singular bounded pending-completion and learning-governance records and closed governance phases.
- Keep LearningReviewEvent v1, legacy assessment equivalence, and prose-only findings audit-only.
- Treat the immutable Feature009 research, data model, schemas, quickstart, and checklists as read-only normative inputs.

**Focused Verification**:

```bash
node --test src/skills/dude-work/recovery.test.mjs
```

**Independent Test**: Table and process fixtures classify first, distinct, replayed, same-attempt, equal-basis, differently worded, missing, stale, and conflicting evidence according to the closed oracle; no repeat is derived before exact dual retention.

- [ ] T002@6964656e [Shared] Implement and test trusted VerificationEnvelopeV2 and IndependentReviewEnvelopeV2 normalization, immutable FindingOccurrenceEventV1 and ApproachOccurrenceEventV1 retention, two-stage completion, basis and chronology validation, replay handling, earliest Repeat Relationship derivation, complete failed-approach sets, bounded governance state, and v1 audit-only compatibility in `src/skills/dude-work/recovery.mjs` and `src/skills/dude-work/recovery.test.mjs`; accept only caller identity references, count no occurrence before fresh dual retention, and verify with `node --test src/skills/dude-work/recovery.test.mjs`.
    deps: T001@7365616c

## Phase 3: Public Learning And Exact Projection Batches

**Goal**: Make exact learning event bodies, dual projection, selected-alternative continuation, and verified no-progress reachable through public runtime and CLI paths.

**Implementation Write Set**:

- `src/skills/dude-work/recovery.mjs`
- `src/skills/dude-work/recovery.test.mjs`

**Execution Details**:

- Add exact response-carried ProjectionBatchV1 values with hash-only RunState commitments.
- Make `complete.capture`, `complete.finalize`, and `learn` return the exact event bodies required by projection.
- Require the exact batch in projection preparation and fresh projection verification.
- Validate every event body, event hash, batch commitment, target, owner mapping, and authoritative surface.
- Implement finding-triggered and approach-triggered branches against the complete failed-approach set.
- Require every credible alternative to differ materially from every failed approach and carry one discriminating check.
- Derive no-progress only from a complete projected no-alternative result plus fresh proof of no distinguishing evidence.
- Preserve operation with no objective and optional matching objective evidence without inventing an objective.
- Add serialized process-level flows; helper-only calls do not satisfy acceptance.

**Focused Verification**:

```bash
node --test src/skills/dude-work/recovery.test.mjs
```

**Independent Test**: Public process fixtures execute occurrence retention, repetition, learning, exact dual projection, selected-alternative continuation, and verified no-progress in order; missing bodies, hash mismatch, one-sided projection, incomplete failed-approach comparisons, and objective invention refuse.

- [ ] T003@7075626c [US2] Extend the public `runCommand`/CLI implementation and tests in `src/skills/dude-work/recovery.mjs` and `src/skills/dude-work/recovery.test.mjs` with two-stage completion, exact response-carried projection batches, batch commitments, reachable projection preparation and fresh verification, complete failed-approach material-difference checks, selected-alternative learning, verified no-progress, and no-objective operation; prove finding-triggered and approach-only branches through serialized process calls and verify with `node --test src/skills/dude-work/recovery.test.mjs`.
    deps: T002@6964656e

## Phase 4: Scoped Halts, Re-Derivation, Scheduling, And Audit

**Goal**: Preserve immediate halt and budget authority at the correct scope, resume from retained evidence, and audit only byte-equivalent authoritative history.

**Implementation Write Set**:

- `src/skills/dude-work/recovery.mjs`
- `src/skills/dude-work/recovery.test.mjs`

**Execution Details**:

- Classify target-scoped hard stops and per-target recovery-budget exhaustion without resolving learning or consuming unrelated scheduling authority.
- Keep run-wide safety, security, authority, credential, destructive-confirmation, spending, external-authorization, ownership, lane, unrecoverable-evidence, and overall-budget stops invocation-wide.
- Treat missing or conflicting halt scope as run-wide ambiguity.
- Resume required governance deterministically from dual-retained occurrence events.
- Reuse Feature005's exact sequential readiness, dependency-independence, and change-set-disjointness decision; add no scheduler or concurrent start.
- Permit unchanged affected-target suspension only under the approved projection or immediate-halt re-derivation rules.
- Implement Controlled Unresolved End without converting the target to blocked, completed, or no-progress.
- Derive AuditSummary v2 from the byte-equivalent event intersection, never the union.
- Report unresolved projection or re-derivation state without claiming a resolved disposition.

**Focused Verification**:

```bash
node --test src/skills/dude-work/recovery.test.mjs
```

**Independent Test**: Public fixtures prove target-scoped conditions preserve only eligible sequential disjoint scheduling, run-wide conditions start no other target, concurrency always refuses, controlled unresolved end does not become block or no-progress, resume restores governance, and audit rows match fresh authoritative intersection.

- [ ] T004@68616c74 [Shared] Implement target-scoped and run-wide halt precedence, per-target and overall budgets, unchanged suspension, deterministic governance re-derivation from retained occurrence events, three-way Immediate Halt End projection evidence with exact disposition equality, exact Feature005 sequential scheduling reuse, branch-gated Controlled Unresolved End from `alternative-inspected` or `no-progress-verified` but never `projected`, and conditional intersection-derived AuditSummary v2 in `src/skills/dude-work/recovery.mjs` and `src/skills/dude-work/recovery.test.mjs`; prove public halts after verified projection and before projection, no false target completion, authority transfer, or concurrency and verify with `node --test src/skills/dude-work/recovery.test.mjs`.
    deps: T003@7075626c

## Phase 5: Acyclic Permits, Receipts, And Incident Contracts

**Goal**: Bind authorization and lane mutation to separate fresh state phases and define evidence-safe incident supersession without fabricating repetition.

**Implementation Write Set**:

- `src/skills/dude-work/recovery.mjs`
- `src/skills/dude-work/recovery.test.mjs`

**Execution Details**:

- Implement pure AttemptAuthorizationPermitV1 issuance against the unchanged post-learning Inspection state.
- Make `authorize` validate and consume that permit before changing RunState.
- Derive any required LaneMutationPermitV1 from the post-authorization state.
- Implement ProjectionPermitV1, exact lane receipts, replay prevention, prestate enforcement, and terminal receipt commit.
- Keep failed mutation or receipt commit from clearing governance.
- Enforce the acyclic order: post-learning Inspection, attempt permit, `authorize`, optional lane claim, receipt commit, execution, retained completion, and terminal lane mutation.
- Implement AcceptedFeatureEvidenceV1, including `mode:"core-close"` and `acceptedFeatureEvidenceIdentity`.
- Implement IncidentSupersessionEventV1 separately from Governance Event.
- Implement the closed exact-evidence and evidence-incomplete Feature007 preview union from the immutable schemas.
- Forbid a Repeat Relationship or Governance Event in the evidence-incomplete branch.

**Focused Verification**:

```bash
node --test src/skills/dude-work/recovery.test.mjs
```

**Independent Test**: Permit fixtures accept one exact fresh transition and reject stale, replayed, forged, transferred, changed-prestate, mismapped, wrong-phase, or post-authorization reuse; incident fixtures prove the incomplete branch cannot fabricate a repeat or Governance Event.

- [ ] T005@7065726d [US3] Implement and test acyclic AttemptAuthorizationPermitV1 issuance and consumption, post-authorization LaneMutationPermitV1 derivation, ProjectionPermitV1, exact receipt commit, replay and prestate enforcement, AcceptedFeatureEvidenceV1, IncidentCorrectionIntentV1, intent-bound IncidentSupersessionEventV1, the dedicated finding-only incident-evidence batch, and the exact/incomplete acyclic Feature007 preview union in `src/skills/dude-work/recovery.mjs` and `src/skills/dude-work/recovery.test.mjs`; enforce the order post-learning Inspection, attempt permit, `authorize`, optional lane claim, retained completion, incident intent, events and batches, preview, exact mutation, and terminal receipt, and verify with `node --test src/skills/dude-work/recovery.test.mjs`.
    deps: T004@68616c74

## Phase 6: Lightweight And Tracked Trust Boundaries

**Goal**: Implement the already closed immutable wrapper schemas at both lane boundaries with exact mapping, prestate, mutation, and receipt verification.

**Implementation Write Set**:

- `src/skills/dude-lightweight-execution/board.mjs`
- `src/skills/dude-lightweight-execution/board.test.mjs`
- `src/skills/dude-lightweight-execution/SKILL.md`
- `library/packs/beads/skills/dude-pack-beads-workflow/beads.mjs`
- `library/packs/beads/skills/dude-pack-beads-workflow/beads.test.mjs`
- `library/packs/beads/skills/dude-pack-beads-workflow/SKILL.md`

**Execution Details**:

- Implement the closed Lightweight `work-project` and `work-set` requests defined by the immutable Feature009 schemas.
- Implement the closed tracked `work-project` and `work-transition` requests defined by those same schemas.
- Bind canonical root, exact owner capture, target, full RunState, permit, unique mapping, lane-specific expected captures, exact mutation event lines, and poststate receipt.
- Reject unknown fields, caller command strings, free-form governance flags, ambiguous mapping, and stale or mismatched expected bytes.
- Freshly reacquire every authoritative prestate and recompute owner, mapping, state, permit, every event parsed from `mutation.eventLines`, and mutation identities before mutation.
- Reacquire poststate before returning a receipt.
- Leave all authoritative surfaces byte-for-byte unchanged on refusal.
- Preserve existing manual, guarded, non-Work, mirror, and coordinator-maintenance authority.
- Do not revise Feature009 schemas or supporting artifacts during implementation.

**Focused Verification**:

```bash
node --test src/skills/dude-lightweight-execution/board.test.mjs
node --test library/packs/beads/skills/dude-pack-beads-workflow/beads.test.mjs
```

**Independent Test**: Lightweight and tracked integration matrices accept only complete valid envelopes, reject every omitted, unexpected, stale, mismapped, forged, or transferred field without mutation, and return an exact receipt only after fresh poststate capture.

- [ ] T006@6c616e65 [US3] Implement the immutable closed wrapper schemas through permit-aware `work-project` and `work-set` boundaries in `src/skills/dude-lightweight-execution/board.mjs`, `src/skills/dude-lightweight-execution/board.test.mjs`, and `src/skills/dude-lightweight-execution/SKILL.md`, and tracked `work-project`, `work-transition`, proof-only `work-prove-poststate`, and owner-only `work-reconcile-owner` boundaries in `library/packs/beads/skills/dude-pack-beads-workflow/beads.mjs`, `library/packs/beads/skills/dude-pack-beads-workflow/beads.test.mjs`, and `library/packs/beads/skills/dude-pack-beads-workflow/SKILL.md`; implement EventLineText and exact LF serialization, complete lane-specific mutation hashing, closed transition, refusal, and recovery matrices, atomic Lightweight tasks/snapshot/owner application, tracked lane-plus-owner composite receipts, stage-bound recovery identities, no-redispatch poststate proof, and idempotent owner reconciliation; verify exact root, owner, target, state, permit, mapping, expected or observed bytes, blocker text, event line, owner-log lines, mutation, recovery evidence, poststate receipt, replay behavior, no false success, and byte-preserving refusal with the two focused suites.
    deps: T005@7065726d

## Phase 7: Static Contracts And Documentation

**Goal**: Keep one detailed Work authority and align source pointers, static contracts, and user documentation without materializing generated core.

**Source And Documentation Write Set**:

- `src/agents/dude.agent.md`
- `src/instructions/dude.instructions.md`
- `src/skills/dude-work/SKILL.md`
- `src/skills/dude-lightweight-execution/SKILL.md`
- `src/skills/dude-reviewer-protocol/SKILL.md`
- `src/skills/dude-receiving-code-review/SKILL.md`
- `scripts/current-format-contract.test.mjs`
- `docs/commands.md`
- `docs/workflow.md`
- `docs/reference.md`

**Execution Details**:

- Assert that `src/skills/dude-work/SKILL.md` is the sole detailed autonomous learning-governance owner.
- Keep other source prompt surfaces concise and limited to authority, deferral, evidence, and wrapper-use pointers.
- Assert public `inspect`, `authorize`, `complete`, `learn`, `transition`, and `audit` reachability without presenting new user-facing workflow commands.
- Cover retention-first completion, trusted source evidence, projection batches, complete failed-approach comparison, acyclic permit ordering, scoped halts and budgets, sequential disjoint scheduling, controlled unresolved end, resume, and conditional audit.
- Assert guarded and non-Work parity, autonomous reviewer deferral, no concurrency, no new store or lane, and current-only state.
- Align `docs/commands.md`, `docs/workflow.md`, and `docs/reference.md`.
- Do not run repository `build-dev` and do not write generated `.github` core. Generated materialization is deferred exclusively to T009.

**Focused Verification**:

```bash
node --test scripts/current-format-contract.test.mjs
```

**Independent Test**: Static tests fail if detailed policy is duplicated, autonomous disposition returns to the generic reviewer, retention or projection is bypassed, permit order becomes cyclic, a Work mutation bypasses its wrapper, sequential scheduling broadens to concurrency, guarded behavior changes, or generated core is materialized early.

- [ ] T007@646f6373 [US7] Finalize concise source authority pointers in `src/agents/dude.agent.md`, `src/instructions/dude.instructions.md`, `src/skills/dude-work/SKILL.md`, `src/skills/dude-lightweight-execution/SKILL.md`, `src/skills/dude-reviewer-protocol/SKILL.md`, and `src/skills/dude-receiving-code-review/SKILL.md`; enforce retention-first completion, exact projection reachability, trusted review evidence, acyclic permits, wrapper use, scoped sequential scheduling, no concurrency, guarded parity, and one detailed Work owner in `scripts/current-format-contract.test.mjs`; align `docs/commands.md`, `docs/workflow.md`, and `docs/reference.md`; do not materialize generated core; verify with `node --test scripts/current-format-contract.test.mjs`.
    deps: T006@6c616e65

## Phase 8: Freeze And Independently Accept Source Revision

**Goal**: Freeze and independently accept the exact unmaterialized source revision before generated-core materialization becomes eligible.

**Planned Product Write Set**: None.

**Execution Details**:

- Reacquire the active T009 declaration from the live lane and recompute its complete declaration identity.
- Recompute the current source and baseline-changed identities.
- Require every changed `src/**` path to appear in the T009 declaration and require no undeclared, duplicate, directory, glob, or unsorted entry.
- Freeze the exact source revision and run all focused source, lane-wrapper, pack-wrapper, and static-contract suites.
- Run mutation cases covering the nine corrected architecture boundaries: definition mutation, unreachable event bodies, cyclic permits, pre-retention repetition, fabricated incident governance, caller-authored findings or checks, incomplete failed-approach comparison, open lane schemas, and premature core materialization.
- Route the same fresh evidence and exact unchanged source identities independently to the Tester and Code Reviewer.
- Require both to accept public-path ordering, trusted evidence, both repeat channels, complete failed-approach comparison, both lanes, halt and budget scopes, audit outcomes, guarded parity, and Feature007 correction safety.
- Any failure leaves T008 open and routes correction to the owning T001-T007 source task, after which T008 repeats over a new exact source identity.
- Make no product, source, generated-core, owner-log, Feature007, or Feature009-package write.
- Do not run repository `build-dev` and do not append the Feature008 accepted line.

**Focused Source Verification**:

```bash
node --test src/skills/dude-work/recovery.test.mjs
node --test src/skills/dude-lightweight-execution/board.test.mjs
node --test library/packs/beads/skills/dude-pack-beads-workflow/beads.test.mjs
node --test scripts/current-format-contract.test.mjs
```

**Independent Test**: Tester evidence and independent Code Reviewer acceptance bind the same unmodified source revision, declaration, source identity, changed identity, and verification set, with no unresolved finding and no materialization or product write.

- [ ] T008@70726f6f [Shared] Freeze the complete declared Feature009 source revision, prove every changed `src/**` path belongs to terminal T009@696e6369, run the focused and mutation suites, and route the same fresh evidence and exact source identities independently to the Tester and Code Reviewer; require source acceptance with no unresolved finding, make no product write, do not append accepted owner-log evidence, and do not run repository `build-dev`.
    deps: T007@646f6373

## Phase 9: Terminal Core Close And Feature007 Supersession

**Goal**: After unchanged T008 source acceptance, materialize and independently accept generated core, record current Feature008 evidence, derive `acceptedFeatureEvidenceIdentity`, and apply only the evidence-supported Feature007 supersession.

**Source Write Set**: None.

**Generated Core Write Set**:

- `.github/agents/dude.agent.md`
- `.github/instructions/dude.instructions.md`
- `.github/skills/dude-lightweight-execution/SKILL.md`
- `.github/skills/dude-lightweight-execution/board.mjs`
- `.github/skills/dude-receiving-code-review/SKILL.md`
- `.github/skills/dude-reviewer-protocol/SKILL.md`
- `.github/skills/dude-work/SKILL.md`
- `.github/skills/dude-work/recovery.mjs`

**Coordinator-Owned Feature009 Lifecycle Evidence**:

- `.dude/ideas/autonomous-learning-governance.md`

Only the bounded append-only Feature008 accepted line may be added after fresh final acceptance:

`- <UTC> - core-dogfood-accepted v1 terminal=T009@696e6369 head=<gitOid> declared=<sha256> source=<sha256> changed=<sha256> review=<sha256>`

**Exclusive Feature007 Correction Write Set**:

- `.dude/ideas/technical-docs-pack-remediation.md`
- `.dude/specs/007-technical-docs-pack-remediation/tasks.md`
- `.dude/state/task-state.json`

**Execution Details**:

1. Require successful T008 acceptance over unchanged declaration, source, changed, verification, and review identities.
2. Reacquire the latest matching Feature008 baseline and recheck `HEAD`, active lane declaration, source identity, changed identity, generated prestate, ignored entries, and protected boundaries.
3. Snapshot installed packs, project-local skill bytes, workflows, and `.dude/**`.
4. Run `node scripts/build-dev.mjs`; no generated core path is hand-edited.
5. Prove exact source/generated parity, intended generated scope, source-test exclusion, stale cleanup, idempotence, and complete protected path/type/content preservation.
6. Run focused tests, build tests, the full discovered repository suite, Dude lint, compose verification, pristine external release build and lint, generated-scope checks, final parity, and whitespace checks.
7. Obtain fresh independent final review over the exact materialized identities and complete verification evidence.
8. Only after that approval, append the bounded Feature008 accepted line to the unique Feature009 owner log and immediately recompute every bound identity.
9. Derive `acceptedFeatureEvidenceIdentity` from the current `mode:"core-close"` AcceptedFeatureEvidenceV1 record. A stale line or unavailable transient review requires fresh review and a later accepted line.
10. Freshly capture and hash the complete Feature007 owner and task bytes, canonical owner identity, board and T001 state, task-state snapshot, original policy and target, available attempt and review records, occurrence evidence, Inspections, Lightweight history, projected events, verification evidence, owner-log tail, and absence of a later supersession.
11. Produce a read-only correction preview with exact prestate hashes, evidence classification, append-only events, exact task and snapshot mutation, and rollback bytes.
12. Select the exact-evidence branch only when one stable basis, two distinct valid retained occurrences, distinct attempts, strict chronology, and no replay or conflict are freshly proved.
13. In the exact-evidence branch, append the supported occurrence, Governance, and IncidentSupersession evidence; preserve prior history; issue the exact incident-supersession permit; change only Feature007 T001 from `[!]` to `[~]`; remove its obsolete blocker; and leave it unresolved as learning-required.
14. For unavailable, partial, duplicated, stale, or conflicting occurrence evidence, use the evidence-incomplete branch. Append no Repeat Relationship or Governance Event, append only the supported IncidentSupersession evidence, preserve Feature007 T001 as `[!]`, and replace its invalid blocker with exactly `contract-mismatch: evidence-incomplete autonomous review occurrence evidence unavailable`.
15. Apply either branch only through the permit-aware Lightweight boundary and only to the three declared Feature007 correction paths.
16. Run fresh Dude lint, the public governance audit, and independent correction review.
17. Recheck the latest matching Feature008 accepted evidence before terminal close.
18. Perform no technical-docs source, pack, test, specification, plan, support-artifact, or implementation work, and do not dispatch or resume Feature007 T001.
19. Leave T009 open on any mismatch, failed gate, stale identity, rejected review, partial write, or unsupported correction branch.

**Full Validation**:

```bash
node scripts/build-dev.mjs
node --test src/skills/dude-work/recovery.test.mjs
node --test src/skills/dude-lightweight-execution/board.test.mjs
node --test library/packs/beads/skills/dude-pack-beads-workflow/beads.test.mjs
node --test scripts/current-format-contract.test.mjs scripts/build-dev.test.mjs scripts/build-release.test.mjs
find . -path ./dist -prune -o -name '*.test.mjs' -print0 | xargs -0 node --test
node .github/skills/dude-lint/lint.mjs .
node .github/skills/dude-compose/compose.mjs verify
RELEASE_ROOT="$(mktemp -d)"
node scripts/build-release.mjs --out "$RELEASE_ROOT/bundle" --tag v0.0.0
node "$RELEASE_ROOT/bundle/.github/skills/dude-lint/lint.mjs" "$RELEASE_ROOT/bundle"
git status --porcelain -- .github
git diff --check
```

**Independent Test**: T009 refuses unless T008 acceptance remains current; materialization produces exact generated parity without protected drift; full verification and final independent review bind one unchanged revision; the accepted line and `acceptedFeatureEvidenceIdentity` recompute; and exactly one supported Feature007 branch preserves history, touches only its three declared paths, receives independent correction review, and performs no technical-docs implementation.

- [ ] T009@696e6369 [Shared] Materialize and accept core dogfood, then apply the evidence-safe Feature007 incident supersession; declared-src: `src/agents/dude.agent.md`, `src/instructions/dude.instructions.md`, `src/skills/dude-lightweight-execution/SKILL.md`, `src/skills/dude-lightweight-execution/board.mjs`, `src/skills/dude-lightweight-execution/board.test.mjs`, `src/skills/dude-receiving-code-review/SKILL.md`, `src/skills/dude-reviewer-protocol/SKILL.md`, `src/skills/dude-work/SKILL.md`, `src/skills/dude-work/recovery.mjs`, `src/skills/dude-work/recovery.test.mjs`; after unchanged T008 source acceptance, recheck the Feature008 baseline and identities, run `node scripts/build-dev.mjs`, prove exact generated parity and protected preservation, run all full gates, obtain fresh final independent approval, append and immediately verify the matching accepted owner-log evidence, derive `acceptedFeatureEvidenceIdentity`, then derive one acyclic IncidentCorrectionIntentV1, derive only the branch-authorized events and batches, compute one previewIdentity that no event references, bind intentIdentity and previewIdentity into the complete Lightweight mutation, and atomically apply only the supported append-only exact-evidence or evidence-incomplete Feature007 supersession to `.dude/ideas/technical-docs-pack-remediation.md`, `.dude/specs/007-technical-docs-pack-remediation/tasks.md`, and `.dude/state/task-state.json`; run fresh lint, audit, and independent correction review, perform no technical-docs implementation, and leave open on any mismatch.
    deps: T001@7365616c, T002@6964656e, T003@7075626c, T004@68616c74, T005@7065726d, T006@6c616e65, T007@646f6373, T008@70726f6f

## Requirements And Success Traceability

| Specification coverage | Plan ownership | Tasks |
|---|---|---|
| Feature009 FR-001, FR-002, FR-024, FR-025 / SC-001, SC-002, SC-003, SC-009 | Autonomous precedence, immediate seal, scoped halts and budgets, guarded compatibility | T001@7365616c, T004@68616c74, T007@646f6373, T008@70726f6f, T009@696e6369 |
| Feature009 FR-003 through FR-006 / SC-004 | Trusted bases, distinct retained occurrences, chronology, replay, incomplete evidence | T002@6964656e, T003@7075626c, T008@70726f6f, T009@696e6369 |
| Feature009 FR-007 through FR-012 / SC-001, SC-002, SC-005 | Learning-required ordering, complete failed-approach alternatives, verified no-progress | T001@7365616c, T002@6964656e, T003@7075626c, T005@7065726d, T008@70726f6f, T009@696e6369 |
| Feature009 FR-013 through FR-015 / SC-006 | Singular governance, exact projection, unresolved retention, re-derivation, controlled end | T002@6964656e, T003@7075626c, T004@68616c74, T006@6c616e65, T008@70726f6f, T009@696e6369 |
| Feature009 FR-016 through FR-018 / SC-005, SC-007 | Fresh post-learning Inspection, evidence drift, alternative verification and review | T003@7075626c, T005@7065726d, T006@6c616e65, T008@70726f6f, T009@696e6369 |
| Feature009 FR-019 through FR-021 / SC-003, SC-010 | Target seal, acyclic permits, exact receipts, lane mapping and non-transfer | T001@7365616c, T005@7065726d, T006@6c616e65, T007@646f6373, T008@70726f6f, T009@696e6369 |
| Feature009 FR-022, FR-023 / SC-008 | No-objective, unmatched-objective, and optional matching-objective behavior | T002@6964656e, T003@7075626c, T008@70726f6f, T009@696e6369 |
| Feature009 FR-026, FR-027 / SC-011 | Fresh conditional audit from authoritative event intersection | T004@68616c74, T007@646f6373, T008@70726f6f, T009@696e6369 |
| Feature009 FR-028 / SC-001 through SC-003 | Fail-closed rollout before selected-alternative continuation | T001@7365616c through T009@696e6369 |
| Feature009 FR-029, FR-030 / SC-012, SC-015 | Public-path and mutation tests, static contracts, full validation, independent acceptance | T001@7365616c, T003@7075626c, T006@6c616e65, T007@646f6373, T008@70726f6f, T009@696e6369 |
| Feature009 FR-031, FR-032 / SC-013 | Accepted-feature evidence and exact/incomplete append-only Feature007 correction | T005@7065726d, T008@70726f6f, T009@696e6369 |
| Feature009 FR-033 / SC-014 | Preserve Feature005 and Feature008; prohibit technical-docs remediation | T004@68616c74, T007@646f6373, T008@70726f6f, T009@696e6369 |
| Feature009 FR-034 / SC-014 | No new user command, lane, service, store, concurrency, scheduler, or delivery action | T001@7365616c through T009@696e6369 |
| Feature008 FR-005 through FR-008 / SC-001, SC-003 | One open non-parallel shared terminal, exact sorted source declaration, direct contributor dependencies, and independent malformed-definition rejection | Definition readiness review, T001@7365616c, T008@70726f6f, T009@696e6369 |
