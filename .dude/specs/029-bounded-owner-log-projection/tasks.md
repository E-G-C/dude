<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@70726f6a [US1] Implement `plan.md` Chosen Design sections 1 through 4 in `src/skills/dude-work/recovery.mjs`: parse top-level dated Coordinator Log events with whole Unicode/multiline continuations and comment framing; emit the one exact closed owner-log body; select the maximal chronological suffix by rebuilding the complete canonical packet against all current evidence in `buildInspection`; retain the newest event or current descriptor-only overflow; route every direct consumer through the new validator; and derive Feature 007/Feature 009 `ownerLogTailHash` directly from `fullLogSha256`. Extend `src/skills/dude-work/recovery.test.mjs` for small logs, exact metadata and descriptor binding, Unicode/multiline/framing, exact maximality, oversized-newest and non-owner overflow, owner resolution, definition reconciliation, learning presence, old-shape rejection, and incident prestate. Use `.dude/ideas/agent-orchestration-metadata.md` and `.dude/ideas/remove-legacy-compatibility.md` read-only and prove their bytes do not change. Add no fixed reservation, generic budget framework, compatibility parser, state, archive, compression, batch, config, migration, or production module. (US1, US2, US3 -> FR-001 through FR-012; SC-001 through SC-009)
- T002@73657474 [US2] Extend `src/skills/dude-work/host-adapter.test.mjs` with the plan's temporary-workspace settlement regression driven through `runHostAdapter`, the actual specialist-attestation/safety-writer capture path, and structurally valid Tester and Reviewer results of two different sizes; do not fabricate raw review, verification, trusted-capture, or Inspection fixtures. Require fresh packets under 65,536 bytes, a strict suffix-size adjustment, exact maximality, and an unchanged owner file. If a direct terminal route is structurally unreachable, document the dominating production boundary and test the actual reachable capture-to-fresh-Inspection boundary without bypassing it. Update `src/skills/dude-work/SKILL.md`, `docs/commands.md`, `docs/reference.md`, `docs/workflow.md`, and `scripts/current-format-contract.test.mjs` for bounded owner history, complete-log metadata, omitted-history honesty, one current body, and no compatibility read. Run `node scripts/build-dev.mjs` to refresh only intended `.github/skills/dude-work/**` projections and prove source/generated parity. (US2, US3 -> FR-002 through FR-014; SC-004, SC-005, SC-008 through SC-010)
- T003@61637074 [Shared] Run acceptance over one unchanged integrated revision: focused recovery and host-adapter tests; current-format contracts; the full recursively discovered suite; `node scripts/build-dev.mjs` twice with an idempotent second run and intended generated-core parity; Dude lint; compose verification; pristine release build and release lint; and `git diff --check`. Recheck exact packet maximality, descriptor-only overflow, actual settlement reachability, every direct owner-log consumer, complete-digest incident binding, one production body with no `coordinatorLog` compatibility path, and byte-identical Feature 004, Feature 009, Feature 028, and `remove-legacy-compatibility` historical inputs. Route the unchanged diff and same evidence for independent review of safety, guidance honesty, and YAGNI compliance. Do not mutate this definition package, task state, either oversized ledger, or generated core by hand while obtaining evidence. (US1, US2, US3 -> FR-001 through FR-014; SC-001 through SC-010)

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/029-bounded-owner-log-projection.md#coordinator-log -->

# Tasks: Bounded Owner Log Projection

Three all-open canonical units implement `.dude/specs/029-bounded-owner-log-projection/spec.md`, owned prospectively and exactly by `.dude/ideas/bounded-owner-log-projection.md`.

No task carries `[P]`. Runtime shape and safety land first, the actual settlement path and generated bundle follow, and final acceptance uses one unchanged integrated revision.

Core source lives under `src/`; `.github/skills/dude-work/**` is generated only through `node scripts/build-dev.mjs` and is never hand-edited. The existing owner ledgers at `.dude/ideas/agent-orchestration-metadata.md` and `.dude/ideas/remove-legacy-compatibility.md` are read-only regression inputs. No task may mutate them, rewrite Feature 004 or Feature 009 history, add a compatibility body, or change stored Coordinator Log bytes.

The plan's exact body, whole-event parser, fresh-packet selection, overflow, and no-added-complexity rules are normative. A required change outside those boundaries stops as `contract-mismatch: redefine-required`.

## Phase 1: Runtime Projection And Focused Safety

**Goal**: Replace the complete-text model body at the current recovery chokepoint while preserving complete-log and incident safety.

- [x] T001@70726f6a [US1] Implement `plan.md` Chosen Design sections 1 through 4 in `src/skills/dude-work/recovery.mjs`: parse top-level dated Coordinator Log events with whole Unicode/multiline continuations and comment framing; emit the one exact closed owner-log body; select the maximal chronological suffix by rebuilding the complete canonical packet against all current evidence in `buildInspection`; retain the newest event or current descriptor-only overflow; route every direct consumer through the new validator; and derive Feature 007/Feature 009 `ownerLogTailHash` directly from `fullLogSha256`. Extend `src/skills/dude-work/recovery.test.mjs` for small logs, exact metadata and descriptor binding, Unicode/multiline/framing, exact maximality, oversized-newest and non-owner overflow, owner resolution, definition reconciliation, learning presence, old-shape rejection, and incident prestate. Use `.dude/ideas/agent-orchestration-metadata.md` and `.dude/ideas/remove-legacy-compatibility.md` read-only and prove their bytes do not change. Add no fixed reservation, generic budget framework, compatibility parser, state, archive, compression, batch, config, migration, or production module. (US1, US2, US3 -> FR-001 through FR-012; SC-001 through SC-009)

## Phase 2: Actual Settlement And Bundle Integration

**Goal**: Prove fresh fitting through the real trusted-capture boundary and make current guidance and generated output honest.

- [x] T002@73657474 [US2] Extend `src/skills/dude-work/host-adapter.test.mjs` with the plan's temporary-workspace settlement regression driven through `runHostAdapter`, the actual specialist-attestation/safety-writer capture path, and structurally valid Tester and Reviewer results of two different sizes; do not fabricate raw review, verification, trusted-capture, or Inspection fixtures. Require fresh packets under 65,536 bytes, a strict suffix-size adjustment, exact maximality, and an unchanged owner file. If a direct terminal route is structurally unreachable, document the dominating production boundary and test the actual reachable capture-to-fresh-Inspection boundary without bypassing it. Update `src/skills/dude-work/SKILL.md`, `docs/commands.md`, `docs/reference.md`, `docs/workflow.md`, and `scripts/current-format-contract.test.mjs` for bounded owner history, complete-log metadata, omitted-history honesty, one current body, and no compatibility read. Run `node scripts/build-dev.mjs` to refresh only intended `.github/skills/dude-work/**` projections and prove source/generated parity. (US2, US3 -> FR-002 through FR-014; SC-004, SC-005, SC-008 through SC-010)
    deps: T001@70726f6a

## Phase 3: Integrated Acceptance And Independent Review

**Goal**: Prove the complete current-format bundle without changing historical ledgers, definition artifacts, or the accepted revision.

- [x] T003@61637074 [Shared] Run acceptance over one unchanged integrated revision: focused recovery and host-adapter tests; current-format contracts; the full recursively discovered suite; `node scripts/build-dev.mjs` twice with an idempotent second run and intended generated-core parity; Dude lint; compose verification; pristine release build and release lint; and `git diff --check`. Recheck exact packet maximality, descriptor-only overflow, actual settlement reachability, every direct owner-log consumer, complete-digest incident binding, one production body with no `coordinatorLog` compatibility path, and byte-identical Feature 004, Feature 009, Feature 028, and `remove-legacy-compatibility` historical inputs. Route the unchanged diff and same evidence for independent review of safety, guidance honesty, and YAGNI compliance. Do not mutate this definition package, task state, either oversized ledger, or generated core by hand while obtaining evidence. (US1, US2, US3 -> FR-001 through FR-014; SC-001 through SC-010)
    deps: T002@73657474

## Requirements And Success Traceability

| Specification coverage | Tasks |
|---|---|
| FR-001 through FR-008 / SC-001 through SC-007 | T001@70726f6a, T002@73657474, T003@61637074 |
| FR-009 through FR-011 / SC-008, SC-009 | T001@70726f6a, T003@61637074 |
| FR-012, FR-013 / SC-009 | T002@73657474, T003@61637074 |
| FR-014 / SC-003 through SC-010 | T001@70726f6a, T002@73657474, T003@61637074 |
