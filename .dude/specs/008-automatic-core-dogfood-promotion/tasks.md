<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@8f2c1a47 [Shared] Write policy-coverage and temporary-Git tests first, then implement the project-local `Core Dogfood Close` convention and early bounded CI drift contract in `.github/skills/project/SKILL.md`, `.github/workflows/ci.yml`, and `scripts/current-format-contract.test.mjs`; verify with `node --test scripts/current-format-contract.test.mjs`.
- T002@5b7d930e [Shared] Strengthen `scripts/build-dev.test.mjs` first to prove complete installed-pack, project-skill, workflow, and `.dude/**` path/type/content preservation plus exact parity, source-test exclusion, stale cleanup, and idempotence; modify `scripts/build-dev.mjs` only if that focused test proves a concrete defect, and verify with `node --test scripts/build-dev.test.mjs`.
- T004@e2a91f6c [Shared] Write contract tests first, create `.github/skills/dude-local-core-dogfood-promotion/SKILL.md` with the reusable post-readiness promotion procedure, retain the complete executable pre-terminal baseline contract plus concise trigger and route in `.github/skills/project/SKILL.md`, and update `scripts/current-format-contract.test.mjs`; make no `src/**` or base-owned generated-core write and verify with `node --test scripts/current-format-contract.test.mjs`.
- T005@3d7b0af5 [Shared] Write the failing assertions first, then close the three recorded pre-resolver gaps: make the guard in `.github/skills/project/SKILL.md` fail closed on a nonzero `git ls-files -v -z -- src` exit status, correct its remaining `eight-command cleanliness block` label to the ten-command preflight, and anchor the ordering regression in `scripts/current-format-contract.test.mjs` on the guard fence instead of the first prose mention; make no `src/**` or base-owned generated-core write and verify with `node --test scripts/current-format-contract.test.mjs`.
- T006@62726964 [Shared] Write tests first in `scripts/current-format-contract.test.mjs`, then minimally update `.github/skills/project/SKILL.md` and `.github/skills/dude-local-core-dogfood-promotion/SKILL.md` to designate only Feature 009 `T009@696e6369` as the first adopter, preserve and consume its immutable original valid pre-source baseline through fail-closed explicit-authorized descendant continuity, require unchanged accepted T008 source identity, exact original-baseline changed-set coverage by the live T009 declaration, original-baseline generated prestate, current authority and protected boundaries, no unrelated or concurrent drift, unchanged normal future gates, and the exact T006 -> T009 -> T002 -> T003 sequence; verify focused Core Dogfood and full current-format contracts, Dude lint, scope/no-source/no-generated-core/no-Feature009-write checks, and independent review; do not run repository `build-dev`, append baseline or accepted evidence, create a helper/runtime/command/state/ledger/framework/report, materialize generated core, mutate Feature 009, or close Feature 008.
- T007@9a4e7c12 [Shared] Write failing pre-materialization route assertions first in `scripts/current-format-contract.test.mjs`, then minimally update `.github/skills/project/SKILL.md` and `.github/skills/dude-local-core-dogfood-promotion/SKILL.md` to preserve T009's accepted ten-path declaration; freshly bind the complete 20-path original-baseline delta and exact disjoint nine-path Feature 003 plus one-path Feature 006 attribution; bind all source/generated dirt layers, deterministic complete projection and cleanup, protected prestate, passable focused source/policy/runtime checks, and the sole exact named parity-sensitive failure; require Tester and Reviewer to independently reacquire or verify authoritative evidence; preserve the accepted-line shape while applying ten-path `declared`, complete `source`, and 20-path `changed` semantics through authorization, final review, recheck, latest-match evidence, and close; verify the complete disjoint scope of eight T009-owned outputs plus five live-derived contributor-owned outputs, five no-output results, unchanged inventory, and cleanup without transferring ownership; retain every post-materialization full-suite, parity, lint, compose, release, scope, whitespace, review, and terminal gate; invalidate and freshly reacquire the transient packet after any change; add no persistent packet/helper/runtime/command/state/store/ledger/schema/framework/ObjectiveRegistry/API/report; verify all applicable parity-independent contracts, conditional passable ancillary checks, deterministic scope, and independent acceptance; do not run repository `build-dev`, append evidence, mutate `src/**`, generated core, Feature 009, contributor artifacts, glyphs, blocker metadata, or state.
- T003@c4e6812d [Shared] Complete the explicit bootstrap no-source acceptance; declared-src: none; prove no accepted Feature 008 `src/**` or base-owned generated-core delta, do not run repository `build-dev` as a mutation, and validate the complete pre-terminal baseline contract plus the accepted one-time Feature 009 `T009@696e6369` route: passable pre-materialization checks and exact isolated parity delta; independently reacquired or verified Tester and Reviewer packet approval; unchanged ten-path T009 declaration; complete 20-path original-baseline `changed` identity with exact disjoint 9/1 contributor attribution; complete projection of eight T009-owned plus five live-derived contributor-owned outputs, five no-output results, unchanged inventory, and cleanup; mandatory post-materialization full suite, exact parity, lint, compose, pristine release, scope, whitespace, every terminal gate, and final review; unchanged accepted-line shape and event interpretation through immediate recheck, latest-match evidence, and close; no packet persistence, ownership transfer, contributor re-acceptance, or later-feature weakening; complete the Plan Phase 6 verification and bounded final owner-log evidence, obtain independent final review, and leave the task open or blocked on any failure.

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/automatic-core-dogfood-promotion.md#coordinator-log -->

# Tasks: Automatic Core Dogfood Promotion

Seven canonical units preserve the accepted project-local and CI implementation, hold the blocked materializer contract, preserve completed pre-terminal and first-adopter continuity work, revise one bounded pre-materialization acceptance route, and retain the explicit final bootstrap acceptance.

T001, T004, T005, and T006 remain done one-to-one with byte-identical canonical task units. T002 remains blocked one-to-one with its exact external-dependency blocker unchanged. T007 remains open, depends on completed T006 rather than T002, and is the sole ready Feature 008 task; its durable identity, glyph, and dependency metadata are unchanged. T003 remains open one-to-one, retains its no-source terminal, and keeps dependencies on T007 and T002 while its acceptance wording aligns with the revised gate, evidence, and generated-scope contracts. This revision adds and drops zero tasks and changes no glyph, blocker metadata, dependency metadata, board, snapshot, or execution state.

No generated board, `## Discovered During Execution`, or `## Lightweight Execution History` section is included.

## Phase 1: Existing Project-Local Close And CI Contract

**Goal**: Preserve the accepted model-facing procedural convention, policy-only static coverage, and bounded CI drift backstop as the extraction baseline.

**Write Set**:

- `.github/skills/project/SKILL.md`
- `.github/workflows/ci.yml`
- `scripts/current-format-contract.test.mjs`

**Execution Details**:

- T001 is complete historical implementation. T004 redistributes procedure ownership without reopening it or transferring state.
- Preserve its accepted CI boundary and evidence semantics as specified in Plan Sections 4, 5, 6, and 13.

**Independent Test**: The accepted focused contract test and independent review remain historical execution evidence; fresh final validation occurs in T003.

- [x] T001@8f2c1a47 [P] [Shared] Write policy-coverage and temporary-Git tests first, then implement the project-local `Core Dogfood Close` convention and early bounded CI drift contract in `.github/skills/project/SKILL.md`, `.github/workflows/ci.yml`, and `scripts/current-format-contract.test.mjs`; verify with `node --test scripts/current-format-contract.test.mjs`.

## Phase 2: Materializer Preservation And Parity Contract

**Goal**: Prove the existing materializer preserves the complete protected boundary and remains an exact idempotent projection after Feature 009 performs its terminal materialization.

**Execution Details**:

- Primary write-set: `scripts/build-dev.test.mjs`.
- Conditional write-set: `scripts/build-dev.mjs` only if a correctly scoped new test demonstrates a production defect.
- Preserve the accepted focused preservation, exact projection, source-test exclusion, stale cleanup, and repeatability meaning.
- T002 resumes only after Feature 009 `T009@696e6369` materializes and accepts generated core under the first-adopter bridge.
- Make no production change without the specified focused failing test.

**Independent Test**: A fixture mutation to any protected path, unexpected generated path, missing or changed projection, generated source test, or non-idempotent second run fails the focused test; exact full verification must be green before T002 can close.

- [x] T002@5b7d930e [P] [Shared] Strengthen `scripts/build-dev.test.mjs` first to prove complete installed-pack, project-skill, workflow, and `.dude/**` path/type/content preservation plus exact parity, source-test exclusion, stale cleanup, and idempotence; modify `scripts/build-dev.mjs` only if that focused test proves a concrete defect, and verify with `node --test scripts/build-dev.test.mjs`.

## Phase 3: Completed Baseline Guidance And Terminal Procedure

**Goal**: Preserve the accepted project/local ownership split and completed fail-closed pre-resolver behavior without reopening either task.

**Execution Details**:

- T004 is completed accepted implementation of the concise project-side terminal and pre-source baseline contract plus the terminal-loaded local procedure.
- T005 is completed accepted hardening: the pre-resolver guard observes a failing `git ls-files -v -z -- src` through its temporary-file form under `set -eu`, the ordering regression anchors on the guard fence, and the preflight is labelled as ten commands.
- The current plan describes those facts as shipped behavior. It contains no future T005 work and no stale process-substitution description.
- Preserve every scope exclusion in the plan.

**Independent Test**: Accepted focused contracts and independent reviews remain historical execution evidence. T006 adds only first-adopter coverage; T003 freshly validates the complete lifecycle.

- [x] T004@e2a91f6c [Shared] Write contract tests first, create `.github/skills/dude-local-core-dogfood-promotion/SKILL.md` with the reusable post-readiness promotion procedure, retain the complete executable pre-terminal baseline contract plus concise trigger and route in `.github/skills/project/SKILL.md`, and update `scripts/current-format-contract.test.mjs`; make no `src/**` or base-owned generated-core write and verify with `node --test scripts/current-format-contract.test.mjs`.
    deps: T001@8f2c1a47

- [x] T005@3d7b0af5 [Shared] Write the failing assertions first, then close the three recorded pre-resolver gaps: make the guard in `.github/skills/project/SKILL.md` fail closed on a nonzero `git ls-files -v -z -- src` exit status, correct its remaining `eight-command cleanliness block` label to the ten-command preflight, and anchor the ordering regression in `scripts/current-format-contract.test.mjs` on the guard fence instead of the first prose mention; make no `src/**` or base-owned generated-core write and verify with `node --test scripts/current-format-contract.test.mjs`.
    deps: T004@e2a91f6c

## Phase 4: Completed First-Adopter Continuity Foundation

**Goal**: Preserve the accepted fail-closed continuity bridge for exactly Feature 009 `T009@696e6369`, including its valid refusal when T009's declaration does not cover the complete original-baseline source delta.

**Write Set**:

- `scripts/current-format-contract.test.mjs`
- `.github/skills/project/SKILL.md`
- `.github/skills/dude-local-core-dogfood-promotion/SKILL.md`

**Execution Details**:

- T006 is completed accepted work and is not reopened or rewritten as a successful materialization gate.
- Preserve its original valid baseline resolution, fresh checkout-HEAD binding, descendant and explicit continuity authorization, protected-boundary and ordinary preflight checks, closed invalid-packet matrix, no-new-baseline behavior, and strict later-feature branch.
- Preserve its accepted fail-closed result: the complete original-baseline delta has 20 source paths while T009's live accepted declaration has its ten Feature 009 paths.
- T006 remains non-terminal, has no `declared-src:` clause, and performed no materialization, evidence append, Feature 009 mutation, or Feature 008 close.

**Independent Test**: Accepted focused contracts, scope evidence, and independent review remain historical execution evidence. T007 adds only the transient current-session route; T003 freshly validates the complete lifecycle.

- [x] T006@62726964 [Shared] Write tests first in `scripts/current-format-contract.test.mjs`, then minimally update `.github/skills/project/SKILL.md` and `.github/skills/dude-local-core-dogfood-promotion/SKILL.md` to designate only Feature 009 `T009@696e6369` as the first adopter, preserve and consume its immutable original valid pre-source baseline through fail-closed explicit-authorized descendant continuity, require unchanged accepted T008 source identity, exact original-baseline changed-set coverage by the live T009 declaration, original-baseline generated prestate, current authority and protected boundaries, no unrelated or concurrent drift, unchanged normal future gates, and the exact T006 -> T009 -> T002 -> T003 sequence; verify focused Core Dogfood and full current-format contracts, Dude lint, scope/no-source/no-generated-core/no-Feature009-write checks, and independent review; do not run repository `build-dev`, append baseline or accepted evidence, create a helper/runtime/command/state/ledger/framework/report, materialize generated core, mutate Feature 009, or close Feature 008.
    deps: T004@e2a91f6c, T005@3d7b0af5

## Phase 5: Passable Pre-Materialization Acceptance Route

**Goal**: Make one complete packet that can truthfully pass before projection plus independently grounded Tester and Reviewer approval sufficient for the exact current T009 materialization event, without reconstructing historical identities, expanding T009's declaration, weakening final gates, or transferring contributor ownership.

**Write Set**:

- `scripts/current-format-contract.test.mjs`
- `.github/skills/project/SKILL.md`
- `.github/skills/dude-local-core-dogfood-promotion/SKILL.md`

**Execution Details**:

- Write focused transient-route assertions first.
- Preserve T009's accepted ten-path declaration; use only T008's exact declaration and changed-identity evidence that actually exists. Do not require, claim, or reconstruct missing historical accepted HEAD, complete source, or dual-review identities.
- Resolve Feature 003 and Feature 006 by exact `spec_path` with zero diagnostics and preserve exact current owner/task attribution where available. Treat their historical records as attribution context only, not materialization authority.
- Derive the complete exactly 20-path original-baseline source delta from current Git trees, types, modes, object or content identities, and bytes.
- Bind independent source and generated index, worktree, untracked, ignored, and hidden-index layers, including command success, file modes, and fail-closed generated ownership classification.
- Evaluate the existing mapping and materializer behavior live to derive the expected complete generated inventory, every destination, explicit no-output result, stale-output cleanup, type, mode, content identity, and byte sequence. Preserve T009's eight listed destinations as its owned outputs; derive exactly five additional contributor-owned destinations and five no-output results from the ten contributor paths without freezing contributor destinations in policy.
- Capture current generated prestate and exact protected-boundary prestate.
- Bind focused source, policy, and runtime checks that do not require current generated parity. Include Dude lint or another check only when it can truthfully pass before projection.
- Isolate only `checked-in dev core is a byte-identical non-mutating projection of authoritative source` from `scripts/build-dev.test.mjs` with an anchored exact-name filter; require exactly that one selected failure, zero other failed or cancelled tests, and an independently derived observed path/type/mode/byte delta equal to the packet-described expected projection delta. Any unrelated or additional failure blocks; no generic expected-failure allowance is permitted.
- Require the Tester and Reviewer each to independently reacquire authoritative Git, byte, mapping, ownership, and command-result evidence, or independently verify every supplied value against those authorities, before approving the complete packet.
- Immediately reacquire and compare every input after both approvals. Any interruption or changed bound input invalidates the packet and requires complete fresh reacquisition and both new approvals.
- Make an unchanged valid packet sufficient authority only for one immediate T009 materializer invocation in the current main checkout without isolation.
- Preserve the accepted-line textual shape while defining this event's `declared` as T009's unchanged ten-path declaration, `source` as the complete current source inventory, and `changed` as the complete 20-path original-baseline delta. Require the ten rows outside `declared` to equal the fresh disjoint nine-path Feature 003 plus one-path Feature 006 partition.
- Apply the same event-only identity interpretation to changed validation, materialization authorization, post-materialization acceptance, immediate identity recheck, latest-match evidence, and close. Retain declaration-equals-changed behavior for every ordinary or later feature.
- Preserve contributor ownership, task history, prior acceptance records, and closure; keep T009's declaration and acceptance claim limited to its own work.
- Keep the recursively discovered full suite, exact parity, Dude lint, compose, pristine release build and lint, complete intended-scope, whitespace, every other existing terminal gate, and fresh final independent review mandatory after materialization. The packet waives none of them and is not durable accepted evidence.
- Preserve original-baseline immutability, explicit descendant continuity, the ordinary ten-command preflight, protected boundaries, no concurrency, no-new-baseline behavior, and strict normal future gates.
- T007 is not a terminal and carries no `declared-src:` clause.
- Do not run repository `build-dev`, mutate `src/**` or base-owned generated core, append baseline or accepted evidence, mutate Feature 009 or contributor artifacts or state, or close Feature 008.
- Do not create or persist a packet, digest, approval body, inventory, snapshot, helper, runtime, command, state, store, ledger, schema, framework, ObjectiveRegistry, API, or report.

**Independent Test**: Applicable current-format contracts accept one packet with exact current identities, every dirt layer, expected cleanup and projection, protected prestate, passable focused checks, one exact isolated parity failure, independently reacquired or verified Tester and Reviewer evidence, event-only 10-path `declared` and 20-path `changed` semantics, complete 8+5 generated scope, and an unchanged immediate recheck. They reject unrelated failures, missing historical-field reconstruction, wrong event, isolation, interruption, changed input, ambiguous attribution, concealed dirt, mapping or cleanup mismatch, generic or echoed approval, persistence, declaration expansion, ownership transfer, incomplete generated scope, and later-feature use. Dude lint or another ancillary check is required before projection only when truthfully passable; all terminal gates remain mandatory after materialization.

- [x] T007@9a4e7c12 [Shared] Write failing pre-materialization route assertions first in `scripts/current-format-contract.test.mjs`, then minimally update `.github/skills/project/SKILL.md` and `.github/skills/dude-local-core-dogfood-promotion/SKILL.md` to preserve T009's accepted ten-path declaration; freshly bind the complete 20-path original-baseline delta and exact disjoint nine-path Feature 003 plus one-path Feature 006 attribution; bind all source/generated dirt layers, deterministic complete projection and cleanup, protected prestate, passable focused source/policy/runtime checks, and the sole exact named parity-sensitive failure; require Tester and Reviewer to independently reacquire or verify authoritative evidence; preserve the accepted-line shape while applying ten-path `declared`, complete `source`, and 20-path `changed` semantics through authorization, final review, recheck, latest-match evidence, and close; verify the complete disjoint scope of eight T009-owned outputs plus five live-derived contributor-owned outputs, five no-output results, unchanged inventory, and cleanup without transferring ownership; retain every post-materialization full-suite, parity, lint, compose, release, scope, whitespace, review, and terminal gate; invalidate and freshly reacquire the transient packet after any change; add no persistent packet/helper/runtime/command/state/store/ledger/schema/framework/ObjectiveRegistry/API/report; verify all applicable parity-independent contracts, conditional passable ancillary checks, deterministic scope, and independent acceptance; do not run repository `build-dev`, append evidence, mutate `src/**`, generated core, Feature 009, contributor artifacts, glyphs, blocker metadata, or state.
    deps: T006@62726964

## Phase 6: Exclusive Feature 009 Adoption And Materializer Unblock

**Goal**: Preserve cross-feature authority while the fresh packet is consumed and the existing T002 blocker is resolved by Feature 009's own terminal work.

**Execution Details**:

- This phase derives no Feature 008 implementation task.
- After T007 acceptance, Feature 009 `T009@696e6369` alone acquires and immediately consumes one valid packet and owns actual `node scripts/build-dev.mjs` materialization; post-materialization parity, protected-boundary, recursively discovered full-suite, lint, compose, pristine-release, complete-scope, whitespace, and all other terminal gates; fresh final independent review; normal final evidence; and its separate Feature 007 correction.
- T009's declaration and source acceptance claim remain limited to its ten paths and its generated ownership remains limited to its eight listed destinations. The five contributor destinations are derived live, remain contributor-owned, and are not re-accepted; each contributor retains ownership, task history, prior acceptance records, and closure.
- Normal final evidence is appended only after all post-materialization gates and final review pass. Its unchanged line shape uses the event-only ten-path `declared` and 20-path `changed` interpretation through immediate recheck, latest-match selection, and close.
- Feature 008 does not mutate Feature 009's package, owner log, task glyph, blocker, metadata, board, snapshot, or state.
- After T009 succeeds, T002 may resume and closes only when its exact full `scripts/build-dev.test.mjs` verification is green.
- Any first-adopter or T009 failure leaves T009 and T002 open or blocked under their owning authorities.

**Independent Test**: The live Feature 009 declaration and owner evidence remain authoritative, T009 is the only bridge adopter and materializer, its eight outputs plus five live-derived contributor outputs form the complete disjoint projection, all final gates pass only after materialization, accepted evidence binds the exact 10/20 identities through close, contributor acceptance is unchanged, and T002's full suite becomes green only after accepted T009 materialization.

## Phase 7: Final Bootstrap No-Source Acceptance

**Goal**: Accept the complete project-local lifecycle without manufacturing source work, persisting model scenarios, or absorbing Feature 009 implementation.

**Execution Details**:

- This task has no implementation write path and no `[P]` marker.
- `declared-src: none` remains this package's explicit bootstrap exception.
- Preserve the existing pre-terminal baseline, route, malformed-derivation, missing-baseline, no-source, close-blocking, focused/full verification, bounded owner evidence, and final independent-review meaning.
- Add fresh validation that T006 continuity acceptance and T007 route acceptance are current; T009 was the sole adopter and materializer; its ten-path declaration remained unchanged; only T008 evidence that actually exists was used; Feature 003 and Feature 006 records remained attribution only with ownership and history preserved; one pre-materialization packet bound the complete 20-path source delta, exact 9/1 partition, every dirt layer, expected cleanup and projection, protected prestate, passable checks, the sole exact parity failure, independently reacquired or verified Tester and Reviewer approvals, and an unchanged immediate recheck; no historical identity was reconstructed and no packet was persisted; T009's eight outputs plus five live-derived contributor outputs formed the complete projection with five no-output results; every full terminal gate passed after materialization; final evidence retained the line shape and exact 10/20 interpretation through close; the original baseline remained comparison authority; the void replacement authorized nothing; T002 full verification is green; and normal future exact-baseline and declaration-equals-changed gates remain unchanged.
- Do not rerun or duplicate Feature 009 implementation or materialization.
- Keep every authority exercise transient and read-only; create no helper, runtime, state, ledger, ObjectiveRegistry, or report.
- Any ownership, baseline, authorization, declaration, source, generated, verification, preservation, routing, skill, sequence, or review failure leaves T003 open or blocked.

**Independent Test**: The accepted feature contains no Feature 008 source or base-owned generated-core delta, the exclusive first-adopter sequence and future-gate non-regression are freshly exercised, all project validation is current, and an independent Reviewer accepts the exact final identities and evidence.

- [x] T003@c4e6812d [Shared] Complete the explicit bootstrap no-source acceptance; declared-src: none; prove no accepted Feature 008 `src/**` or base-owned generated-core delta, do not run repository `build-dev` as a mutation, and validate the complete pre-terminal baseline contract plus the accepted one-time Feature 009 `T009@696e6369` route: passable pre-materialization checks and exact isolated parity delta; independently reacquired or verified Tester and Reviewer packet approval; unchanged ten-path T009 declaration; complete 20-path original-baseline `changed` identity with exact disjoint 9/1 contributor attribution; complete projection of eight T009-owned plus five live-derived contributor-owned outputs, five no-output results, unchanged inventory, and cleanup; mandatory post-materialization full suite, exact parity, lint, compose, pristine release, scope, whitespace, every terminal gate, and final review; unchanged accepted-line shape and event interpretation through immediate recheck, latest-match evidence, and close; no packet persistence, ownership transfer, contributor re-acceptance, or later-feature weakening; complete the Plan Phase 6 verification and bounded final owner-log evidence, obtain independent final review, and leave the task open or blocked on any failure.
    deps: T001@8f2c1a47, T002@5b7d930e, T004@e2a91f6c, T005@3d7b0af5, T006@62726964, T007@9a4e7c12

## Requirements And Success Traceability

| Specification coverage | Tasks |
|---|---|
| FR-001 through FR-003 / SC-001 | T001@8f2c1a47, T004@e2a91f6c, T006@62726964, T007@9a4e7c12, T003@c4e6812d |
| FR-004 through FR-007 / SC-001, SC-002 | T001@8f2c1a47, T004@e2a91f6c, T003@c4e6812d |
| FR-008 through FR-012 / SC-003 | T001@8f2c1a47, T004@e2a91f6c, T005@3d7b0af5, T003@c4e6812d |
| FR-013 through FR-021 / SC-015, SC-017 | T001@8f2c1a47, T002@5b7d930e, T004@e2a91f6c, T007@9a4e7c12, T003@c4e6812d |
| FR-022 through FR-026 / SC-014, SC-016 | T001@8f2c1a47, T004@e2a91f6c, T005@3d7b0af5, T006@62726964, T007@9a4e7c12, T003@c4e6812d |
| FR-027 through FR-032 / SC-004 through SC-006, SC-009 | T006@62726964, T007@9a4e7c12, T003@c4e6812d |
| FR-033 through FR-041 / SC-006 through SC-010, SC-020 | T007@9a4e7c12, T003@c4e6812d |
| FR-042 through FR-047 / SC-011 through SC-017, SC-023 | T007@9a4e7c12, T003@c4e6812d |
| FR-048 through FR-060 / SC-011, SC-012, SC-018 through SC-024 | T002@5b7d930e, T006@62726964, T007@9a4e7c12, T003@c4e6812d |
