<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@6f3a2c18 [Shared] Bind the existing checkpoint release to both retained artifact versions in `src\skills\dude-work\host-adapter.mjs`.
- T002@4b7d9e02 [US1] Finalize the original owner's pair after an eligible first-Assessment payload rejection while preserving the Work hard stop.
- T003@c581e6af [Shared] Document the narrow Work-owned finalization boundary and regenerate its dogfood core.

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/056-ship-orphan-cleanup.md#coordinator-log -->
# Tasks: Ship Orphan Cleanup

Spec: `.dude/specs/056-ship-orphan-cleanup/spec.md`  
Plan: `.dude/specs/056-ship-orphan-cleanup/plan.md`

All units are new and open. Verification commands below are proposed checks,
not recorded results. Tasks share implementation paths and are not parallel
candidates.

## Phase 1: Foundational Exact-Pair Release

- [x] T001@6f3a2c18 [Shared] Bind the existing checkpoint release to both retained artifact versions in `src\skills\dude-work\host-adapter.mjs`.
    Write set: `src\skills\dude-work\host-adapter.mjs`; `src\skills\dude-work\host-adapter.test.mjs`.
    Implement the plan's transient claim/checkpoint expectations in the existing closed store results, host retention, and expected-state arguments. Update in-repository test doubles with the same contract. Keep persisted pair bytes, derived paths, operation set, and existing admission authority unchanged.
    Require complete exact-pair comparison before removal. Keep checkpoint-then-claim order, recheck at each removal boundary, and establish both absent before success. Stop on partial state, drift, reappearance, or failure without retry or rollback claims.
    Trace: FR-005, FR-006, FR-008, FR-009, FR-010, FR-017; US2; SC-002, SC-004.
    Acceptance: Add `056 owner finalization` tests proving that independently changed claim/checkpoint bytes with valid recomputed hashes and unchanged revisions refuse before deletion. Cover missing, corrupt, unsafe-file, wrong-worker, and wrong-revision cases. Inject each removal/absence failure and reappearance boundary; verify the remaining or replacement artifact is not removed and success is not reported.
    Verification: Run the focused new tests and existing terminal-boundary, failed-clear, admission, handoff, and resume tests in `src\skills\dude-work\host-adapter.test.mjs`. Report observed outcomes and any unsupported platform fixture, without substituting real ownership files.

## Phase 2: Bounded Surviving-Owner Finalization

- [x] T002@4b7d9e02 [US1] Finalize the original owner's pair after an eligible first-Assessment payload rejection while preserving the Work hard stop.
    deps: T001@6f3a2c18
    Write set: `src\skills\dude-work\host-adapter-runner.mjs`; `src\skills\dude-work\host-adapter.mjs`; `src\skills\dude-work\host-adapter.test.mjs`.
    Select only the correctly bound first Assessment exchange's invalid/stale payload branches. Check original surviving authority, unchanged accepted state, fresh owner/lane/prestate binding, and the absence of authorization requests or unresolved obligations under the current claim. Add no public cleanup switch, semantic operation, or reason-only allowlist.
    Retain the original terminal result before invoking the existing guarded end boundary once. Keep `finish` authoritative, preserve the original hard-stop reason and accepted state, and report cleanup separately. Keep the CLI nonzero on that hard stop.
    Trace: FR-001 through FR-004, FR-007 through FR-017; US1, US2, US3; SC-001 through SC-006.
    Acceptance: Exercise the real runner with invalid and stale payloads in valid matched envelopes; assert one clear, no prompt or extra challenge, no authorization/dispatch/new claim, unchanged task/lane bytes, and preserved accounting including nonzero prior counters. Verify no automatic cleanup for malformed/foreign/replayed/out-of-order framing, EOF, exchange failure, missing capability, later Assessment phases, pending effects, governance obligations, and stale owner context. Preserve cancellation and existing successful settlement behavior.
    Acceptance: On failed cleanup, retain both the original refusal and the bounded cleanup failure, with no retry or replacement. Confirm diagnostics omit hostile payloads and ownership credentials. A separately invoked fixture admission must recheck both artifacts absent; returned state and cleanup success must not revive the old invocation.
    Verification: Run the focused new tests, `focused table A: sequential challenge protocol and foreground CLI`, and the complete `src\skills\dude-work\host-adapter.test.mjs` and `src\skills\dude-work\recovery.test.mjs` suites.

## Phase 3: Owning Guidance And Generated Parity

- [x] T003@c581e6af [Shared] Document the narrow Work-owned finalization boundary and regenerate its dogfood core.
    deps: T002@4b7d9e02
    Write set: `src\skills\dude-work\SKILL.md`; `docs\commands.md`; `scripts\current-format-contract.test.mjs`; generated `.github\skills\dude-work\SKILL.md`, `.github\skills\dude-work\host-adapter.mjs`, and `.github\skills\dude-work\host-adapter-runner.mjs`.
    Replace the blanket first-Assessment orphan wording only where the implemented exception applies. State the full eligibility boundary, original-stop reporting, no automatic new claim, and later independent authorization. Keep transport/context loss, cancellation, manual true-orphan cleanup, Work limits, and successful same-invocation continuity explicit.
    Add section-bounded guidance checks with meaningful deletion falsifiers. Do not move authority into Ship intake or change another feature's definition/history.
    Generate core through `node scripts\build-dev.mjs`; never hand-edit the listed generated files. Resolve unexpected unrelated generated changes before including them.
    Trace: FR-002, FR-011, FR-014, FR-015, FR-016, FR-017; US3; SC-005, SC-006.
    Acceptance: Source and generated runtime/guidance agree on the one eligible case. Documentation does not imply proven supervisor death, automatic retry, resumed old state, task completion, or a new command. Existing manual cleanup and Work-stop instructions remain accurate.
    Verification: Run `node --test scripts\current-format-contract.test.mjs scripts\build-dev.test.mjs scripts\build-release.test.mjs`, the complete host-adapter and recovery suites, and `node .github\skills\dude-lint\lint.mjs .`. Return fresh evidence for independent review; do not mark this package's tasks or close the feature.
