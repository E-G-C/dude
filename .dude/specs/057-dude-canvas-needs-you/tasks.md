<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T007@a57c0707 [Shared] Prove the expanded installed-host handoff, text/image tool result, and available HTML capture capability using `src/extensions/dude/extension.mjs` APIs and `scripts/dude-canvas-ui/browser.test.mjs` provider/probe harness.
- T008@a57c0808 [Shared] Create and iterate `.dude/specs/057-dude-canvas-needs-you/design/needs-you-workspace.html`, including internal Sharpie Review, and obtain explicit approval of its exact current revision before UI source changes.
- T003@a57c0303 [US2] Extend `src/extensions/dude/lib/projection.mjs` and `projection.test.mjs` for blank/draft/defined discovery, source-declared nonurgent work, and per-scope coverage.
- T009@a57c0909 [Shared] Implement bounded `dude_needs_you` publication/reply/acknowledgment and idea capture, including the typed preview report/image boundary, in `src/extensions/dude/{extension.mjs,lib/needs-you.mjs,lib/canvas-server.mjs}` with owner-producer guidance and focused tests.
- T010@a57c1010 [US3] Adopt the scoped HTML Sharpie engine and implement exact-owner review storage, capture, and sealed submission in `src/extensions/dude/{lib/review.mjs,ui/review/}` with integration at the existing `lib/canvas-server.mjs`/`lib/needs-you.mjs` boundary and focused tests.
- T011@a57c1111 [Shared] Apply the approved Needs You design and mounted Sharpie Review entry/return in `src/extensions/dude/frontend/{app.jsx,styles.js,theme.js}` and build the real-provider `src/extensions/dude/ui/` artifacts.
- T013@a57c1313 [US3] Implement the fresh-render alignment evidence contract across `src/extensions/dude/{ui/review/,lib/review.mjs,lib/review/}` while retaining inline host review, owned Chromium capture, and existing marker composition.
- T012@a57c1212 [Shared] Complete SC-001–008 through required Node/CDP and actual installed Copilot-host runs; prove source-bound fresh-render alignment, source/deployed/install parity and notices, and update existing Canvas guidance in `README.md`/`docs/`.

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/057-dude-canvas-needs-you.md#coordinator-log -->
# Tasks: Dude Canvas Needs You

Spec: `.dude/specs/057-dude-canvas-needs-you/spec.md`

Lightweight Execution. The canonical task units below are the live board; only the coordinator changes their state after routed work and fresh evidence. All four stories, six request classes, and the working HTML annotation loop belong to the first delivery. Re-definition preserves completed units and their original evidence scope. T013 adds the fresh-render contract correction; T012 retains final acceptance and documentation. Host lifecycle actions remain outside an active Work adapter claim/supervisor.

## Phase 1 — Prove Capability And Agree The Design

- [x] T007@a57c0707 [Shared] Prove the expanded installed-host handoff, text/image tool result, and available HTML capture capability using `src/extensions/dude/extension.mjs` APIs and `scripts/dude-canvas-ui/browser.test.mjs` provider/probe harness.
    blocked-by: 
    - Owners: Coder and Tester; coordinator arranges host actions/runtime execution. Spec Lead does not run terminal commands or prototype code.
    - Replaces former T001's meaning. Use disposable installed non-Git/Git fixtures to prove discovery/root, coordinator tool exposure/profile limits, Spec Lead -> coordinator -> owner delegation, pending reply/cancellation, foreground outside answers, second-invocation acknowledgment, and idle idea-capture send without replay.
    - Through a minimal non-UI probe on an already-defined fixture, produce a real source-aligned PNG and report, then observe both text and actual image reaching the original waiting `dude_needs_you` tool and same-session continuation. SDK types, path-only images, and marker-only output are insufficient.
    - Follow `plan.md` Hard Host And Capture Prerequisite: preflight an actually available Chromium-family/host executable and demonstrate capture in the supported environment with no consumer install/build or automatic browser download. Local Edge is a candidate, not a portable assumption. If the established target contract requires a new runtime prerequisite, return the concrete material scope choice rather than silently narrow support or make capture optional.
    - Run automatable checks first; any host lifecycle actions occur outside an active Work claim/supervisor. Add no production proof-only route, retired 052 flag, persistent service, or UI source.
    - Check: actual host evidence establishes all required seams, including exact-source view/capture and one-time typed text/image receipt. Record missing capabilities as hard prerequisites; this is not complete product acceptance.

- [x] T008@a57c0808 [Shared] Create and iterate `.dude/specs/057-dude-canvas-needs-you/design/needs-you-workspace.html`, including internal Sharpie Review, and obtain explicit approval of its exact current revision before UI source changes.
    blocked-by: 
    deps: T007@a57c0707
    - Owners: FluentUI and Coder for mock/capability mapping; coordinator for rendering, approval checkpoint, and authorized lifecycle bookkeeping. Spec Lead supplies definition consistency only.
    - Show blank/draft/defined discovery, all six forms and FR-015 states, Save as idea versus unsaved Defer, exact preview/permission context, and partial/unavailable coverage. Add Review entry/return, annotation tools/comments, current revision, capture failure, sealed evidence, source drift, and explicit approval separate from Send annotations.
    - Map every field/control to a real source and the proven envelope. Drafts lacking an exact package show Define-first without fake mock/storage. Preserve explored variants and applicable screenshots; use Fluent tokens, fluid layout, keyboard/focus, and WCAG 2.2 AA at 360/768/1440 pixels in light/dark.
    - Check: inspected mock has no dead affordances; user approval follows exploring -> proposed -> approved. Bootstrap approval uses the existing design-owner path, not the not-yet-built annotation loop. This task creates no product UI or claim that SC-008 already passes.

## Phase 2 — Supply Authoritative Discovery, Responses, And Review

- [x] T003@a57c0303 [US2] Extend `src/extensions/dude/lib/projection.mjs` and `projection.test.mjs` for blank/draft/defined discovery, source-declared nonurgent work, and per-scope coverage.
    deps: T007@a57c0707
    - Owners: Coder; Tester supplies source/identity regression evidence.
    - Reuse exact lifecycle/owner readers without a second resolver. Include context kind/status and intent; treat missing directories as blank, retain complete 50-draft discovery, and preserve explicit query/selection semantics.
    - Keep inventory and attributed dispositions separate from current requests. Do not turn blank answers, history, old approvals, or ordinary failures into human prompts. Preserve 025 and existing defined-feature attention behavior.
    - Check: US1/US2/US4 fixtures prove all drafts and acknowledged source-backed deferrals remain discoverable/nonurgent after reread/restart; malformed/partial sources remain scoped uncertainty; unrelated valid contexts survive selection and source failure. No UI files change.

- [x] T009@a57c0909 [Shared] Implement bounded `dude_needs_you` publication/reply/acknowledgment and idea capture, including the typed preview report/image boundary, in `src/extensions/dude/{extension.mjs,lib/needs-you.mjs,lib/canvas-server.mjs}` with owner-producer guidance and focused tests.
    deps: T007@a57c0707
    - Owners: Coder for runtime; AgentSmith and SkillSmith for core guidance; Tester for contract/race tests. Coordinator handles execution and owner delegation.
    - Retain two closed tool operations, six class-specific responses, invocation/cancellation identity, source/revision binding, same-origin bounded HTTP actions, shared provider/session receipts, and explicit acknowledgment plus reread. No generic RPC, protected answer/task/approval writes, or second queue.
    - Extend preview response to accept only adapter-validated sealed evidence: bounded report references/text and actual PNG via SDK `textResultForLlm`/`binaryResultsForLlm` to the original waiting tool. Define the exact-source/image/provenance validation seam used by T010; no arbitrary path/MIME authority. Consume a response once, not before required evidence passes.
    - Wire actual callers through `src/skills/dude-work-intake/SKILL.md` with minimal references at `src/agents/{dude,dude-spec-lead}.agent.md` and `src/skills/{dude-feature-definition,dude-work}/SKILL.md`. Design feedback requests canonical mock revision under the definition/design owner; it neither approves a revision nor permits premature product UI edits. Acknowledged revision yields a fresh request, never inherited approval.
    - Include outside-answer invalidation/recognition, declined acknowledgment/reactivation, explicit Save as idea, ordinary Defer without auto-capture, source deduplication, and idle send only after a blocking tool yields. Preserve literal user text and keep tokens/sensitive payloads out of capture/logs. Omit standalone Sharpie `session.send`; do not fix its unreachable displayName path.
    - Check: focused Node/real-provider tests cover all six interactions, preview/target drift, duplicates, forged handles/Origin/paths, canceled/changed sessions, missing ack, uncertain send without replay, canonical capture/disposition reread, and one-time typed image receipt. No product UI files change.

- [x] T010@a57c1010 [US3] Adopt the scoped HTML Sharpie engine and implement exact-owner review storage, capture, and sealed submission in `src/extensions/dude/{lib/review.mjs,ui/review/}` with integration at the existing `lib/canvas-server.mjs`/`lib/needs-you.mjs` boundary and focused tests.
    deps: T008@a57c0808, T009@a57c0909
    - Owners: Coder for adoption/adapter; FluentUI for keyboard/accessibility mapping; Tester for engine/capture/ownership checks. This is the one new adapter task, not a standalone Review product.
    - Copy only the plan's HTML annotation modules/fragments, semantic submission helpers, and necessary serve/capture/store patterns from the user-owned source. Freely adapt the local copy; leave the external repository untouched and runtime-independent. Exclude PDF/export/file-picker/package/node_modules, standalone sending, and global prefs/latest services; preserve required copied-file notices.
    - Mountable vanilla workspace preserves shapes/lines/arrows, repeat use, undo/redo, selector/text/style feedback, comment caret, shortcuts, and tool/viewport persistence. Keep shell entry/return wiring in T011. Isolate untrusted mock content from parent/private mutation authority.
    - Allocate provider-chosen UUID review directories under the actual exact owner, not always 057. Implement editable `working.json`, exclusive `report.md`/`annotated.png`, and last-written `provenance.json` seal as specified in the plan. Existing sealed evidence never overwrites; partial/caught failure preserves work without treating incomplete files as success. Files restore evidence, not live request authority. No new registry, workflow board, or metadata writer.
    - Capture real source-aligned HTML using preflighted capability. Bind source/assets, viewport/scroll, dimensions, mode/warnings, selectors/match counts, and evidence hashes. Verify line/marker coordinates, sticky/vh/scrolled layouts, and source before/after capture. Reject drift, ambiguous anchors, unsafe paths/symlinks, transient mismatch, malformed PNG/MIME, or wrong report/image/source matching at the current tool boundary.
    - Check: real-provider adapter tests prove valid sealed report plus actual image reaches the original waiter once; missing capture keeps annotations safe without report-only success. Exercise keyboard/engine repetition, unchanged-source restore, target-owner isolation, failed sealing, duplicate submit, session loss, and no silent replay. Adopted static files and notices are explicitly accounted for in runtime copy/build handling, not sourced live from Sharpie.

## Phase 3 — Apply, Correct Capture Evidence, And Verify The Attention Loop

- [x] T011@a57c1111 [Shared] Apply the approved Needs You design and mounted Sharpie Review entry/return in `src/extensions/dude/frontend/{app.jsx,styles.js,theme.js}` and build the real-provider `src/extensions/dude/ui/` artifacts.
    deps: T008@a57c0808, T003@a57c0303, T009@a57c0909, T010@a57c1010
    - Owners: Coder for integration/state/data; FluentUI for composition/accessibility; Tester for interaction evidence. Coordinator executes builds/captures.
    - Implement US1–US4 and all six forms. Preserve complete context discovery, one blocking clarification, sent/acknowledged/applied/declined/deferred/stale/unavailable distinctions, explicit capture, unsaved Defer warning, later discovery, literal permission, outside answers, and reconnect reconciliation.
    - User entry from an exact current preview mounts the vanilla annotation workspace inside the one existing Canvas/provider. Return retains selected context and useful markers/comment focus/viewport for unchanged source. Sending annotations awaits owner revision/ack; a new request drives the next revision, with separate explicit current approval. No auto-open on every update or approval shortcut.
    - Show Define-first for missing exact package, capture failure with retained working markup, sealed historical evidence without live authority, and source-drift refusal without remapping. Copy/chat remains expert fallback, not normal delivery.
    - Check: real-provider interactions match the approved preview and FR-015/016/019–022 states at every required width/theme, preserve wording, execute no underlying operation, and keep annotation static assets/notices through clean browser builds. Report design mismatch through the existing workflow.

- [x] T013@a57c1313 [US3] Implement the fresh-render alignment evidence contract across `src/extensions/dude/{ui/review/,lib/review.mjs,lib/review/}` while retaining inline host review, owned Chromium capture, and existing marker composition.
    deps: T011@a57c1111
    - Owners: Coder for the coherent pipeline correction; Tester for focused contract/format falsifiers; independent reviewers through the coordinator. Spec Lead owns definition consistency only.
    - Remove cross-renderer style/signature equality, not add observations. Limit production edits to the four sites below under `src/extensions/dude/`. In `ui/review/geometry.mjs`, add a capture-only matcher over the projected descriptor excluding `styles` (`selector`, `selectorMatches`, `tag`, `text`, `rect`); keep native `anchorMatches` admission/restore semantics unchanged. No inspector, bridge, or engine extension is needed.
    - In `lib/review/browser.mjs`, drop `before.signature !== state.view.signature` at line 333, use the portable matcher in `readAnchors()`, and hash the portable subset for `elementRevision` at lines 368-369. The existing viewport/scroll and `readAnchors()` identity, scroll-basis, projected-geometry, visibility, and `clipHidesAnchor`/`insideClip` containment checks at lines 332-344 must become reachable and remain enforced. Keep measurement precision, limits, and the Chromium before/after signature check at line 357 unchanged; do not normalize font, border, or overflow strings.
    - In `lib/review.mjs`, drop both comparisons with `state.view.signature` at lines 326-327, retain `capture.beforeSignature === capture.afterSignature` as the renderer-local re-check, and hash the same portable subset for `elementRevision` at lines 333-337. Keep the host before/after and restore signature checks in `engine.mjs:332,336` unchanged.
    - In `lib/review/data.mjs`, accept the revised capture shape and keep stored/reported target text and `styles` labeled as host observations, not cross-renderer proof. Bind existing evidence to frozen working bytes and the owned capture result. Keep `view.signature` and the saved descriptor schema unchanged; no new observation payload or interaction replay is required.
    - Preserve version-1 historical validation and byte-exact report reconstruction with synthetic old-format fixtures. Limit version-2 changes to revised capture/report/seal semantics, with no automatic migration or resave-on-open. Retain the fresh-render warning, native-pixel/glyph/whole-page-paint/unanchored-target limits, and existing `review_transient_unsupported` refusal.
    - Retain the existing real-PNG-plus-plain-SVG compositor, viewport/DPR/nested-scroll CDP control, source isolation, exact-owner storage, immutable last-written seal, PNG/composite verification, and original waiter receipts. Add no bitmap/input transport, service, store, installer, wholesale import, runtime Sharpie dependency, markers-only fallback, or alternate sender.
    - Preserve every `plan.md` Required Non-regressions row. Focused checks must accept equivalent portable evidence with different style serialization and reject altered identity, geometry, clipping, visibility, containment, local instability, forged evidence, and premature waiter consumption. Restoring the old equality gate must fail the positive case.
    - Check: focused production-boundary tests and independent review establish the revised contract and historical compatibility without touching protected real reviews, the approved mock/assets, the live canvas, or provider PID 63484. No completion evidence is inherited from T010 or T012; complete installed/desktop acceptance remains T012.

- [x] T012@a57c1212 [Shared] Complete SC-001–008 through required Node/CDP and actual installed Copilot-host runs; prove source-bound fresh-render alignment, source/deployed/install parity and notices, and update existing Canvas guidance in `README.md`/`docs/`.
    deps: T011@a57c1111, T013@a57c1313
    - Owners: Tester for verification, Coder for scoped fixes/docs, coordinator for runtime/host/build steps and independent reviewer routing.
    - Retain this task's final acceptance/documentation outcome and prior evidence with its original scope. Extend `scripts/dude-canvas-ui/browser.test.mjs`, extension/adapter tests, scoped build tests, and `scripts/{build-dev,build-release}.test.mjs` only as needed. Use `DUDE_CANVAS_BROWSER_REQUIRED=1`, real `createFixtureServer`/`openInstance`, realistic files, and existing Beads-process fixtures, not mocked projections alone.
    - Execute every plan acceptance row: blank non-Git/Git capture, 50 drafts/mixed contexts, deferrals, all six response/ack loops, preview/permission drift, outside answers, click/reconnect races, cancellation, missing owner/ack, session loss, unsaved/captured Defer, and reactivation.
    - Prove the new contract through actual inline host review and owned Chromium capture, including the 989x728 DPR2 condition with disposable synthetic source/markup. Same-engine tests cannot establish cross-renderer behavior. Verify that renderer-specific style serialization does not block valid alignment and that real geometry, clipping, identity, and observed interaction-state mismatches still refuse.
    - Prove two on-mock annotation/report/actual-PNG submissions to original installed waiting tools, same-agent mock revisions and acknowledgments, fresh requests, then explicit approval of the current revision. Use raw installed owner profiles without capability augmentation. Inspect exact-owner immutable evidence, browser/version, source/assets, projected boxes, marker/badge containment, viewport/scroll, verified checks, limitations, return focus, and saved working markup. Report-only, path-only, or marker-only output cannot pass SC-008.
    - Confirm invisible-candidate admission, hidden equals blocks-submission, `clipHidesAnchor`, pinned viewport, ZoomIt head-at-press input without reversing old arrows, inline numbered Send blockers with Inspect annotations, and owner-acknowledgment precedence. Tester must specifically confirm the landed optional-marker-text behavior: a valid blank numbered pin can send its report and PNG.
    - Diagnose the existing scroll-time readiness-message conflation among actual fonts/images/animations loading, bridge-query timeout, and view-refresh mismatch using bounded evidence before changing causal copy. Preserve an undiagnosed result as open; do not close it by attributing it to the cross-engine Send explanation. The unavailable full native pre-hash rows and exact failed Send payload remain a limit on that incident's causal proof.
    - Automate shortcuts/repeat use/undo/redo/comment caret, required states/forms at 360/768/1440 light/dark, geometry/screenshots/accessibility tree/contrast, source/asset/selector drift, timeout bounds, missing browser/capture, malformed image, provenance mismatch, old/new report validation, and sealed-write failure. Preserve non-consuming failure and once-only delivery. Then request only named host-unique observations under existing authority.
    - Check: focused and recursive tests, required browser evidence, two clean browser builds with exact committed/static/legal bytes, runtime allowlist and pristine release/install parity, actual supported-platform capture without consumer install/build or auto-download, budget/license report, lint/compose/diff/prose gates, and independent acceptance under existing rules. All real 057 review directories and approved mock artifacts remain protected and are not fixtures. No unauthorized live-canvas/provider action, Git commit, unrelated model-map output, 025 replacement, 052/055 reopening, 053/054 absorption, or deferred 056 implementation.
