<!-- audit log: .dude/ideas/062-dude-canvas-workspace-integration.md#coordinator-log -->
# Tasks: Dude Canvas Workspace Integration

**Specification:** `.dude/specs/062-dude-canvas-workspace-integration/spec.md`  
**Plan:** `.dude/specs/062-dude-canvas-workspace-integration/plan.md`

All tasks are proposed and open. Phase 1 continues explicit definition/design after publication, before Work; it is not permission to execute design through Work. Product tasks are conditional on explicit user approval of the exact current canonical layout with the stated native-feedback proof boundary and `design_status: approved`. Ship, native-feedback success, and historical 052/057/062 approvals do not satisfy that gate.

Canonical units below are the only task list. No board, imported state, completion evidence, or execution history is created here. Only the coordinator may later change task state.

## Phase 1: Establish The Integrated Design

- [ ] T001@a062c1d4 [Shared] Revise and compare the canonical 062 layout at `.dude/specs/062-dude-canvas-workspace-integration/design/workspace-integration.html`, prove real annotation feedback through existing native Dude Review around it, then obtain explicit current-layout-revision approval through the existing design workflow.

    Preserve the rejected `ca164ca808c61bef78304212217234b25147b8fddef3a387f268102dbe304180` candidate, approved reference, rev-7, and every earlier variant; this staged unit edits or renders none of them. After authorized definition application, retain phase-grouped rows, section-local status filtering, full source-text detail in the dock/narrow disclosure, dependencies, explicit blockers, result-coverage limits, and honest frozen/non-live status. Keep captured 062's five todo units and completed 052 Done cases; controlled active/blocked examples must remain labeled and agree with their rollups. Never change repository task state for the mock.

    Retain no-selection Overview, the numbered single finder, selected-only Overview/Working on/Clear, Now/Next, rail disclosure, valid local return, and all response/capture and focused-Review placements as literal design layouts. Remove the embedded annotation controller and fake Save/Send/Approve or successful-operation controls; use non-actionable layout labels where an operation is unavailable, and provide no dead Open Review destination. Keep genuinely local input/filter/disclosure/navigation behavior and confirm affordances with the target owner. Selected-task descriptors remain explicitly planned enrichment, not already on the wire.

    Reuse actual ReviewWorkspace, mountReview/inspector, comment fields/caret, toolbar, and createReview through one joined real provider and original waiting request, as the plan specifies. Before human feedback, verify variable pointer geometry, hit-testing and Select move/resize, real comments/editing/history, saved markup and unchanged-source return, report plus actual aligned PNG delivery exactly once, owner acknowledgment, and the applicable refusal, viewport, accessibility, and keyboard cases through the existing real-provider/installed-owner harness. Use only bindings that invocation establishes, never dummy engines, canned callbacks, or fabricated current handles. Keep 059's pending gesture defect separate and do not call a failing required gesture functional.

    Acceptance: the exact canonical layout comparison and fresh visual evidence satisfy VSC-001–003 and the mock cases of SC-007, with no false task results, live-agent claims, task mutations, or Settings UI. Real native feedback proof must precede an owner-qualified annotation-feedback request; it proves existing tools annotating the mock, not 062 chrome already enclosing Review. T004/T005 still owe the real integrated response/capture/Review entry-and-return loop, input/markup/caret retention, pinned geometry, and SC-003–006 acceptance. Only later explicit current-layout-revision approval uses the existing owner/coordinator path in spec.md and the exact idea log; feedback, historical approval, and automated fixture responses cannot substitute. No product source is changed, and all requested revisions stay in this design loop.

## Phase 2: Find And Select Work In The Approved Shell

- [ ] T002@b062d2e5 [US1] Apply the approved workspace chrome and single numbered work finder in `src/extensions/dude/frontend/app.jsx` and `styles.js`, with discovery and selection regressions in `scripts/dude-canvas-ui/browser.test.mjs` and `t011-browser.test.mjs`.
    deps: T001@a062c1d4

    Retain Open/Closed/All and complete discovery while moving the sole finder to the command-bar workspace. Add capture-number labels and numeric presentation order from validated context paths, including compact rows. Keep exact selection, pending reads, query/scope/scroll return, working rail destinations, status chrome, and partial/empty states distinct. Do not change inventory or request contracts.

    Acceptance: SC-001/006 and the applicable visual criteria pass on the built surface, including one-activation results, full-list keyboard reachability, no implicit selection, and source-identity failure cases. Follow the plan's per-slice build and comparison checks.

## Phase 3: Restore Selected-Feature Now Orientation

- [ ] T003@c062e3f6 [US2] Enrich `src/extensions/dude/lib/projection.mjs` with read-only selected-task descriptors and compose approved Next, phase-grouped Tasks/detail, Activity, and the properties/evidence dock in `src/extensions/dude/frontend/{app.jsx,styles.js}`; extend projection and scoped browser acceptance.
    deps: T002@b062d2e5

    Use existing canonical parsing/visibility offsets to expose the whole file-backed unit, including paragraphs after blank lines and acceptance/context; do not substitute `description` or `extraMeta`, add a task regex parser, or change engine grammar. Exclude generated/archived/history content and discovered mirror bleed. Return planned rows/counts/phases for defined all-open sources without inventing execution. Reuse complete Beads list/status/readiness/blocker readers with exact imported task mapping or explicit unavailability, never markdown fallback. Missing imported phase/body or result data remains visibly unexposed.

    Carry rows and rollups through the selected feature's existing read/source verification and data-hook epochs, not inventory bodies, a new endpoint, or a second store. Render full source as inert wrapping text. Keep Pending/ready-by-dependencies/waiting/explicitly blocked distinct; highlight recorded in-progress without agent inference. Show no synthetic test/review verdict from Done, acceptance criteria, generic files, or feature history. Use local row selection/filter/disclosure only, bound to the exact feature/key and current read; Clear, filtering, source changes, and switching work cannot leak old detail or retarget requests.

    Acceptance: SC-002/007 and VSC-001/002 pass for draft, 062 all-open, completed 052, controlled mixed states, malformed/missing source, unmapped/duplicate tracked keys, selected-B/tracked-A, and racing refresh/selection. Extend `src/extensions/dude/{projection,work-index}.test.mjs` and `scripts/dude-canvas-ui/{browser,t011-browser}.test.mjs` to prove full-body extraction, inert markup, phase/count agreement, selected-only data exposure, unavailable-result copy, read-only controls, and safe focus/return. Keep existing bounds, source/authority and Review contracts, and approved 052 adaptations unchanged.

## Phase 4: Complete The Context-Preserving Interaction Loop

- [ ] T004@d062f4a7 [US3] Integrate existing Needs You, New idea, and Review entry/return with Now in `src/extensions/dude/frontend/{app.jsx,needs-you.jsx,review.jsx,styles.js}`, retaining the data-hook and Review-engine contracts.
    deps: T003@c062e3f6

    Cover US3 and US4 together: the shell must retain all six forms, Submit/Save/Cancel, Save as idea/Defer, current-request-only Review, and sealed-history access. Keep browsing and request scopes distinct, unsent input keyed to its request, the Review engine mounted on valid return, and pinned geometry/focus/caret intact. Reuse `use-canvas-data.js`, provider acknowledgment, capture, and stored evidence without runtime changes.

    Acceptance: SC-003–006 pass through the real-provider browser harness, including two report/actual-image revision rounds, explicit current-revision approval, stale/missing-capture/duplicate/uncertain/declined cases, and unchanged-source return. Extend `scripts/dude-canvas-ui/t011-browser.test.mjs` and relevant `browser.test.mjs` cases without replacing inherited safety assertions. Obtain fresh visual/accessibility comparison evidence for this slice.

## Phase 5: Verify The Installed Walkthrough

- [ ] T005@e062a5b8 [Shared] Verify the integrated built and installed walkthrough, including read-only task drill-down, with `scripts/dude-canvas-ui/` acceptance, source/generated parity, and release checks; update `docs/commands.md` for the approved navigation and task-data limits.
    deps: T004@d062f4a7

    Build frontend assets and project generated core using the plan's existing commands. Recheck production byte pins maintained by each slice and adapt installed-driver navigation/task-inspection assertions in `t012-installed-host.mjs`; retain earlier approved-design hashes. Exercise the complete find -> Now/Tasks/detail -> response/Review -> return loop on realistic source-backed data, including five planned 062 tasks and real Done cases from 052, without writing real task state for examples.

    Verify full body/acceptance text, phase/filter/rollup agreement, dependency versus explicit-block distinctions, recorded/non-live copy, absent result/mapping coverage, and no unselected-body inventory or mixed-source detail. Preserve single-selector/Clear semantics, 48/208 rail and accessible narrow overlay, independent requests/drafts, and Review's pinned frame. Include width/theme, pointer/keyboard/focus, geometry, screenshots, accessibility, contrast, error transitions, and installed owner acknowledgment.

    Acceptance: SC-001–007 and VSC-001–003 have fresh evidence; named required suites execute nonzero cases without required skips; source/generated/static/legal parity and the existing size ceiling hold. Guidance explains read-only inspection, frozen previews, recorded in-progress versus live work, and unexposed evidence without promising task controls or Settings. Any human smoke is strictly host-only after automation. Return evidence/limitations for independent review and coordinator lint; this task grants no Git/release action or self-approval.
