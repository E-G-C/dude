<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T005@6d696772 [US1] Preserve and recheck the T005-approved frontmatter on the 29 authoritative `library/packs/*/agents/*.agent.md` sources: each pack stem has its accepted class, every specialist is `user-invocable: false`, canonical Copilot tool selectors remain intact, and every declared `agents` roster is non-empty, unique, stable-stem-only, free of self-reference, and resolvable in that pack. In `library/packs/technical-docs/agents/dude-pack-technical-docs-writer.agent.md`, add only the missing `agents` roster with exactly `dude-pack-technical-docs-extractor`, `dude-pack-technical-docs-planner`, `dude-pack-technical-docs-drafter`, and `dude-pack-technical-docs-reviewer`; keep its existing `agent` tool and body. Keep all pack sources free of concrete models, effort fields, MCP blocks, `skills`, and `disable-model-invocation`. The body-preservation evidence applies only to T005's own frontmatter mutations and explicitly excludes the separately owned T007 Agent Smith body rewrite; do not refresh installed `authoring` here or otherwise refactor the Technical Docs pack. Acceptance check: all 29 pack frontmatters match the approved assignment and composite rules, the Technical Docs Writer roster contains exactly those four stems, and T005 changes no byte in that writer's body; other bytes outside frontmatter match T005's own pre/post evidence except where T007 later owns Agent Smith's body. (FR-001 through FR-003, FR-014, FR-022; SC-001, SC-006)
- T011@76326472 [Shared] With the current `src/skills/dude-compose/compose.mjs` version 2 reader still intact, remove the currently installed `authoring`, `coding`, `release`, and `strata` packs through compose, using the recorded-source restore/remove guardrail if a source digest has drifted and never using `--force` or editing `.dude/metadata/profile.md`. Leave the existing version 1 `design` and `writing` entries untouched. Acceptance check: the profile contains no version 2 record, the four removed packs are absent from `enabled_packs` and `installed`, their recorded destinations are gone, and no unrelated installed byte changed. This is one-time implementation sequencing, not product migration code. (FR-009, FR-017; SC-007)
- T015@72657374 [Shared] Immediately after T011, use the currently installed generated version 2 compose/engine to add only `coding` and `authoring` from their exact canonical `library/packs/coding/` and `library/packs/authoring/` sources. Do not add `release` or `strata`, change the version 1 `design` or `writing` records or artifacts, edit `.dude/metadata/profile.md`, use `--force`, run a real-workspace development build, or overwrite the installed compose/engine. Acceptance check: the four coding direct profiles and all authoring direct profiles, including Agent Smith, are restored; `release` and `strata` remain absent; `design` and `writing` remain byte-identical version 1 installs; the new records are valid version 2 installs from the exact canonical sources; compose verification succeeds; and the installed version 2 remover bytes and T011's independently approved removal path remain intact for cutover. This is temporary execution routing, not product migration or persistent workflow state. (FR-013, FR-017; SC-005, SC-007)
- T012@636f6e66 [US1] Add `src/config/agent-models.json` with the plan's exact provenance/classes/targets shape and replace hardcoded data in `src/skills/dude-engine/lib/agent-model-map.mjs` with `loadAgentModelConfig(absolutePath)` plus `resolveCopilotModel(config, modelClass)`. The loader reads only its supplied absolute path, validates every known field, the closed effort vocabulary, and exact class coverage, deep-freezes the result, and has no eager load, default path, relative-path derivation, search, fallback, environment override, or embedded map. Simplify `agent-projection.mjs` to the neutral parser, `validateAgentSet`, one Copilot path, and a renderer that receives validated config explicitly. Treat omitted `agents` as leaf and present non-empty `agents` as the composite declaration; reject duplicate/display/self/unresolved entries and restrict unmixed `["*"]` to Dude. Delete plural outputs, Claude/SDK code, and static model/effort data. Acceptance check: valid sources render deterministic Copilot profiles from explicitly passed config; malformed config/source/set cases fail by name before output; no concrete model identifier remains in code; and no `composite` key, registry, cycle framework, or dropped renderer remains. (FR-003 through FR-010, FR-014, FR-022, FR-023; SC-002 through SC-004, SC-006)
- T013@746f706f [US3] In `src/skills/dude-engine/lib/ownership.mjs`, delete `CORE_TREES` and restore direct `.github/agents` classification/enumeration; add no configuration path or root rule because `.github/skills/dude-engine/config/agent-models.json` is ordinary content under the already-owned core skill. In `profile.mjs`, restore inventory version 1 and exact `.github/<source>` one-to-one binding; delete version 2 and its projection import. In `upgrade.mjs`, restore direct agent scanning and delete derived-root adoption machinery while keeping `verifyRestorableDestinations` and every ordinary path, symlink, evidence, rollback, pack-tier, and local-tier check. Acceptance check: ownership/profile/upgrade suites pass with one Copilot tree and version 1 only; the existing engine-skill enumerator includes packaged JSON without a new root rule; no plan field or report mentions derived adoption; and an ignored untracked planned destination is still refused before any write. (FR-015, FR-017 through FR-019; SC-008)
- T003@6275696c [US1] Change the shared development/release output planner to call `loadAgentModelConfig` with the absolute canonical `src/config/agent-models.json` path and validate it plus the complete core agent set before any cleanup, staging directory, or write. Render one `.github/agents/<stem>.agent.md` per source with the validated config and copy the canonical bytes unchanged to `.github/skills/dude-engine/config/agent-models.json`; create no `.github/config` path or ownership rule. Remove dropped-target build behavior while preserving stale Copilot cleanup, destination preflight, foreign tiers, projection equivalence, and unrelated byte parity. Add a bounded temporary release-bootstrap fixture that runs the last pre-feature installed upgrader behavior against the candidate release, proves its existing core-skill enumerator installs the new loader/engine files and packaged JSON, then runs upgraded compose add and renders a pack; add no production migration or compatibility branch. Acceptance check: invalid config leaves prior outputs unchanged, dev/release outputs are byte-stable and contain exact packaged bytes, and the historical bootstrap reaches a rendered pack. (FR-008, FR-011, FR-012, FR-015, FR-026; SC-004, SC-008)
- T004@636f6d70 [US2] Remove compose's top-level/static projection and config dependencies, including the profile module's transitive projection import. After command selection, add and verify dynamically acquire the loader and Copilot renderer, pass the absolute packaged config path, and fail before stage/profile/artifact mutation when either module or config is missing or invalid. Remove, list, and status dispatch without loading config, mapping, or renderer. Keep version 1 one-source/one-destination inventory, exact files/inventory congruence, installed hashes, safe recorded paths, rollback, and optional raw-source digest comparison only when source is available; removal must succeed after config deletion/corruption, renderer removal, source unavailability, or mapping change. Validate each complete incoming pack set before staging with `agents` as the composite declaration and no global claim. Acceptance check: focused missing/corrupt dependency cases leave add/verify unmutated, remove leaves zero leftovers, list/status dispatch, mapping-change remove/add picks up the new model, and invalid roster cases fail by name. (FR-013 through FR-017, FR-020, FR-022, FR-023; SC-005 through SC-007)
- T007@6c696e74 [US4] Simplify lint to direct owned Copilot checks and delete dropped-target branches. Replace eager class imports in lint/scaffolding with `loadAgentModelConfig` called on the absolute skill-local packaged path before any scaffold write, while adding no installed-model comparison. After T005 and the configuration foundation, rewrite only Agent Smith's body plus the relevant team-expansion/lint guidance for canonical source/configuration, explicit `agents` composite semantics, one Copilot output, un-emitted class effort, stable-stem delegation, visibility limits, concrete-model authority, and documentation-only adapters. Preserve Agent Smith's T005-approved frontmatter byte-for-byte, including class, visibility, tools, and roster; keep its coordinator-only boundary and authoring exclusions. Do not refresh installed `authoring` in this task. Acceptance check: lint/scaffolding fail closed on invalid packaged config, Agent Smith's approved frontmatter is unchanged across its body rewrite, no dropped-target branch remains, and installed authoring refresh is deferred to T014. (FR-021 through FR-024; SC-009 through SC-011)
- T014@7265696e [US2] Execute the roster-safe cutover as one bounded coordinator-run sequence with no specialist dispatch from recorded-source preparation through final roster verification. Keep the installed generated `.github` version 2 compose/engine untouched until it has removed the temporary `coding` and `authoring` installs. Before that removal, delete only generated base or already-unowned residual artifacts under the dropped trees, without touching temporary-pack recorded destinations or adding product cleanup machinery. For T007's Agent Smith source drift, copy the edited `library/packs/authoring/` source aside, prove no file-set change, restore the exact source recorded by the version 2 install, remove temporary `coding` and `authoring` with the preserved remover, and immediately restore the edited source as the final guardrail step; apply the same guardrail or stop on any other digest drift, and never use `--force` or edit the profile. Immediately run the first real-workspace `node scripts/build-dev.mjs` to install the version 1 engine and byte-identical packaged configuration, then immediately reinstall `authoring`, `coding`, `release`, and `strata` through version 1 compose from canonical sources while leaving `design` and `writing` intact. Acceptance check: the direct roster matches every applicable core and installed-pack agent source across the six profile records before dispatch resumes, Agent Smith's installed profile reflects T007 while preserving T005-approved frontmatter, every inventory is version 1 with one destination per source, the packaged configuration is byte-identical to canonical source, compose verify reports zero failures and leftovers, and no dropped target artifact exists. (FR-008, FR-011, FR-013, FR-016, FR-017, FR-026; SC-004, SC-005, SC-007, SC-010)
- T008@646f6373 [Shared] Keep corrected Copilot normalization and canonicalizer deletion, and remove stale three-target/CI prose. Rewrite compose, upgrade, reference, upgrading, and command documentation for `src/config/agent-models.json` and byte-identical `.github/skills/dude-engine/config/agent-models.json`, explicit caller-supplied loading, build validation before mutation, existing core-skill bootstrap ownership, compose's post-dispatch add/verify dependency and config/renderer-independent remove/list/status paths, version 1 evidence, regeneration and remove/add refresh, direct one-tree ownership, and generic unrestorable-destination refusal. Document `agents` as the sole composite declaration and the bounded core/incoming-pack validation scope. State the adapter contract and concrete-model authority without placing a real model identifier in independently authored docs. Delete first-adoption, version 2 downgrade, SDK lint, live effort emission, and three-tree claims. Acceptance check: docs contain no `.github/config`, fallback/search, extra composite key, global validation, or executable Claude/SDK claim; normalization remains exact; CI stays drift-only. (FR-003, FR-020, FR-022, FR-024 through FR-027; SC-002, SC-009 through SC-011)
- T009@66616c73 [US1] Replace `scripts/model-mapping-falsifier.test.mjs` with a temporary-workspace sequence: baseline build-dev and release, install a representative pack, derive a distinct replacement from one canonical model without hardcoding another identifier, edit only that config entry, rebuild, and prove only applicable base profiles change, every generated profile is projection-equivalent, and the skill-local packaged copy equals canonical bytes. With the old pack installed, remove it successfully, add it again, and prove the new model appears; rebuild once more byte-stably. Extract concrete model identifiers structurally from config and scan independently authored product code, tests, documentation, and pack/agent sources while excluding generated output and preserved `.dude` workflow/history evidence; then inject the replacement into unrelated authored code and require the test-local authority scan to fail. Do not scan generic effort words or add a production scanner/manifest. Acceptance check: each guarantee has a focused falsifying mutation and no real workspace byte changes. (FR-003 through FR-013, FR-016, FR-017, FR-026; SC-002 through SC-004, SC-007)
- T010@61637074 [Shared] Obtain fresh integrated evidence without changing this definition package or task state: verify the exact 32-source assignment and `agents` composite rules; run the full recursive suite; run build-dev twice and check drift; run Dude lint and compose verify; build and lint a release; run the bounded last-pre-feature-upgrader bootstrap through compose add; run the authority/remove-add falsifier; inspect every installed inventory as version 1; and run the generic ignored-destination upgrade fixture. Verify no `.github/config`, dropped tree, `CORE_TREES`, first-adoption, SDK-lint, static compose projection dependency, fallback/search/environment path, extra composite key/registry/cycle framework, production authority scanner, generated catalog, or installed-model comparison remains. Route the unchanged diff and evidence for independent review of source/config authority, bootstrap packaging, command dispatch and removal independence, composite scope, T005/T007 ownership, documentation honesty, and dead-code deletion. Acceptance check: every required check reports zero failures and an independent reviewer approves. (FR-001 through FR-027; SC-001 through SC-012)

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/agent-orchestration-metadata.md#coordinator-log -->

# Tasks: Agent Orchestration Metadata

These canonical units implement `.dude/specs/028-agent-orchestration-metadata/spec.md`, owned exactly by `.dude/ideas/agent-orchestration-metadata.md`.

The only generated agent target is `.github/agents/<stem>.agent.md`. Canonical core agents remain under `src/agents/`, canonical pack agents remain under `library/packs/*/agents/`, canonical model configuration lives at `src/config/agent-models.json`, and `.github/skills/dude-engine/config/agent-models.json` is its generated byte-identical packaged copy. Configuration loads only from caller-supplied absolute paths. No task may add `.github/config/`, a Claude or SDK renderer, another ownership root, a compatibility or migration branch, fallback/search/environment settings, a `composite` key or registry, a production model-authority scanner, a generated pack catalog, or a model-literal check against an installed profile.

T011's accepted operation drained all four version 2 packs. T015 restores only `coding` and `authoring` through the still-installed generated version 2 compose/engine before any dependent implementation. That installed remover remains untouched while source work proceeds; no real-workspace development build occurs before T014. T014 is one coordinator-run no-dispatch cutover that applies the recorded-source guardrail, removes temporary `coding` and `authoring` with version 2, runs the first real-workspace build, immediately reinstalls `authoring`, `coding`, `release`, and `strata` under version 1, and verifies routing before further dispatch. Core `.github/` output is never hand-edited.

## Phase 1: Preserved Source Assignment

**Goal**: Preserve the surviving 29-pack-source frontmatter migration and apply its one bounded missing-roster correction.

- [x] T005@6d696772 [US1] Preserve and recheck the T005-approved frontmatter on the 29 authoritative `library/packs/*/agents/*.agent.md` sources: each pack stem has its accepted class, every specialist is `user-invocable: false`, canonical Copilot tool selectors remain intact, and every declared `agents` roster is non-empty, unique, stable-stem-only, free of self-reference, and resolvable in that pack. In `library/packs/technical-docs/agents/dude-pack-technical-docs-writer.agent.md`, add only the missing `agents` roster with exactly `dude-pack-technical-docs-extractor`, `dude-pack-technical-docs-planner`, `dude-pack-technical-docs-drafter`, and `dude-pack-technical-docs-reviewer`; keep its existing `agent` tool and body. Keep all pack sources free of concrete models, effort fields, MCP blocks, `skills`, and `disable-model-invocation`. The body-preservation evidence applies only to T005's own frontmatter mutations and explicitly excludes the separately owned T007 Agent Smith body rewrite; do not refresh installed `authoring` here or otherwise refactor the Technical Docs pack. Acceptance check: all 29 pack frontmatters match the approved assignment and composite rules, the Technical Docs Writer roster contains exactly those four stems, and T005 changes no byte in that writer's body; other bytes outside frontmatter match T005's own pre/post evidence except where T007 later owns Agent Smith's body. (FR-001 through FR-003, FR-014, FR-022; SC-001, SC-006)

## Phase 2: Drain Unreleased Dogfood State

**Goal**: Preserve T011's accepted four-pack drain and the independently verified version 2 removal path before the bounded routing restore.

- [x] T011@76326472 [Shared] With the current `src/skills/dude-compose/compose.mjs` version 2 reader still intact, remove the currently installed `authoring`, `coding`, `release`, and `strata` packs through compose, using the recorded-source restore/remove guardrail if a source digest has drifted and never using `--force` or editing `.dude/metadata/profile.md`. Leave the existing version 1 `design` and `writing` entries untouched. Acceptance check: the profile contains no version 2 record, the four removed packs are absent from `enabled_packs` and `installed`, their recorded destinations are gone, and no unrelated installed byte changed. This is one-time implementation sequencing, not product migration code. (FR-009, FR-017; SC-007)

## Phase 3: Restore Routable Specialists

**Goal**: Restore only the direct specialists required to route the remaining source work while retaining the proven version 2 remover.

- [x] T015@72657374 [Shared] Immediately after T011, use the currently installed generated version 2 compose/engine to add only `coding` and `authoring` from their exact canonical `library/packs/coding/` and `library/packs/authoring/` sources. Do not add `release` or `strata`, change the version 1 `design` or `writing` records or artifacts, edit `.dude/metadata/profile.md`, use `--force`, run a real-workspace development build, or overwrite the installed compose/engine. Acceptance check: the four coding direct profiles and all authoring direct profiles, including Agent Smith, are restored; `release` and `strata` remain absent; `design` and `writing` remain byte-identical version 1 installs; the new records are valid version 2 installs from the exact canonical sources; compose verification succeeds; and the installed version 2 remover bytes and T011's independently approved removal path remain intact for cutover. This is temporary execution routing, not product migration or persistent workflow state. (FR-013, FR-017; SC-005, SC-007)
    deps: T011@76326472

## Phase 4: Configuration And Copilot Adapter

**Goal**: Replace hardcoded three-target resolution with one fail-closed configuration and one production renderer.

- [x] T012@636f6e66 [US1] Add `src/config/agent-models.json` with the plan's exact provenance/classes/targets shape and replace hardcoded data in `src/skills/dude-engine/lib/agent-model-map.mjs` with `loadAgentModelConfig(absolutePath)` plus `resolveCopilotModel(config, modelClass)`. The loader reads only its supplied absolute path, validates every known field, the closed effort vocabulary, and exact class coverage, deep-freezes the result, and has no eager load, default path, relative-path derivation, search, fallback, environment override, or embedded map. Simplify `agent-projection.mjs` to the neutral parser, `validateAgentSet`, one Copilot path, and a renderer that receives validated config explicitly. Treat omitted `agents` as leaf and present non-empty `agents` as the composite declaration; reject duplicate/display/self/unresolved entries and restrict unmixed `["*"]` to Dude. Delete plural outputs, Claude/SDK code, and static model/effort data. Acceptance check: valid sources render deterministic Copilot profiles from explicitly passed config; malformed config/source/set cases fail by name before output; no concrete model identifier remains in code; and no `composite` key, registry, cycle framework, or dropped renderer remains. (FR-003 through FR-010, FR-014, FR-022, FR-023; SC-002 through SC-004, SC-006)
    deps: T015@72657374

## Phase 5: Direct Ownership And Version 1

**Goal**: Remove every product path whose only purpose was the two dropped trees while keeping generic upgrade safety.

- [x] T013@746f706f [US3] In `src/skills/dude-engine/lib/ownership.mjs`, delete `CORE_TREES` and restore direct `.github/agents` classification/enumeration; add no configuration path or root rule because `.github/skills/dude-engine/config/agent-models.json` is ordinary content under the already-owned core skill. In `profile.mjs`, restore inventory version 1 and exact `.github/<source>` one-to-one binding; delete version 2 and its projection import. In `upgrade.mjs`, restore direct agent scanning and delete derived-root adoption machinery while keeping `verifyRestorableDestinations` and every ordinary path, symlink, evidence, rollback, pack-tier, and local-tier check. Acceptance check: ownership/profile/upgrade suites pass with one Copilot tree and version 1 only; the existing engine-skill enumerator includes packaged JSON without a new root rule; no plan field or report mentions derived adoption; and an ignored untracked planned destination is still refused before any write. (FR-015, FR-017 through FR-019; SC-008)
    deps: T015@72657374

## Phase 6: Build And Compose Source

**Goal**: Prepare the version 1 build and compose paths without overwriting the installed version 2 remover.

- [x] T003@6275696c [US1] Change the shared development/release output planner to call `loadAgentModelConfig` with the absolute canonical `src/config/agent-models.json` path and validate it plus the complete core agent set before any cleanup, staging directory, or write. Render one `.github/agents/<stem>.agent.md` per source with the validated config and copy the canonical bytes unchanged to `.github/skills/dude-engine/config/agent-models.json`; create no `.github/config` path or ownership rule. Remove dropped-target build behavior while preserving stale Copilot cleanup, destination preflight, foreign tiers, projection equivalence, and unrelated byte parity. Add a bounded temporary release-bootstrap fixture that runs the last pre-feature installed upgrader behavior against the candidate release, proves its existing core-skill enumerator installs the new loader/engine files and packaged JSON, then runs upgraded compose add and renders a pack; add no production migration or compatibility branch. Acceptance check: invalid config leaves prior outputs unchanged, dev/release outputs are byte-stable and contain exact packaged bytes, and the historical bootstrap reaches a rendered pack. (FR-008, FR-011, FR-012, FR-015, FR-026; SC-004, SC-008)
    deps: T004@636f6d70, T012@636f6e66, T013@746f706f

- [x] T004@636f6d70 [US2] Remove compose's top-level/static projection and config dependencies, including the profile module's transitive projection import. After command selection, add and verify dynamically acquire the loader and Copilot renderer, pass the absolute packaged config path, and fail before stage/profile/artifact mutation when either module or config is missing or invalid. Remove, list, and status dispatch without loading config, mapping, or renderer. Keep version 1 one-source/one-destination inventory, exact files/inventory congruence, installed hashes, safe recorded paths, rollback, and optional raw-source digest comparison only when source is available; removal must succeed after config deletion/corruption, renderer removal, source unavailability, or mapping change. Validate each complete incoming pack set before staging with `agents` as the composite declaration and no global claim. Acceptance check: focused missing/corrupt dependency cases leave add/verify unmutated, remove leaves zero leftovers, list/status dispatch, mapping-change remove/add picks up the new model, and invalid roster cases fail by name. (FR-013 through FR-017, FR-020, FR-022, FR-023; SC-005 through SC-007)
    deps: T012@636f6e66, T013@746f706f

## Phase 7: Enforcement And Agent Smith

**Goal**: Make mechanical checks and Agent Smith describe only the shipped behavior while keeping generated dogfood untouched.

- [x] T007@6c696e74 [US4] Simplify lint to direct owned Copilot checks and delete dropped-target branches. Replace eager class imports in lint/scaffolding with `loadAgentModelConfig` called on the absolute skill-local packaged path before any scaffold write, while adding no installed-model comparison. After T005 and the configuration foundation, rewrite only Agent Smith's body plus the relevant team-expansion/lint guidance for canonical source/configuration, explicit `agents` composite semantics, one Copilot output, un-emitted class effort, stable-stem delegation, visibility limits, concrete-model authority, and documentation-only adapters. Preserve Agent Smith's T005-approved frontmatter byte-for-byte, including class, visibility, tools, and roster; keep its coordinator-only boundary and authoring exclusions. Do not refresh installed `authoring` in this task. Acceptance check: lint/scaffolding fail closed on invalid packaged config, Agent Smith's approved frontmatter is unchanged across its body rewrite, no dropped-target branch remains, and installed authoring refresh is deferred to T014. (FR-021 through FR-024; SC-009 through SC-011)
    deps: T005@6d696772, T012@636f6e66

## Phase 8: Coordinator Cutover

**Goal**: Remove the temporary version 2 packs, install the version 1 engine, immediately restore all four packs, and verify routability.

- [x] T014@7265696e [US2] Execute the roster-safe cutover as one bounded coordinator-run sequence with no specialist dispatch from recorded-source preparation through final roster verification. Keep the installed generated `.github` version 2 compose/engine untouched until it has removed the temporary `coding` and `authoring` installs. Before that removal, delete only generated base or already-unowned residual artifacts under the dropped trees, without touching temporary-pack recorded destinations or adding product cleanup machinery. For T007's Agent Smith source drift, copy the edited `library/packs/authoring/` source aside, prove no file-set change, restore the exact source recorded by the version 2 install, remove temporary `coding` and `authoring` with the preserved remover, and immediately restore the edited source as the final guardrail step; apply the same guardrail or stop on any other digest drift, and never use `--force` or edit the profile. Immediately run the first real-workspace `node scripts/build-dev.mjs` to install the version 1 engine and byte-identical packaged configuration, then immediately reinstall `authoring`, `coding`, `release`, and `strata` through version 1 compose from canonical sources while leaving `design` and `writing` intact. Acceptance check: the direct roster matches every applicable core and installed-pack agent source across the six profile records before dispatch resumes, Agent Smith's installed profile reflects T007 while preserving T005-approved frontmatter, every inventory is version 1 with one destination per source, the packaged configuration is byte-identical to canonical source, compose verify reports zero failures and leftovers, and no dropped target artifact exists. (FR-008, FR-011, FR-013, FR-016, FR-017, FR-026; SC-004, SC-005, SC-007, SC-010)
    deps: T003@6275696c, T004@636f6d70, T007@6c696e74, T012@636f6e66, T013@746f706f

## Phase 9: Documentation And Falsifier

**Goal**: Update developer contracts and prove the one-edit/deletion boundary only after the direct roster is restored under version 1.

- [x] T008@646f6373 [Shared] Keep corrected Copilot normalization and canonicalizer deletion, and remove stale three-target/CI prose. Rewrite compose, upgrade, reference, upgrading, and command documentation for `src/config/agent-models.json` and byte-identical `.github/skills/dude-engine/config/agent-models.json`, explicit caller-supplied loading, build validation before mutation, existing core-skill bootstrap ownership, compose's post-dispatch add/verify dependency and config/renderer-independent remove/list/status paths, version 1 evidence, regeneration and remove/add refresh, direct one-tree ownership, and generic unrestorable-destination refusal. Document `agents` as the sole composite declaration and the bounded core/incoming-pack validation scope. State the adapter contract and concrete-model authority without placing a real model identifier in independently authored docs. Delete first-adoption, version 2 downgrade, SDK lint, live effort emission, and three-tree claims. Acceptance check: docs contain no `.github/config`, fallback/search, extra composite key, global validation, or executable Claude/SDK claim; normalization remains exact; CI stays drift-only. (FR-003, FR-020, FR-022, FR-024 through FR-027; SC-002, SC-009 through SC-011)
    deps: T014@7265696e

- [x] T009@66616c73 [US1] Replace `scripts/model-mapping-falsifier.test.mjs` with a temporary-workspace sequence: baseline build-dev and release, install a representative pack, derive a distinct replacement from one canonical model without hardcoding another identifier, edit only that config entry, rebuild, and prove only applicable base profiles change, every generated profile is projection-equivalent, and the skill-local packaged copy equals canonical bytes. With the old pack installed, remove it successfully, add it again, and prove the new model appears; rebuild once more byte-stably. Extract concrete model identifiers structurally from config and scan independently authored product code, tests, documentation, and pack/agent sources while excluding generated output and preserved `.dude` workflow/history evidence; then inject the replacement into unrelated authored code and require the test-local authority scan to fail. Do not scan generic effort words or add a production scanner/manifest. Acceptance check: each guarantee has a focused falsifying mutation and no real workspace byte changes. (FR-003 through FR-013, FR-016, FR-017, FR-026; SC-002 through SC-004, SC-007)
    deps: T014@7265696e

## Phase 10: Integrated Acceptance

**Goal**: Obtain complete fresh evidence and independent review after documentation and falsification.

- [x] T010@61637074 [Shared] Obtain fresh integrated evidence without changing this definition package or task state: verify the exact 32-source assignment and `agents` composite rules; run the full recursive suite; run build-dev twice and check drift; run Dude lint and compose verify; build and lint a release; run the bounded last-pre-feature-upgrader bootstrap through compose add; run the authority/remove-add falsifier; inspect every installed inventory as version 1; and run the generic ignored-destination upgrade fixture. Verify no `.github/config`, dropped tree, `CORE_TREES`, first-adoption, SDK-lint, static compose projection dependency, fallback/search/environment path, extra composite key/registry/cycle framework, production authority scanner, generated catalog, or installed-model comparison remains. Route the unchanged diff and evidence for independent review of source/config authority, bootstrap packaging, command dispatch and removal independence, composite scope, T005/T007 ownership, documentation honesty, and dead-code deletion. Acceptance check: every required check reports zero failures and an independent reviewer approves. (FR-001 through FR-027; SC-001 through SC-012)
    deps: T005@6d696772, T008@646f6373, T009@66616c73, T014@7265696e

## Requirements And Success Traceability

| Specification coverage | Tasks |
|---|---|
| FR-001 through FR-003 / SC-001 through SC-003 | T005@6d696772, T009@66616c73, T010@61637074 |
| FR-004 through FR-007 / SC-002, SC-003 | T012@636f6e66, T009@66616c73, T010@61637074 |
| FR-008 through FR-012 / SC-004 | T012@636f6e66, T003@6275696c, T014@7265696e, T009@66616c73 |
| FR-013, FR-014 / SC-005 through SC-007 | T005@6d696772, T015@72657374, T004@636f6d70, T014@7265696e, T010@61637074 |
| FR-015 through FR-019 / SC-007, SC-008 | T011@76326472, T015@72657374, T013@746f706f, T003@6275696c, T004@636f6d70, T014@7265696e |
| FR-020, FR-021, FR-027 / SC-009 | T004@636f6d70, T007@6c696e74, T008@646f6373 |
| FR-022 through FR-025 / SC-006, SC-010, SC-011 | T005@6d696772, T007@6c696e74, T008@646f6373 |
| FR-026 / SC-003, SC-004, SC-010 | T003@6275696c, T008@646f6373, T009@66616c73, T014@7265696e |
| SC-012 | T010@61637074 |

## Lightweight Execution History

- dude-run-event: {"basis":{"action":"execute-task","assumptionIdentities":[],"evidenceAcquisitionIdentities":[],"materialInputs":{"checks":["verification"],"operations":["execute-task"],"targets":["src/skills/dude-engine/lib/agent-model-map.mjs","src/skills/dude-engine/lib/agent-model-map.test.mjs","src/skills/dude-engine/lib/agent-projection.mjs","src/skills/dude-engine/lib/agent-projection.test.mjs"]},"mechanismIdentities":[],"target":{"lane":"lightweight","specPath":".dude/specs/028-agent-orchestration-metadata/spec.md","taskKey":"T001@6d617073"},"validationPlanIdentities":[],"version":1},"eventHash":"d87515908261723eae76620df0e931ef6e64a2c1f6b8f5ed378c5116f8aa9288","occurrence":{"attemptIdentity":"0b03de39b90d864a60e692197ec937a4d853ae7cefcf933b8b32cb616d554887","authorizationEvidenceHash":"2dc585cfd44a59314a249da22542a13401747a797ba4e0770fa9063b3cc71a72","basisIdentity":"adc8ec024f57d903d6282e7ebc6742159c4265066c2e0ef786e78b993302a76a","chronology":{"attemptOrdinal":1},"disposition":"review-rejected","resultIdentity":"3394fcf404451c7f15e11990785d8ab8be58c78839632a345d1e9f91bb93603f","version":1},"occurrenceIdentity":"a416c6d4332b026ada3e8610685adfc5c3a4beabd70dc4f0309a046a68a81f7c","reviewEnvelopeIdentity":"a53652640f6c035ddf30cfc05cb086ad0fee32ee60b2c378828f18c6d48d851f","target":{"lane":"lightweight","specPath":".dude/specs/028-agent-orchestration-metadata/spec.md","taskKey":"T001@6d617073"},"type":"approach-occurrence","verificationEnvelopeIdentity":"0081a284c6e59fcd992326bc676ee57540fa3e8d02cd706d30901dd0fbbe2f21","version":1}
- dude-run-event: {"basis":{"checkDefinitionIdentity":"7fcf73680f52b8be3641c2d12b40fc2e9b63ea6a9041346b5fc8bf93f7390b42","expectation":{"identity":"0ad1426bf4454f78f98d22aaf1b7a7d1eff843d927fc46bf580fd46109b53e80","kind":"governing-rule"},"failureClass":"review-escalation","subjects":["library/packs/newsroom/agents/dude-pack-newsroom-event-deep-fetcher.agent.md","library/packs/release/agents/dude-pack-release-manager.agent.md","src/skills/dude-engine/lib/agent-model-map.mjs","src/skills/dude-engine/lib/agent-projection.mjs"],"target":{"lane":"lightweight","specPath":".dude/specs/028-agent-orchestration-metadata/spec.md","taskKey":"T001@6d617073"},"version":1},"eventHash":"2f9b3c0f6d323b5d4b146fefadcab671726c19d1a9c5fb3c2a9e16271771fd97","occurrence":{"attemptApproachBasisIdentity":"adc8ec024f57d903d6282e7ebc6742159c4265066c2e0ef786e78b993302a76a","attemptIdentity":"0b03de39b90d864a60e692197ec937a4d853ae7cefcf933b8b32cb616d554887","basisIdentity":"0f4c3d4bc327bc54b201fe0209ced760191f3057efb62d69a511323ae050e9d1","chronology":{"attemptOrdinal":1,"reviewOrdinal":1},"findingIdentity":"858e8447e6b42b39e5001650c7cea883e1bed8e7443c47b9ee390d02e77d60f4","observation":{"identity":"9efd406ff04e866a56585530b040a4e4a541f8ef2579ef773eaa5e84afecd0c5","kind":"observed-evidence"},"reviewEnvelopeIdentity":"a53652640f6c035ddf30cfc05cb086ad0fee32ee60b2c378828f18c6d48d851f","version":1},"occurrenceIdentity":"95b117c4d65eab91ca08c21ed9963c7f13e75f94e6cc9277b7f0fc5d21e13ddc","sourceCaptureIdentity":"674e827022942a547556fed7a0f6b029d017dc41a2a83980ab150dc87cd62d79","target":{"lane":"lightweight","specPath":".dude/specs/028-agent-orchestration-metadata/spec.md","taskKey":"T001@6d617073"},"type":"finding-occurrence","version":1}

- dude-redefinition-archive: {"version":1,"taskKey":"T001@6d617073","priorState":"complete","classification":"superseded-by-explicit-redefinition","dropReason":"The durable unit implemented and verified a three-target model/projection foundation, including Claude and SDK rendering and effort emission. The revised feature requires those capabilities deleted and moves values from code to a canonical configuration. Treating that reversal as a changed task would falsely preserve the completed meaning.","successorTaskKeys":["T012@636f6e66","T007@6c696e74","T008@646f6373","T009@66616c73"],"carryCompletion":false,"confirmedAt":"2026-08-10T01:20:54Z"}
- dude-redefinition-archive: {"version":1,"taskKey":"T002@6f776e73","priorState":"complete","classification":"superseded-by-explicit-redefinition","dropReason":"The durable unit added CORE_TREES and inventory version 2 for one source with three destinations. The verified production topology requires direct one-tree ownership and version 1. Its central meaning is reversed, not narrowed.","successorTaskKeys":["T011@76326472","T013@746f706f","T004@636f6d70"],"carryCompletion":false,"confirmedAt":"2026-08-10T01:20:54Z"}
- dude-redefinition-archive: {"version":1,"taskKey":"T006@75706772","priorState":"complete","classification":"superseded-by-explicit-redefinition","dropReason":"The durable unit implemented target-specific first-adoption preservation for two generated roots that no longer exist. The revised obligation is to delete that machinery while retaining only an independent generic destination preflight.","successorTaskKeys":["T013@746f706f","T008@646f6373"],"carryCompletion":false,"confirmedAt":"2026-08-10T01:20:54Z"}
