<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@70726f64 [Shared] Implement one dependency-free closed verification/review builder in `src/skills/dude-work/specialist-attestation.mjs` with focused tests in `src/skills/dude-work/specialist-attestation.test.mjs`: admit the sole own-data, inert structured specialist result as the exclusive semantic source; preserve the complete semantic sets of check definitions, findings, and subjects and every row's exact semantic fields; emit set-like collections in Feature 009's required canonical order without promising input-order preservation; reject every equal, conflicting, or byte-identical duplicate check definition, finding, or subject before sorting or construction so none is silently dropped or deduplicated; derive only the identities required by the existing capture shape from that result and authoritative context; reuse the existing trusted-capture and envelope validators; prove complete-set and exact-row-field preservation, canonical ordering, and internal consistency; and refuse separate semantic overrides, precomputed identities, unknown fields, malformed or incomplete records, context mismatches, cycles, arrays with extra own keys, and inherited, accessor-backed, executable, custom-prototype, or otherwise behavior-bearing containers. Include focused cycle and extra-key-array refusal fixtures. Do not claim detection of a different otherwise-valid sole result or malicious or pre-boundary rewriting. Verify with `node --test src/skills/dude-work/specialist-attestation.test.mjs`.
- T002@696e7467 [Shared] Integrate the builder into autonomous `record-attempt-result` in `src/skills/dude-work/host-adapter.mjs` and `src/skills/dude-work/host-adapter.test.mjs`: host integration acquires the sole structured Tester and independent Reviewer results from their actual dispatches, derives authoritative target, attempt, source-revision, dispatch, and chronology context from accepted host state, retains the exact builder-produced verification capture, and passes that exact output into review construction. Ordinary requests provide no semantic overrides, precomputed trusted identities, dispatch context, verification capture choice, or low-level route. Feed builder outputs into the unchanged trusted completion, projection, finalization, permit, receipt, recovery, learning, and close flow; preserve guarded behavior and Feature 009/018 semantics. Update the cooperative-attestation boundary and its pre-boundary rewrite limit concisely in `src/skills/dude-work/SKILL.md`, `src/agents/dude.agent.md`, `scripts/current-format-contract.test.mjs`, and `docs/reference.md`; run `node scripts/build-dev.mjs`; verify with `node --test src/skills/dude-work/specialist-attestation.test.mjs src/skills/dude-work/host-adapter.test.mjs src/skills/dude-work/recovery.test.mjs scripts/current-format-contract.test.mjs scripts/build-dev.test.mjs`; and prove a focused production-path autonomous close plus refusal of ordinary-request verification substitution, caller-authored identities or overrides, and low-level routes without a throwaway driver.
- T003@76616c69 [Shared] Run final acceptance over the unchanged integrated revision: demonstrate one end-to-end autonomous Lightweight close using sole structured Tester and independent Reviewer results acquired by host integration from actual dispatch, authoritative host context, and exact prior verification-output threading through the ordinary host adapter; prove exact semantic preservation and refusal of separate overrides, precomputed identities, malformed or incomplete records, internal inconsistencies, unknown fields, hostile containers, ordinary-request verification selection, low-level routes, replay across mismatched context, and target, attempt, dispatch, chronology, and source-revision mismatches before trusted capture or close authority; verify that a different otherwise-valid sole result is treated as a different cooperative assertion without pre-boundary rewrite-detection claims; verify guarded behavior, Feature 009 validators and learning governance, Feature 018 continuity, lane permits and receipts, ownership, safety, budgets, review independence, generated parity, and zero forbidden signatures, keys, registries, transcript parsers, services, state, or bytes-and-hash authenticity theater; run the full repository, lint, compose, pristine-release, generated-scope, and whitespace gates from `plan.md`; route one fresh unchanged evidence set independently to the Tester and Code Reviewer; and confirm Feature 018 T004 and Feature 017 T003 remain blocked with neither owning package mutated.

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/019-specialist-attestation-producer.md#coordinator-log -->

<!-- canonical task units - only the coordinator mutates task state or metadata -->

# Tasks: Specialist Attestation Producer

Three sequential canonical units implement the contract at `.dude/specs/019-specialist-attestation-producer/spec.md`, owned exactly by `.dude/ideas/specialist-attestation-producer.md`.

Execution may read but must not write this definition package. A required normative change stops as `contract-mismatch: redefine-required`. No task may mutate Feature 017 or Feature 018 task glyphs, blockers, metadata, generated boards, execution history, or state. Feature 018 T004 and Feature 017 T003 remain blocked until independent acceptance and later coordinator-owned reconciliation.

No task carries `[P]`. Durable suffixes are fixed lowercase hexadecimal encodings of `prod`, `inte`, and `vali`.

## Phase 1: Production Attestation Builder

- [x] T001@70726f64 [Shared] Implement one dependency-free closed verification/review builder in `src/skills/dude-work/specialist-attestation.mjs` with focused tests in `src/skills/dude-work/specialist-attestation.test.mjs`: admit the sole own-data, inert structured specialist result as the exclusive semantic source; preserve the complete semantic sets of check definitions, findings, and subjects and every row's exact semantic fields; emit set-like collections in Feature 009's required canonical order without promising input-order preservation; reject every equal, conflicting, or byte-identical duplicate check definition, finding, or subject before sorting or construction so none is silently dropped or deduplicated; derive only the identities required by the existing capture shape from that result and authoritative context; reuse the existing trusted-capture and envelope validators; prove complete-set and exact-row-field preservation, canonical ordering, and internal consistency; and refuse separate semantic overrides, precomputed identities, unknown fields, malformed or incomplete records, context mismatches, cycles, arrays with extra own keys, and inherited, accessor-backed, executable, custom-prototype, or otherwise behavior-bearing containers. Include focused cycle and extra-key-array refusal fixtures. Do not claim detection of a different otherwise-valid sole result or malicious or pre-boundary rewriting. Verify with `node --test src/skills/dude-work/specialist-attestation.test.mjs`.
   blocked-by: contract-mismatch: Feature 009 requires sorted duplicate-free checks/findings while Feature 019 incorrectly requires input-sequence preservation; clarify canonical semantic-set preservation and duplicate refusal

## Phase 2: Ordinary Work Integration

- [x] T002@696e7467 [Shared] Integrate the builder into autonomous `record-attempt-result` in `src/skills/dude-work/host-adapter.mjs` and `src/skills/dude-work/host-adapter.test.mjs`: host integration acquires the sole structured Tester and independent Reviewer results from their actual dispatches, derives authoritative target, attempt, source-revision, dispatch, and chronology context from accepted host state, retains the exact builder-produced verification capture, and passes that exact output into review construction. Ordinary requests provide no semantic overrides, precomputed trusted identities, dispatch context, verification capture choice, or low-level route. Feed builder outputs into the unchanged trusted completion, projection, finalization, permit, receipt, recovery, learning, and close flow; preserve guarded behavior and Feature 009/018 semantics. Update the cooperative-attestation boundary and its pre-boundary rewrite limit concisely in `src/skills/dude-work/SKILL.md`, `src/agents/dude.agent.md`, `scripts/current-format-contract.test.mjs`, and `docs/reference.md`; run `node scripts/build-dev.mjs`; verify with `node --test src/skills/dude-work/specialist-attestation.test.mjs src/skills/dude-work/host-adapter.test.mjs src/skills/dude-work/recovery.test.mjs scripts/current-format-contract.test.mjs scripts/build-dev.test.mjs`; and prove a focused production-path autonomous close plus refusal of ordinary-request verification substitution, caller-authored identities or overrides, and low-level routes without a throwaway driver.
    deps: T001@70726f64

## Phase 3: Full Acceptance

- [x] T003@76616c69 [Shared] Run final acceptance over the unchanged integrated revision: demonstrate one end-to-end autonomous Lightweight close using sole structured Tester and independent Reviewer results acquired by host integration from actual dispatch, authoritative host context, and exact prior verification-output threading through the ordinary host adapter; prove exact semantic preservation and refusal of separate overrides, precomputed identities, malformed or incomplete records, internal inconsistencies, unknown fields, hostile containers, ordinary-request verification selection, low-level routes, replay across mismatched context, and target, attempt, dispatch, chronology, and source-revision mismatches before trusted capture or close authority; verify that a different otherwise-valid sole result is treated as a different cooperative assertion without pre-boundary rewrite-detection claims; verify guarded behavior, Feature 009 validators and learning governance, Feature 018 continuity, lane permits and receipts, ownership, safety, budgets, review independence, generated parity, and zero forbidden signatures, keys, registries, transcript parsers, services, state, or bytes-and-hash authenticity theater; run the full repository, lint, compose, pristine-release, generated-scope, and whitespace gates from `plan.md`; route one fresh unchanged evidence set independently to the Tester and Code Reviewer; and confirm Feature 018 T004 and Feature 017 T003 remain blocked with neither owning package mutated.
    deps: T002@696e7467

## Requirements And Success Traceability

| Specification coverage | Plan ownership | Tasks |
|---|---|---|
| FR-001 through FR-005 / SC-001 through SC-003 | Closed builder, internal identity derivation, semantic preservation, mismatch refusal, existing-validator authority | T001@70726f64, T002@696e7467, T003@76616c69 |
| FR-006 through FR-008 / SC-004, SC-005 | Ordinary host-adapter integration, unchanged trusted flow, guarded and authority compatibility | T002@696e7467, T003@76616c69 |
| FR-009 / SC-006 | Cooperative trust wording and rejected architecture | T001@70726f64, T002@696e7467, T003@76616c69 |
| FR-010 / SC-003, SC-004, SC-007 | End-to-end actual-result close, negative proof, full validation, independent acceptance | T002@696e7467, T003@76616c69 |
| FR-011 / SC-007 | Cross-feature non-mutation and coordinator-owned later reconciliation | T003@76616c69 |
