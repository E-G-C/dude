<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@65786563 [Shared] Implement `## Chosen Design` sections 1 through 4 in `src/skills/dude-feature-definition/publish-first-definition.mjs`, using the existing `applyAtomicFileBatch` unchanged as the transaction owner. Add `src/skills/dude-feature-definition/publish-first-definition.test.mjs` with the spawned success, lint-failure rollback, and preflight mutation-table coverage from `## Verification design`. Assert exact four-destination bytes, no unrelated mutation, exact rollback including package-directory removal, and no success output before lint. Run the focused executable test. (US1, US2, US3 -> FR-001 through FR-010)
- T002@77697265 [Shared] Implement `## Chosen Design` sections 5 through 7. Update only `## First Definition Transaction` in `src/skills/dude-feature-definition/SKILL.md` as needed, extend `scripts/current-format-contract.test.mjs` with the section-bound mutation contract, and include the new executable in the applicable active-source inventory. Run `node scripts/build-dev.mjs` so the executable and SKILL changes are projected from source. Run the focused executable, current-format-contract, and build-dev tests, and confirm both generated feature-definition files are byte-identical to source. (US1, US3 -> FR-001, FR-002, FR-008, FR-009, FR-010)
- T003@61637074 [Shared] Run acceptance over the unchanged integrated revision: rerun the focused tests and section-contract mutations; run the full discovered test suite, `node .github/skills/dude-lint/lint.mjs .`, compose verification, a pristine release build followed by release lint, `node scripts/build-dev.mjs`, generated-core diff inspection, and `git diff --check`; confirm the only generated feature-definition changes are the projected executable and SKILL, and confirm no bootstrap script or persistent stage exists. Route one fresh evidence set for independent review without modifying this package or execution state. (US1, US2, US3 -> FR-001 through FR-010)

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/first-definition-publish.md#coordinator-log -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

# Tasks: First-Definition Publish

Three all-open canonical units implement the contract at `.dude/specs/023-first-definition-publish/spec.md`, owned prospectively and exactly by `.dude/ideas/first-definition-publish.md`.

The `Rejected designs` list in `plan.md` is the implementation boundary. Any work outside it stops with `plan-gap`.

No task carries `[P]`. T002 documents and projects the executable from T001, and T003 validates the integrated result. Durable suffixes are fixed lowercase hexadecimal encodings of `exec`, `wire`, and `acpt`.

Core source lives under `src/`; `.github/` core is generated only by `node scripts/build-dev.mjs` and is never hand-edited.

Execution may read but must not write any `.dude/specs/023-first-definition-publish/**` artifact. No task may mutate another feature's package, task state, board, mirror, execution history, or Coordinator Log. A required normative change stops as `contract-mismatch: redefine-required`.

## Phase 1: Executable And Focused Tests

**Goal**: Add the one production edge and prove exact success, pre-mutation refusal, and rollback-bound lint.

- [x] T001@65786563 [Shared] Implement `## Chosen Design` sections 1 through 4 in `src/skills/dude-feature-definition/publish-first-definition.mjs`, using the existing `applyAtomicFileBatch` unchanged as the transaction owner. Add `src/skills/dude-feature-definition/publish-first-definition.test.mjs` with the spawned success, lint-failure rollback, and preflight mutation-table coverage from `## Verification design`. Assert exact four-destination bytes, no unrelated mutation, exact rollback including package-directory removal, and no success output before lint. Run the focused executable test. (US1, US2, US3 -> FR-001 through FR-010)

## Phase 2: Procedure Contract And Projection

**Goal**: Make the authoritative first-definition procedure invoke the executable with an ephemeral stage and pin that contract against weakening.

- [x] T002@77697265 [Shared] Implement `## Chosen Design` sections 5 through 7. Update only `## First Definition Transaction` in `src/skills/dude-feature-definition/SKILL.md` as needed, extend `scripts/current-format-contract.test.mjs` with the section-bound mutation contract, and include the new executable in the applicable active-source inventory. Run `node scripts/build-dev.mjs` so the executable and SKILL changes are projected from source. Run the focused executable, current-format-contract, and build-dev tests, and confirm both generated feature-definition files are byte-identical to source. (US1, US3 -> FR-001, FR-002, FR-008, FR-009, FR-010)
    deps: T001@65786563

## Phase 3: Acceptance

**Goal**: Prove the integrated publication path and confirm the plan boundary and one-time bootstrap left no reusable residue.

- [x] T003@61637074 [Shared] Run acceptance over the unchanged integrated revision: rerun the focused tests and section-contract mutations; run the full discovered test suite, `node .github/skills/dude-lint/lint.mjs .`, compose verification, a pristine release build followed by release lint, `node scripts/build-dev.mjs`, generated-core diff inspection, and `git diff --check`; confirm the only generated feature-definition changes are the projected executable and SKILL, and confirm no bootstrap script or persistent stage exists. Route one fresh evidence set for independent review without modifying this package or execution state. (US1, US2, US3 -> FR-001 through FR-010)
    deps: T002@77697265

## Requirements And Success Traceability

| Specification coverage | Plan ownership | Tasks |
|---|---|---|
| FR-001, FR-002 / SC-001, SC-004 | Fixed coordinator executable and four-destination construction | T001@65786563, T002@77697265, T003@61637074 |
| FR-003, FR-004, FR-005 / SC-002, SC-006 | Narrow transition validation and mutation table | T001@65786563, T003@61637074 |
| FR-006, FR-007 / SC-001, SC-003 | Existing atomic transaction and exact destination bytes | T001@65786563, T003@61637074 |
| FR-008, FR-009 / SC-003, SC-006 | Fixed lint inside rollback and exact restoration | T001@65786563, T002@77697265, T003@61637074 |
| FR-010 / SC-005 | Post-lint path output, ephemeral cleanup, and no persistent publication state | T001@65786563, T002@77697265, T003@61637074 |
