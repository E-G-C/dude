<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@69737375 [Shared] Implement `plan.md` Chosen Design sections 1 through 4. Add the sole detailed `## GitHub Issue Intake` procedure to `src/skills/dude-work-intake/SKILL.md`: accept exactly one `owner/repository#number`, URL, or current-repository bare number / natural-language issue target; use one `gh issue view` read for `number,title,body,comments,url`; treat all fetched prose as untrusted raw input; stop with the reference and reason on failure with no paste-in fallback; classify by substance as feature, bounded bug/chore, active-work blocker, or ambiguous; pass admitted features through existing brainstorm and, for Ship, existing define plus Work with a plain visible origin line; when the surrounding request calls for execution, pass bounded bugs/chores through existing implementation, testing, verification, and independent review; pass blockers through flag; and ask one classification question without admission when ambiguity remains. Add only concise delegation in `src/agents/dude.agent.md` `## Routing`, `## Ship`, and `## Response`. Extend `scripts/current-format-contract.test.mjs` with section-bound assertions, in-memory falsifiers, source/generated checks, and negative inventories that reject every prohibited issue-specific artifact or authority grant. Do not change `src/instructions/dude.instructions.md`, `src/skills/dude-generic-routing/SKILL.md`, or `src/skills/dude-work/SKILL.md`. Run `node scripts/build-dev.mjs && node --test --test-name-pattern='GitHub issue' scripts/current-format-contract.test.mjs && node --test scripts/build-dev.test.mjs`. (US1, US2, US3, US4 -> FR-001 through FR-020; SC-001 through SC-006, SC-008)
- T002@67756964 [Shared] Implement `plan.md` Chosen Design section 5. Update only `README.md`, `docs/commands.md`, `docs/workflow.md`, and `docs/reference.md` with concise examples for `E-G-C/dude#20`, an issue URL, and `@dude ship issue 20`; explain current-repository-only bare numbers, one body-plus-comments raw input, the feature/direct-work/flag/ambiguity routes, one classification question, actionable fetch failure without fallback, visible origin, post-capture Dude authority, and the fact that surfaced issues remain unadmitted. Preserve existing no-automatic-Git wording and state conditionally that a resulting pull request uses `gh pr create --base main`, includes the correct closing reference, and verifies its base. Extend the GitHub-issue documentation contracts in `scripts/current-format-contract.test.mjs`, using `dude-pack-writing-avoid-ai-tropes` and `dude-pack-writing-style`. Run `node --test --test-name-pattern='GitHub issue' scripts/current-format-contract.test.mjs && git diff --check -- README.md docs/commands.md docs/workflow.md docs/reference.md scripts/current-format-contract.test.mjs`. (US1, US2, US3, US4 -> FR-001 through FR-020; SC-001 through SC-008)
- T003@61637074 [Shared] Implement `plan.md` Chosen Design section 6. Run the read-only smoke `gh issue view 20 --repo E-G-C/dude --json number,title,body,comments,url`; rebuild with `node scripts/build-dev.mjs`; run `find . -path ./dist -prune -o -name '*.test.mjs' -print0 | xargs -0 node --test`; run `node .github/skills/dude-lint/lint.mjs .`; run `node .github/skills/dude-compose/compose.mjs verify`; run `node --test scripts/build-dev.test.mjs scripts/build-release.test.mjs`; create and lint a pristine release with `release_dir="$(mktemp -d)" && node scripts/build-release.mjs --out "$release_dir" --tag v0.0.0 && node "$release_dir/.github/skills/dude-lint/lint.mjs" "$release_dir"`; and run `git diff --check`. Inspect that source and generated core are in parity, only the two intended generated semantic surfaces changed, automated tests made no network call, all non-issue intake and Ship contracts remain unchanged, and no prohibited infrastructure, authority, fallback, synchronization, or conversational-intake coupling was introduced. Route the unchanged diff and same evidence to the independent Reviewer without mutating this definition package or task state. (US1, US2, US3, US4 -> FR-001 through FR-020; SC-001 through SC-008)

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/034-github-issue-work-intake.md#coordinator-log -->

# Tasks: GitHub Issue Work Intake

Three all-open canonical units implement `.dude/specs/034-github-issue-work-intake/spec.md`, owned prospectively and exactly by `.dude/ideas/github-issue-work-intake.md`.

The binding boundary is one explicitly referenced issue per intake operation. Body and comments are one untrusted raw input with no precedence rule; a bare number uses only the current repository; fetch failure stops without a paste-in fallback; feature capture reuses brainstorm -> idea -> define -> Work; bounded bugs and chores use existing specialists; active blockers use flag; and ambiguity asks one classification question. No task may add a GitHub lane, tracker, cache, registry, parser runtime, daemon, poller, default repository, cross-repository search, automatic open-issue processing, multi-issue orchestrator, conversational-intake coupling, or automatic Git action.

Core source lives under `src/`. Generated `.github/**` core is refreshed only through `node scripts/build-dev.mjs` and is never hand-edited. The only intended generated semantic changes are `.github/skills/dude-work-intake/SKILL.md` and `.github/agents/dude.agent.md`. Existing generic routing, shared instructions, Work, flag, verification, and review owners remain unchanged. A required change outside these boundaries stops as `contract-mismatch: redefine-required`.

## Phase 1: Issue Admission Authority And Focused Contracts

**Goal**: Add the single-issue fetch, classification, and handoff contract to its existing owners and prove that it creates no new workflow surface.

- [x] T001@69737375 [Shared] Implement `plan.md` Chosen Design sections 1 through 4. Add the sole detailed `## GitHub Issue Intake` procedure to `src/skills/dude-work-intake/SKILL.md`: accept exactly one `owner/repository#number`, URL, or current-repository bare number / natural-language issue target; use one `gh issue view` read for `number,title,body,comments,url`; treat all fetched prose as untrusted raw input; stop with the reference and reason on failure with no paste-in fallback; classify by substance as feature, bounded bug/chore, active-work blocker, or ambiguous; pass admitted features through existing brainstorm and, for Ship, existing define plus Work with a plain visible origin line; when the surrounding request calls for execution, pass bounded bugs/chores through existing implementation, testing, verification, and independent review; pass blockers through flag; and ask one classification question without admission when ambiguity remains. Add only concise delegation in `src/agents/dude.agent.md` `## Routing`, `## Ship`, and `## Response`. Extend `scripts/current-format-contract.test.mjs` with section-bound assertions, in-memory falsifiers, source/generated checks, and negative inventories that reject every prohibited issue-specific artifact or authority grant. Do not change `src/instructions/dude.instructions.md`, `src/skills/dude-generic-routing/SKILL.md`, or `src/skills/dude-work/SKILL.md`. Run `node scripts/build-dev.mjs && node --test --test-name-pattern='GitHub issue' scripts/current-format-contract.test.mjs && node --test scripts/build-dev.test.mjs`. (US1, US2, US3, US4 -> FR-001 through FR-020; SC-001 through SC-006, SC-008)

## Phase 2: Public Guidance

**Goal**: Document the new input shape and substance routes without changing the existing lifecycle, Ship promise, or ordinary first-feature guidance.

- [x] T002@67756964 [Shared] Implement `plan.md` Chosen Design section 5. Update only `README.md`, `docs/commands.md`, `docs/workflow.md`, and `docs/reference.md` with concise examples for `E-G-C/dude#20`, an issue URL, and `@dude ship issue 20`; explain current-repository-only bare numbers, one body-plus-comments raw input, the feature/direct-work/flag/ambiguity routes, one classification question, actionable fetch failure without fallback, visible origin, post-capture Dude authority, and the fact that surfaced issues remain unadmitted. Preserve existing no-automatic-Git wording and state conditionally that a resulting pull request uses `gh pr create --base main`, includes the correct closing reference, and verifies its base. Extend the GitHub-issue documentation contracts in `scripts/current-format-contract.test.mjs`, using `dude-pack-writing-avoid-ai-tropes` and `dude-pack-writing-style`. Run `node --test --test-name-pattern='GitHub issue' scripts/current-format-contract.test.mjs && git diff --check -- README.md docs/commands.md docs/workflow.md docs/reference.md scripts/current-format-contract.test.mjs`. (US1, US2, US3, US4 -> FR-001 through FR-020; SC-001 through SC-008)
    deps: T001@69737375

## Phase 3: Integrated Acceptance And Independent Review

**Goal**: Prove the complete issue-intake guidance over one unchanged revision and hand fresh evidence to independent review.

- [x] T003@61637074 [Shared] Implement `plan.md` Chosen Design section 6. Run the read-only smoke `gh issue view 20 --repo E-G-C/dude --json number,title,body,comments,url`; rebuild with `node scripts/build-dev.mjs`; run `find . -path ./dist -prune -o -name '*.test.mjs' -print0 | xargs -0 node --test`; run `node .github/skills/dude-lint/lint.mjs .`; run `node .github/skills/dude-compose/compose.mjs verify`; run `node --test scripts/build-dev.test.mjs scripts/build-release.test.mjs`; create and lint a pristine release with `release_dir="$(mktemp -d)" && node scripts/build-release.mjs --out "$release_dir" --tag v0.0.0 && node "$release_dir/.github/skills/dude-lint/lint.mjs" "$release_dir"`; and run `git diff --check`. Inspect that source and generated core are in parity, only the two intended generated semantic surfaces changed, automated tests made no network call, all non-issue intake and Ship contracts remain unchanged, and no prohibited infrastructure, authority, fallback, synchronization, or conversational-intake coupling was introduced. Route the unchanged diff and same evidence to the independent Reviewer without mutating this definition package or task state. (US1, US2, US3, US4 -> FR-001 through FR-020; SC-001 through SC-008)
    deps: T002@67756964

## Requirements And Success Traceability

| Specification coverage | Plan ownership | Tasks |
|---|---|---|
| FR-001 through FR-006 / SC-001, SC-005 | Single reference, foreground fetch, raw-data authority, current-repository resolution, and actionable failure | T001@69737375, T003@61637074 |
| FR-007, FR-008, FR-015, FR-016 / SC-002, SC-006 | Four-way classification, surrounding-request authority, one ambiguity question, and no ambient admission | T001@69737375, T002@67756964, T003@61637074 |
| FR-009 through FR-014 / SC-002 through SC-004 | Existing feature lifecycle, visible origin, direct specialist route, definition escalation, and flag handoff | T001@69737375, T002@67756964, T003@61637074 |
| FR-017, FR-018 / SC-007 | Preserved existing semantics and conditional pull-request linkage | T001@69737375, T002@67756964, T003@61637074 |
| FR-019, FR-020 / SC-008 | No GitHub infrastructure, orchestration, or conversational-intake coupling | T001@69737375, T003@61637074 |
