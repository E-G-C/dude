<!-- audit log: .dude/ideas/upgrade-pack-guidance-correction.md#coordinator-log -->

# Tasks: Upgrade Pack Guidance Correction

One all-open canonical unit implements
`.dude/specs/036-upgrade-pack-guidance-correction/spec.md`, owned prospectively
and exactly by
`.dude/ideas/upgrade-pack-guidance-correction.md`.

The binding outcome is documentation-only: both shipped upgrade-guidance
surfaces lead installed-pack updates with `compose refresh <pack>`, neither
offers remove-then-add as an update path or legacy fallback, and remove/add keep
only their ordinary uninstall/install meanings. Core upgrade still preserves
installed packs rather than refreshing them automatically. No task may change
engine behavior, command semantics, pack state, Feature 031, or project memory.

Authoritative core guidance lives under `src/`; generated `.github/**` core is
refreshed only through `node scripts/build-dev.mjs` and is never hand-edited.
`.dude/memory/guardrails.md` is outside implementation scope. A required change
outside these boundaries stops as `contract-mismatch: redefine-required`.

## Phase 1: Correct And Prove Shipped Upgrade Guidance

**Goal**: Correct the complete bounded documentation slice and prove it from one
fresh integrated evidence set.

- [x] T001@75706764 [Shared] Implement `plan.md` Chosen Design sections 1 through 4. In `src/skills/dude-bundle-upgrade/SKILL.md`, replace the installed-pack update claims in `## Ownership Boundary For Generated Agent Profiles` and `## Boundaries` with current-engine guidance that uses `compose refresh <pack>` while preserving the rule that core upgrade does not project or repair installed packs. Rewrite only `docs/upgrading.md` `## Refreshing installed packs` so it leads with `compose refresh <pack>`, retains explicit post-upgrade refresh and concise current-source re-projection guidance, and contains no remove-then-add update recipe or legacy fallback. Preserve remove/add only for ordinary uninstall/install and change no engine or memory file. Extend the smallest existing documentation contract surface, `scripts/current-format-contract.test.mjs`, with one section-bound `installed pack refresh guidance stays current and generated` test that checks both source sections, the public section, ordinary lifecycle meanings, source/generated byte parity, and deletion/stale-recipe falsifiers without adding a parser or test module. Regenerate `.github/skills/dude-bundle-upgrade/SKILL.md` only with `node scripts/build-dev.mjs`. Run the focused contract, `scripts/build-dev.test.mjs`, the recursively discovered suite, workspace lint, compose verification, build/release tests, pristine release build and lint, a second idempotent dev build, generated-path inspection, and `git diff --check` over one unchanged revision; confirm the only implementation paths are the three authoritative/test files plus the one generated skill and that no engine behavior or `.dude/memory/guardrails.md` changed. Route the unchanged diff and same fresh evidence to independent review. (US1, US2 -> FR-001 through FR-007; SC-001 through SC-006)

## Requirements And Success Traceability

| Specification coverage | Plan ownership | Task |
|---|---|---|
| FR-001, FR-003, FR-004 / SC-001, SC-002 | Upgrade-skill correction | T001@75706764 |
| FR-002 through FR-005 / SC-001 through SC-003 | Public upgrading-guide correction | T001@75706764 |
| FR-006, FR-007 / SC-004 through SC-006 | Existing-suite contract, generated projection, and fresh integrated evidence | T001@75706764 |
