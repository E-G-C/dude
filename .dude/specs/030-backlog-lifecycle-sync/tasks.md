<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@6c696665 [US1] Implement `plan.md` Chosen Design sections 1 through 4. Extend `src/skills/dude-engine/lib/feature.mjs` with the exact draft/defined/resolved matrix while returning only valid defined owners. Retain the parser's unnormalized `spec_path` scalar value before path normalization and make backlog valid-resolved classification require exact resolved status, `rawSpecPath === ""`, no owner path, and no item-specific owner or metadata diagnostic; bar every invalid resolved candidate from both resolved and package Completed branches. Keep lint delegated to inventory; make first-definition publication explicitly refuse valid resolved until an explicit brainstorm reopen; and update current definition, intake, coordinator, and Spec Lead guidance for ordinary preservation, explicit reopen to draft, and Ship refusal before reopen. Extend `feature.test.mjs`, `lint.test.mjs`, `publish-first-definition.test.mjs`, `backlog.test.mjs`, and `scripts/current-format-contract.test.mjs` for valid resolved plus nonempty, malformed, owner-claiming, diagnostic-bearing, malformed-frontmatter, and non-exact-status regressions; zero resolved ownership; no-write publication refusal; valid-only Completed classification; package-less detail; no task counts; and no awaiting-definition wording. After focused source tests pass, refresh generated core only through `node scripts/build-dev.mjs`; then use the user-authorized Spec Lead lifecycle refresh to migrate `.dude/ideas/core-dogfood-preview.md` to resolved with an empty path, byte-preserve complete `## Idea`, `## Open Questions`, and `## Assumptions` sections, update only managed definition content, append exactly one event naming Feature 012 as consumer and deliverer, create no package, and procedurally regenerate the backlog pair. (US1 -> FR-001 through FR-007, FR-017 through FR-020; SC-001 through SC-003, SC-009)
- T002@72656672 [Shared] Implement `plan.md` Chosen Design sections 5 through 8. Export synchronous `refreshCommittedBacklog({root})` from `src/skills/dude-lightweight-execution/backlog.mjs`, fully render both outputs before writes, capture present-or-missing preimages, restore both on pair failure, and make existing `generate --write` delegate to it without a generic transaction abstraction. Invoke it only after successful guarded `set --write`, guarded `apply-states --write`, and successful `applyLightweightWorkRequest`; never invoke it for `render --write`, reads, dry runs, or failed/refused mutations. Keep guarded canonical task/snapshot poststate on refresh failure, return exit `2`, emit exactly `[FAIL] canonical state committed; backlog refresh failed\n` to stderr, and emit no success line. At the autonomous boundary, use the validated bound root to attempt refresh only after the existing committed result and receipt return from canonical rollback; catch only refresh failure and return the original exact `ok`/`phase`/`receipt` result with no added field. Do not change `host-adapter.mjs`, `host-adapter-runner.mjs`, recovery audits, receipts, or accepted result schemas. Extend `backlog.test.mjs`, `board.test.mjs`, and one end-to-end `host-adapter.test.mjs` regression for guarded claim/close, batch claim/close, autonomous claim/close, exact fresh Markdown and HTML classification, excluded paths, second-write truncation/failure with pair preimages restored, committed canonical poststate, exact guarded output, exact autonomous result keys and receipt, normal host receipt/commit/audit handling, no refusal/unchanged semantics, and stale-pair detection by existing `backlog.mjs check`. Update the Lightweight skill and minimal current docs for covered hooks, bounded autonomous observability, and procedural log/lifecycle/order refresh limits, then refresh generated core only through build-dev. (US2, US3 -> FR-008 through FR-020; SC-004 through SC-009)
- T003@61636365 [Shared] Run `plan.md` Integrated acceptance over one unchanged integrated revision: focused engine, lint, first-definition, backlog, board, host-adapter, and current-format tests; the full recursively discovered suite; backlog freshness; `node scripts/build-dev.mjs` twice with an idempotent second run and source/generated parity; Dude lint; compose verification; pristine release build and release lint; and `git diff --check`. Recheck valid and invalid resolved-shape regressions and `core-dogfood-preview` resolved with empty path, byte-identical protected sections, exactly one Feature 012 resolution event, no package, exact Completed membership, no task counts, and absence from Ideas awaiting definition. Re-run guarded single, guarded batch, and autonomous claim/close freshness plus injected second-write failure; require restored pair preimages, committed canonical state, guarded exit `2` with exact stderr and no success line, exact three-field autonomous receipt result, normal host receipt/commit/audit handling, no false refusal or rollback, and stale-pair detection by `backlog.mjs check`. Inspect for one pair writer, exactly three hooks, refresh outside canonical rollback, unchanged autonomous schemas and host/runner production files, documented bounded/procedural exceptions, build-dev-only generated changes, and none of the prohibited machinery. Route the unchanged diff and same evidence to an independent reviewer; do not mutate this definition package or task state while collecting evidence. (US1, US2, US3 -> FR-001 through FR-020; SC-001 through SC-010)

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/030-backlog-lifecycle-sync.md#coordinator-log -->

# Tasks: Backlog Lifecycle Sync

Three all-open canonical units implement `.dude/specs/030-backlog-lifecycle-sync/spec.md`, owned prospectively and exactly by `.dude/ideas/backlog-lifecycle-sync.md`.

No task carries `[P]`. Resolved lifecycle support and the authorized package-less migration land first; pair-safe refresh and the three mutation hooks follow; final acceptance uses one unchanged integrated revision.

Core source lives under `src/`; generated `.github/**` core is refreshed only through `node scripts/build-dev.mjs` and is never hand-edited. `.dude/backlog.md` and `.dude/backlog.html` remain derived and non-authoritative. No task may add a watcher, service, server, event bus, generic transaction framework, new state, second board, dashboard redesign, extra resolution metadata, compatibility status, package for `core-dogfood-preview`, automatic hook for every writer, or lane-result/host-product/runner/audit machinery for a derived refresh failure.

The plan's strict valid-resolved predicate, explicit brainstorm reopen, exact guarded stderr, unchanged autonomous result, bounded observability, canonical-versus-derived authority, migration order, pair restoration, covered/excluded hooks, and procedural limitations are normative. A required change outside those boundaries stops as `contract-mismatch: redefine-required`.

## Phase 1: Resolved Lifecycle And Authorized Migration

**Goal**: Represent package-less completion truthfully without changing exact package ownership.

- [x] T001@6c696665 [US1] Implement `plan.md` Chosen Design sections 1 through 4. Extend `src/skills/dude-engine/lib/feature.mjs` with the exact draft/defined/resolved matrix while returning only valid defined owners. Retain the parser's unnormalized `spec_path` scalar value before path normalization and make backlog valid-resolved classification require exact resolved status, `rawSpecPath === ""`, no owner path, and no item-specific owner or metadata diagnostic; bar every invalid resolved candidate from both resolved and package Completed branches. Keep lint delegated to inventory; make first-definition publication explicitly refuse valid resolved until an explicit brainstorm reopen; and update current definition, intake, coordinator, and Spec Lead guidance for ordinary preservation, explicit reopen to draft, and Ship refusal before reopen. Extend `feature.test.mjs`, `lint.test.mjs`, `publish-first-definition.test.mjs`, `backlog.test.mjs`, and `scripts/current-format-contract.test.mjs` for valid resolved plus nonempty, malformed, owner-claiming, diagnostic-bearing, malformed-frontmatter, and non-exact-status regressions; zero resolved ownership; no-write publication refusal; valid-only Completed classification; package-less detail; no task counts; and no awaiting-definition wording. After focused source tests pass, refresh generated core only through `node scripts/build-dev.mjs`; then use the user-authorized Spec Lead lifecycle refresh to migrate `.dude/ideas/core-dogfood-preview.md` to resolved with an empty path, byte-preserve complete `## Idea`, `## Open Questions`, and `## Assumptions` sections, update only managed definition content, append exactly one event naming Feature 012 as consumer and deliverer, create no package, and procedurally regenerate the backlog pair. (US1 -> FR-001 through FR-007, FR-017 through FR-020; SC-001 through SC-003, SC-009)

## Phase 2: Pair-Safe Refresh And Post-Commit Hooks

**Goal**: Make both committed backlog artifacts current after the three proven canonical mutation boundaries without making them authoritative.

- [x] T002@72656672 [Shared] Implement `plan.md` Chosen Design sections 5 through 8. Export synchronous `refreshCommittedBacklog({root})` from `src/skills/dude-lightweight-execution/backlog.mjs`, fully render both outputs before writes, capture present-or-missing preimages, restore both on pair failure, and make existing `generate --write` delegate to it without a generic transaction abstraction. Invoke it only after successful guarded `set --write`, guarded `apply-states --write`, and successful `applyLightweightWorkRequest`; never invoke it for `render --write`, reads, dry runs, or failed/refused mutations. Keep guarded canonical task/snapshot poststate on refresh failure, return exit `2`, emit exactly `[FAIL] canonical state committed; backlog refresh failed\n` to stderr, and emit no success line. At the autonomous boundary, use the validated bound root to attempt refresh only after the existing committed result and receipt return from canonical rollback; catch only refresh failure and return the original exact `ok`/`phase`/`receipt` result with no added field. Do not change `host-adapter.mjs`, `host-adapter-runner.mjs`, recovery audits, receipts, or accepted result schemas. Extend `backlog.test.mjs`, `board.test.mjs`, and one end-to-end `host-adapter.test.mjs` regression for guarded claim/close, batch claim/close, autonomous claim/close, exact fresh Markdown and HTML classification, excluded paths, second-write truncation/failure with pair preimages restored, committed canonical poststate, exact guarded output, exact autonomous result keys and receipt, normal host receipt/commit/audit handling, no refusal/unchanged semantics, and stale-pair detection by existing `backlog.mjs check`. Update the Lightweight skill and minimal current docs for covered hooks, bounded autonomous observability, and procedural log/lifecycle/order refresh limits, then refresh generated core only through build-dev. (US2, US3 -> FR-008 through FR-020; SC-004 through SC-009)
    deps: T001@6c696665

## Phase 3: Integrated Acceptance And Independent Review

**Goal**: Prove lifecycle truth, immediate freshness, pair restoration, authority preservation, generated parity, and bounded scope over one unchanged revision.

- [x] T003@61636365 [Shared] Run `plan.md` Integrated acceptance over one unchanged integrated revision: focused engine, lint, first-definition, backlog, board, host-adapter, and current-format tests; the full recursively discovered suite; backlog freshness; `node scripts/build-dev.mjs` twice with an idempotent second run and source/generated parity; Dude lint; compose verification; pristine release build and release lint; and `git diff --check`. Recheck valid and invalid resolved-shape regressions and `core-dogfood-preview` resolved with empty path, byte-identical protected sections, exactly one Feature 012 resolution event, no package, exact Completed membership, no task counts, and absence from Ideas awaiting definition. Re-run guarded single, guarded batch, and autonomous claim/close freshness plus injected second-write failure; require restored pair preimages, committed canonical state, guarded exit `2` with exact stderr and no success line, exact three-field autonomous receipt result, normal host receipt/commit/audit handling, no false refusal or rollback, and stale-pair detection by `backlog.mjs check`. Inspect for one pair writer, exactly three hooks, refresh outside canonical rollback, unchanged autonomous schemas and host/runner production files, documented bounded/procedural exceptions, build-dev-only generated changes, and none of the prohibited machinery. Route the unchanged diff and same evidence to an independent reviewer; do not mutate this definition package or task state while collecting evidence. (US1, US2, US3 -> FR-001 through FR-020; SC-001 through SC-010)
    deps: T002@72656672

## Requirements And Success Traceability

| Specification coverage | Tasks |
|---|---|
| FR-001 through FR-007 / SC-001 through SC-003 | T001@6c696665, T003@61636365 |
| FR-008 through FR-016 / SC-004 through SC-008 | T002@72656672, T003@61636365 |
| FR-017 through FR-020 / SC-009, SC-010 | T001@6c696665, T002@72656672, T003@61636365 |
