<!-- dude:board:start -->
<!-- generated by @dude — do not hand-edit; regenerated on every status/define refresh -->

### Ready Now
- (none)

### In Progress
- (none)

### Blocked
- (none)

### Done
- T001@59a10f01 [Shared] Build and demonstrate the source-derived Review interaction at `.dude/specs/059-annotation-direct-manipulation/design/review-direct-manipulation.html`, with every required prototype file and asset under that `design/` directory.
- T002@59a10f02 [Shared] Bring the working 059 proposal to explicit exact-revision approval through the existing owner path in `.dude/specs/059-annotation-direct-manipulation/spec.md` and the exact owning idea's Coordinator Log.
- T003@59a10f03 [Shared] Apply the approved selected-target routing and coupled comment/history fix in `src/extensions/dude/ui/review/{engine,geometry}.mjs`, `styles.css`, and `src/extensions/dude/frontend/review.jsx`, with focused regressions in `src/extensions/dude/review.test.mjs` and `scripts/dude-canvas-ui/{browser,t011-browser}.test.mjs`.
- T004@59a10f04 [Shared] Verify the rebuilt Review in the existing required browser/build and installed-host acceptance paths, including `scripts/dude-canvas-ui/t012-installed-host.mjs`, and document the approved interaction in `docs/commands.md`.

<!-- dude:board:end -->

<!-- canonical task units — edit task descriptions here, but let @dude mutate state glyphs -->

<!-- audit log: .dude/ideas/059-annotation-direct-manipulation.md#coordinator-log -->

# Tasks: Annotation Direct Manipulation

Spec: `.dude/specs/059-annotation-direct-manipulation/spec.md`  
Plan: `.dude/specs/059-annotation-direct-manipulation/plan.md`  
Preview: `.dude/specs/059-annotation-direct-manipulation/design/review-direct-manipulation.html`

Four initial open units cover the working proof, its approval, one coupled implementation/regression change, and installed acceptance/documentation. They are serial because approval and verified source behavior are real prerequisites. No generated board or execution history is introduced.

Live UI writes require explicit approval of the exact demonstrated canonical HTML and asset revisions through the existing design path. Neither this task list nor a completed approval-task glyph supplies that approval. Definition publication and coordinator-reported zero lint failures precede any task execution.

## Phase 1: Real-Engine Design Proof

- [x] T001@59a10f01 [Shared] Build and demonstrate the source-derived Review interaction at `.dude/specs/059-annotation-direct-manipulation/design/review-direct-manipulation.html`, with every required prototype file and asset under that `design/` directory.
    Scope: Coder declares attainable real-engine/component capability before the first render; retains existing Fluent visuals; confines exploratory routing, cursor, and manipulation-history changes to design assets; reuses actual Review state/history and real isolated fixture transports where needed.
    Verification: Run the plan's US1/US2 proof and relevant US3 keyboard/Comments checks with native input, including older selection, all five armed tools, handle/border/interior routing, matching cursors, both out-and-back defects, and independent no-op history/save checks. Retain source identity, exact asset revisions, bounded diff, screenshots, and honest fixture-only limitations; preserve variants.
    Boundary: No live `src/` or `.github/` UI edits, invented controller, forged waiter/receipt, or weakened source/sandbox/provider guard. Return `design-gap` if faithful proof cannot be achieved.
    Trace: Plan Phase 1; FR-001–013; SC-001–003/005; VSC-001–003.

## Phase 2: Exact-Revision Approval

- [x] T002@59a10f02 [Shared] Bring the working 059 proposal to explicit exact-revision approval through the existing owner path in `.dude/specs/059-annotation-direct-manipulation/spec.md` and the exact owning idea's Coordinator Log.
    deps: T001@59a10f01
    Scope: The definition/design owner settles the working proposal, preserves variants, and returns the bounded preview/approval request to the coordinator with the canonical HTML, all required asset revisions, demonstrated behavior, and material limitations.
    Verification: Literal explicit approval names the current demonstrated revision; authorized design metadata records `design_status: approved`, the approved direction, and revision history. Revision feedback, fixture output, implementation intent, and any 052/057/062 approval do not qualify. A changed artifact/asset requires fresh approval.
    Boundary: The Spec Lead does not publish or acknowledge the request directly. No live UI source application while approval is missing or stale.
    Trace: Plan Phase 2; FR-013; SC-005; VSC-004.

## Phase 3: Approved Interaction And Focused Regressions

- [x] T003@59a10f03 [Shared] Apply the approved selected-target routing and coupled comment/history fix in `src/extensions/dude/ui/review/{engine,geometry}.mjs`, `styles.css`, and `src/extensions/dude/frontend/review.jsx`, with focused regressions in `src/extensions/dude/review.test.mjs` and `scripts/dude-canvas-ui/{browser,t011-browser}.test.mjs`.
    deps: T002@59a10f02
    Scope: Share hover/down targeting; use existing selected handles and boundary-only drawing-mode hits; retain Select's whole-face/topmost/badge behavior. Track actual manipulation monotonically within the existing drag, invalidate both comment candidates on movement, and keep final equality as the no-op-history decision. Update existing accessible/user guidance only; retain the current Comments/caret, toolbar, save, projection, and capture paths.
    Verification: Prove all US1/US2 scenarios and focused US3 regressions, including no unselected grabs, interior drawing, first/second out-and-back moves and resize, unchanged-history/redo, committed-only preview saves, Enter/caret, and visibility/anchor boundaries. Adapt the near-corner overlapping-badge fixture without weakening its Select oracle. Rebuild the UI and project generated artifacts through the current build scripts; verify current-byte pins and compare with the approved proof.
    Boundary: Recheck current exact-revision design approval before the first live UI write. No new controller, persistent fields, recency state, handles, public contract, or 062 shell work; do not hand-edit generated output.
    Trace: Plan Phase 3 and Verification Matrix; FR-001–012; SC-001–004; VSC-001–004.

## Phase 4: Installed Acceptance And Documentation

- [x] T004@59a10f04 [Shared] Verify the rebuilt Review in the existing required browser/build and installed-host acceptance paths, including `scripts/dude-canvas-ui/t012-installed-host.mjs`, and document the approved interaction in `docs/commands.md`.
    deps: T003@59a10f03
    Scope: Use the existing suites and real provider/adapter fixtures; extend only bounded installed Review checks/current-source pins if needed. Verify shipped engine, frontend, static/legal parity and the paired saved/sealed evidence. Update the Canvas drawing-versus-Select instructions without changing other feature scope.
    Verification: Run the named maintainer set serially in required-browser mode and the explicitly invoked installed driver; report actual executed/failed/skipped counts, exact loaded artifact identities, and retained visual/keyboard/save/anchor/report/PNG evidence. Complete automatable interaction, geometry, accessibility, contrast, and error checks before requesting only the remaining embedded-host observation. Route independent acceptance and any remaining limits to the coordinator.
    Boundary: Fixture and installed CLI proof is not native desktop proof, live user work, or approval. A mismatch returns through the existing implementation/design path; this task grants no task closure, Git action, or release publication.
    Trace: Plan Phase 4 and Verification Matrix; FR-007–013; SC-001–005; VSC-001–004.
