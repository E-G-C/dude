---
title: Unattended Work Continuity
slug: unattended-work-continuity
status: defined
spec_path: .dude/specs/013-unattended-work-continuity/spec.md
---

# Idea: Unattended Work Continuity

## Idea

The idea is to avoid these stops and avoid stopping to continue working on a loop. I already lost half a day as I left you unsupervised and you stopped for no reason.

While it was happening I kept asking the same thing: "Were you blocked? Why do you stop? Go for it" and "why did you stop? what do I need to do so you respect `--max unlimited`".

The concrete run was `@dude work autonomous-learning-governance --max unlimited --recover-on-block --recovery-cycles unlimited --policy autonomous`. `dude-work` already defines a closed list of nine named stop reasons. The coordinator halted three times: after closing T004, after closing T005, and after two failed review cycles on T005. Only the third halt named a reason from that list (`two failed attempts on T005@7065726d`). The first two named nothing — the coordinator wrote a progress summary and ended the turn, treating "this is worth reporting" as if it meant "this is a stop." Nothing forces a halt to name one of the nine reasons, so an unnameable halt looks exactly like a legitimate one in the transcript.

There is a second pattern I do not want to treat as settled. Across this session I was asked five separate times to authorize a bounded revision past a hard stop, and I authorized every one. If the answer is always yes, being asked each time is what costs me the day.

One precedent already in the bundle seems relevant: `flag` has to echo `Classified as: <type>` from a closed set. Stops have a closed set but no equivalent echo requirement.

I do not know yet whether this is a new capability or just a tightening of the existing `dude-work` stop discipline.

## Open Questions

1. Which of the nine named stops must stay absolute under any policy, and which should become continuable when a run is explicitly unattended?
   Answer:
2. Should the `two failed attempts` hard stop auto-authorize a bounded number of further revisions under an explicit unattended policy, given that every such request this session was authorized? If so, what bounds it — an attempt count, a budget, or a class of finding?
   Answer:
3. Should reporting be decoupled from stopping, so progress is surfaced inline while the loop keeps running?
   Answer:
4. What is the safety floor that must never be crossed without a human, whatever the policy? The existing invocation-wide categories are destructive operations, spending, credentials, external authorization, and ownership ambiguity.
   Answer:
5. Should every halt be required to echo a named reason, so an unnameable halt becomes structurally impossible rather than merely discouraged?
   Answer:
6. Is this a new capability, or a tightening of `dude-work` stop discipline inside what Feature 005 already settled?
   Answer:
7. Beyond naming a reason from the closed set, what must a halt identify about its specific cause so the owner of an unattended run can act on it without reading the runtime's internals?
   Answer:

## Assumptions

No additional assumptions have been provided.

<!-- dude:managed:start -->
## Normalized Intent

- The outcome wanted is an unattended run that keeps working, not one that idles on a halt with no legitimate basis.
- The observed defect is discretionary halting: two of three halts in the run named no reason from the closed nine-reason list, and the third (`two failed attempts on T005@7065726d`) was legitimate.
- Reporting progress and stopping work were conflated; the coordinator ended turns to summarize rather than because a stop condition applied.
- Because no rule forces a halt to name its reason, an illegitimate halt is indistinguishable from a legitimate one after the fact.
- The repeated hard-stop authorization pattern (five requests, five approvals) is raised as an open design question, not as a settled requirement.
- Whether this is a new capability or a tightening of existing `dude-work` stop discipline is deliberately left open.
- A named halt is not automatically an actionable halt. The 2026-07-28 run below did name a legitimate reason from the closed set and was still effectively undiagnosable: locating the cause took reading four internal functions and bisecting a temp copy. The wanted outcome extends question 5 rather than replacing it — a halt should name its reason *and* identify what specifically made it stop, so the owner of an unattended run can act without reading the runtime's internals. Naming remains necessary; on this evidence it is not sufficient.
- Unattended continuity must survive its own audit trail. An append-only record the system writes must never permanently disable the policy that wrote it. A run that records a governance incident and thereby loses the ability to run unattended on that feature has defeated the outcome this idea exists to produce.

## Dogfooding Evidence

### 2026-07-28 — autonomous run hard-stopped before claiming any task (feature 007)

Invocation: `@dude work --max unlimited --recover-on-block --recovery-cycles unlimited --policy autonomous` on feature `007-technical-docs-pack-remediation`.

Outcome: a hard stop *before* any task was claimed. Reason `evidence-incomplete`, blocker subject `occurrence-retention`. `classifyOutcomeReason('evidence-incomplete')` returns `hard-stop` and `mayContinueAutonomously` returns false. No task state, board, snapshot, or definition byte changed.

Root cause — a write/read round-trip defect in core `src/skills/dude-work/recovery.mjs` (mirrored to `.github/`): the runtime writes a lane-history line of the form `- dude-run-event: {...,"type":"incident-supersession",...}` into a feature's `## Lightweight Execution History`, and `validateEventCommitmentV1` accepts that kind, but the reader constant `V2_EVENT_TYPES` omits it. `isV2AuthoritativeEventRecord` therefore returns false, `t002EventCandidate` returns null, and `parseV2EventLines` throws `contains an unknown prefixed event record`. That surfaces through `dualRetainedOccurrenceEventsV2` as `evidence-incomplete`.

Causation proven on a temp copy: with the event line present, identical autonomous authorization returns `evidence-incomplete`; with the line removed, it returns `authorized`. Guarded policy is unaffected.

Severity worth carrying into definition: lane history is append-only audit evidence. Once an `incident-supersession` event is written to a feature, autonomous mode is permanently unusable on that feature, because the only way to satisfy the reader is to delete audit evidence — exactly what must not happen. The event is written by the incident-correction path, so the machinery that records a governance incident is what disables the governance mode. Feature 009's own `T009@696e6369` materialization wrote the one now sitting on feature 007.

What this evidence establishes for this idea:

- Diagnosability: the closed-set naming rule contemplated by question 5 held here, and the halt was still opaque without source-level investigation.
- Continuity: a record the system itself wrote permanently disabled the policy that wrote it.

## Relationship To Existing Work

- Feature 005 (`autonomous-work-modes`, complete) settled *which* recoverable checkpoints an autonomous policy may pass, and shipped the policy selector, the hard-stop taxonomy, and sequential continuation after a stop. The gap here is different: the coordinator halted without invoking any checkpoint at all. That reads as a discipline and observability gap in `dude-work`, not a change to 005's policy semantics.
- Feature 009 (`autonomous-learning-governance`) governs the opposite failure mode — work that repeats without progress — and deliberately *adds* stop conditions (`learning-required`, `learning-governance-conflict`, scoped halts, budget exhaustion, Controlled Unresolved End). Any "closed set of named stops" rule this idea produces would have to account for those additions, so the set is a moving target until 009 closes.
- Current assessment: a new, small feature rather than a refinement of 005 — sequenced after Feature 009 closes. Question 5 (named-reason echo) is largely a discipline rule and the cheaper half; question 2 (auto-authorized revisions past `two failed attempts`) lands directly on the runtime Feature 009 changed.
- Feature 009's package is `.dude/specs/009-autonomous-learning-governance/`. Observed on 2026-07-28 by reading `.dude/specs/009-autonomous-learning-governance/tasks.md` directly: all nine canonical tasks, `T001@7365616c` through `T009@696e6369`, carry `[x]`, and the generated board lists all nine under Done with Ready Now, In Progress, and Blocked empty. This supersedes the 2026-07-25 note here, which recorded 8 of 9 closed with `T009@696e6369` `[!]` blocked on a typed `external-dependency` awaiting Feature 008's core dogfood promotion. Only task states in that one file were observed; package closure itself was not verified. Its `spec.md` remains immutable through implementation. This is still a point-in-time observation, so re-check it at definition time rather than trusting it as current. It is a sequencing note only; this idea proposes no change to Feature 009's package.
- The external "struggle indicators / stuck-detection" pattern captured below is the complementary half to Feature 009 (`autonomous-learning-governance`), which governs work that repeats without progress: the same liveness/progress signal that decides continue-versus-stop is what would replace discretionary, human-style halts here — a design observation only, implying no change to Feature 009's package.
- For whoever defines this idea: 009 is related because both features govern the same continue-versus-stop decision from opposite directions. This idea targets halts that happen without a legitimate named reason; 009 targets work that continues without progress. Read 009's shipped stop conditions first, and treat its closed set of named stops as the baseline to extend rather than contradict. The two have to stay consistent: any rule requiring every halt to name a reason from a closed set must accommodate the stop conditions 009 adds.
- Scope boundary drawn by the coordinator on 2026-07-28: the writer/reader vocabulary mismatch recorded under Dogfooding Evidence is a `src/**` correctness bug in Feature 009's shipped runtime, and it is blocking autonomous mode now. This idea is sequenced after Feature 009 closes and still has unanswered open questions. The coordinator's assessment is that the vocabulary and round-trip *fix* should be separable from this idea and may warrant its own small feature. This idea carries the diagnosability and continuity requirements and keeps the 2026-07-28 run as evidence; it does not own the code fix, and nothing here schedules or specifies it.
- Candidate requirement the coordinator flagged as valuable regardless of where that fix lands: a round-trip invariant asserting that every event type the writer can emit is accepted by the reader. Recorded as a candidate outcome only — brainstorm selects no mechanic, test shape, or enforcement point.

## External Precedents

Sources: `snarktank/ralph` (https://github.com/snarktank/ralph), `Th0rgal/open-ralph-wiggum` (https://github.com/Th0rgal/open-ralph-wiggum).

These patterns come from researching two implementations of Geoffrey Huntley's "Ralph" agent-loop technique: `snarktank/ralph` (a bash loop that re-spawns a fresh AI CLI each iteration from the same prompt, so cross-iteration memory lives only in git history plus `progress.txt` and `prd.json`) and `Th0rgal/open-ralph-wiggum` (a CLI wrapping several agent CLIs that adds live status, mid-loop hint injection, a tasks mode, and persisted attempt/history state). Each entry is a candidate design input for the open questions, not an answer to them.

- Inverted default (continue is the default, stopping is the exception). In both tools the loop keeps running by default and the only way to end early is an explicit, structurally-detected terminal signal (a promise tag emitted as the final line); ending a turn or writing a progress note never stops the loop. This bears on the core defect (turns ended to summarize were read as stops) and on Q5: an unnameable halt becomes structurally impossible, because a halt that emits no recognized signal simply is not a halt.
- Closed, named terminal vocabulary. `open-ralph-wiggum` exposes two distinct explicit signals: a completion signal and a separate early-abort signal for legitimate early exits (for example, a precondition failed). This mirrors the closed nine-reason stop set plus the existing `flag` / `Classified as:` echo precedent (Q1, Q5): a legitimate early stop must be one of a closed, named set and emitted explicitly.
- Reporting decoupled from stopping. `snarktank/ralph` appends learnings to `progress.txt` every iteration without stopping; `open-ralph-wiggum` surfaces a live status view from a second terminal and lets a human inject guidance mid-loop without halting. This is a direct precedent for Q3: surface progress inline while the loop keeps running.
- Stuck-detection instead of discretionary halting. `open-ralph-wiggum` shows "struggle indicators" (for example, no file changes across several iterations, or repeated errors) that warn a run is stuck without stopping it, plus a ledger fallback that can advance even when the agent forgets to emit the completion signal. This complements Q3/Q5 and connects to the repeats-without-progress problem covered under Relationship To Existing Work.
- Explicit unattended-policy toggles that change stop behavior. Minimum-iteration gates prevent premature completion (do not quit too early), a "do not pause to ask, keep looping" mode turns interactive questions into continued work, and persisted attempt counts bound retries. These are candidate inputs for Q2 (auto-authorizing a bounded number of revisions past a hard stop, bounded by an attempt count) and Q1 (which stops become continuable under an explicit unattended policy).
- Safety net plus durable continuity state. Both keep a runaway iteration cap and persist cross-iteration state in files, so continuity survives restarts. This supports the wanted outcome of an unattended run that keeps working, bounded.

Cautions (do not import): Ralph achieves unattended runs by auto-approving everything (blanket "allow all" / "skip permissions" / "full auto" / "yolo" / "no questions") and has no independent review step ("commit if checks pass"). That blanket permission bypass is exactly the boundary Q4's safety floor must not cross, and the missing review is what this idea's constraints forbid weakening. The transferable part is the discipline — default-continue, explicit named stop, reporting decoupled from stopping, structural stuck-detection — not the permissiveness.

## Constraints

- Brainstorm intake only; do not create a definition package or begin implementation.
- Do not modify Feature 009's package, task state, board, or log, and do not treat any note here as a change to it.
- Do not select syntax, flag spellings, policy names, state shapes, or enforcement mechanics during brainstorm.
- Do not weaken destructive, spending, credential, external-authorization, or ownership-ambiguity hard stops.
- Do not weaken fresh verification or independent review; a continued loop is not an approved loop.
- Keep the scope proportionate — no new lane, ledger, or command is assumed.

## Definition Disposition

Recorded by @dude-spec-lead at first definition (`define unattended-work-continuity` → `.dude/specs/013-unattended-work-continuity/spec.md`). The `## Open Questions` answers stay user-controlled and unchanged; the following are conscious definition-time assumptions carried into the spec's `## Assumptions` and `## Clarifications` equivalents, inventing no scope beyond the normalized intent, constraints, and relationship notes.

- Q1, Q6 — Assumed: reclassify no stop; this is a discipline and observability tightening of `dude-work`, not a change to Feature 005 policy semantics. A new, small feature sequenced after Feature 009 (observed 2026-07-28 with all nine canonical tasks closed; point-in-time, package closure not independently verified).
- Q2 — Deferred (out of scope): auto-authorizing bounded revisions past `two failed attempts` would relax an existing Feature 005 / Feature 009 hard stop and lands on the runtime Feature 009 changed; the idea itself raised it as unsettled. Not decided here.
- Q3 — Assumed yes: reporting is decoupled from stopping; a progress report is never a stop.
- Q4 — Assumed: safety floor is exactly the existing invocation-wide categories (destructive operations, spending, credentials, external authorization, ownership ambiguity) plus mandatory verification and independent review; none weakened.
- Q5 — Assumed yes: every halt echoes a named reason from the existing closed stop set, making an unnameable halt structurally impossible.
- Q7 — Assumed: beyond naming a reason, a halt must identify the affected target, the specific subject or condition that caused it, and the next owner action, sufficient to act without reading the runtime's internals.
- Continuity / round-trip invariant — Handed off, not re-owned: the audit-trail continuity outcome and the round-trip invariant (every writer-emittable event type is reader-acceptable) are already owned by defined Feature 010 (`.dude/specs/010-core-autonomous-event-round-trip/spec.md`, User Stories 1 and 3). Feature 013 adds no overlapping guard.

## Re-definition Disposition

Recorded by @dude-spec-lead on re-definition (`define unattended-work-continuity` → `.dude/specs/013-unattended-work-continuity/spec.md`, sole defined owner, zero resolver diagnostics), after the chartered `contract-mismatch: redefine-required` flag on T005. User-controlled `## Idea`, `## Open Questions` (answers still blank), and `## Assumptions` remain byte-unchanged; `status: defined`, the exact `spec_path:`, and the entire append-only `## Coordinator Log` are preserved. This subsection records the current re-definition and reflects the runner-locus simplification; the earlier `## Definition Disposition` above is preserved unchanged.

- Amendment (resolved-or-explicitly-unresolved): the false clause "the loop ends only on a closed-set reason and every halt echoes a named reason" is replaced. Every unattended halt now carries a report that either resolves to exactly one closed-set reason with actionable detail, or is explicitly unresolved — naming the missing fields and presenting no reason outside the closed set. This lands on FR-001, FR-003, FR-006, and SC-002, extends FR-006's fail-closed license from a missing cause detail to a missing reason, and introduces no new stop reason. FR-004 is clarified so an out-of-set disposition or diagnostic is never presented as a closed-set reason. FR-011 and SC-008 are new and behavior-level: every unattended work-loop halt carries the report; a single-correction incident keeps its one-correction/fresh-inspection route and is not a halt; a learning-governance settlement keeps its established procedure; the safety floor is unchanged. US5, FR-007, and SC-005 read "with its existing named reason where attribution is established," so a safety halt that cannot bind its blocker reports honestly-unresolved while still halting and requesting human input.
- Enforcement locus (plan-only, kept out of the technology-agnostic spec): the single deterministic chokepoint `finish(row)` in the autonomous runner `src/skills/dude-work/host-adapter-runner.mjs`, attaching a bounded `haltReport` (hard-stop → `describeUnattendedHalt(...)`, ended → `null`). The runner is the only production driver and the model submits no operations, so continuation is decided deterministically — resolving the prior wired-versus-unwired / FR-010 dilemma. Eliminated versus the earlier framing: a session gate, a per-operation gate, checkpoint/handoff/resume threading, the lane permit/apply/receipt disposition chain, and any "across subsequent interactions" threading. Guarded and non-unattended behavior is untouched by construction (autonomous-only runner). The landed T001/T002 primitives (`endsUnattendedLoop`, `describeUnattendedHalt`; `recovery.mjs` +190) are reused unchanged, and the purity guard at `recovery.test.mjs:26817` stays honored because the wiring lives in `host-adapter-runner.mjs` (in-`recovery.mjs` reference count stays 1).
- Five conscious rulings encoded in spec/plan: (1) the no-progress lane disposition is out of scope — the no-progress branch drives a terminal `controlled-end`, and the lane chain is an unreachable, un-coverable guard; (2) the lane-ledger apply→commit crash window is a pre-existing, unowned durability gap recorded as backlog, not a requirement; (3) `ended` outcomes (task-settled, controlled-end, cancelled) are clean settlements carrying no report — I consciously rejected forcing a no-progress controlled-end to emit a named halt, which would reclassify a clean governance settlement; (4) some real safety halts report unresolved-with-named-missing-fields honestly, resolving fully only when the re-derived Inspection binds the matching blocker's evidence hash, and this is honest rather than a defect; (5) a future driver streaming adapter operations with a live model would need adapter-level enforcement, not built now (smallest design).
- Reconciliation staged for the coordinator (I apply no glyphs, board, task metadata, or execution-reconciliation events): kept 1 — T003@73616665 [x] one-to-one, byte-identical, deps T002 unchanged; changed 4 — T001@6b656570 [ ], T002@6e616d65 [ ], T004@646f6373 [ ] (deps T003 → T006), T005@72657677 [ ]; new 1 — T006@77697265 [ ] (wire the reporter into `finish(row)`; deps T002, T003); dropped 0. The plan carries zero active ObjectiveRegistry regions.
- Continuity and the writer/reader round-trip invariant remain owned by defined Feature 010 (`.dude/specs/010-core-autonomous-event-round-trip/spec.md`, User Stories 1 and 3); Feature 013 re-owns neither and adds no overlapping guard. Feature 005, 009, and 010 packages are untouched. Generated board fence left for coordinator regeneration; coordinator lint pending, no readiness claimed.

## Definition Checklist

- [x] Outcome is clear enough for brainstorm
- [x] Scope is coherent as one idea ledger
- [x] Open questions are resolved or consciously assumed

## Coordinator Log

- 2026-07-25 UTC - brainstorm captured
- 2026-07-25 UTC - brainstorm refreshed with external research: snarktank/ralph and Th0rgal/open-ralph-wiggum (Ralph loop technique); added External Precedents.
- 2026-07-25 UTC - recorded external source repository URLs in External Precedents for future reference.
- 2026-07-25 UTC - refreshed the Feature 009 relationship note with its package path and current blocked status.
- 2026-07-28 UTC - brainstorm refreshed: added Dogfooding Evidence for the 2026-07-28 autonomous hard-stop on feature 007, derived diagnosability and audit-trail-continuity outcomes into Normalized Intent, recorded the coordinator's scope boundary excluding the underlying `src/**` fix plus the round-trip invariant candidate, re-observed Feature 009's tasks as all closed, and appended one unanswered open question.
- 2026-07-30 UTC - defined as Feature 013; spec_path `.dude/specs/013-unattended-work-continuity/spec.md`. Open questions resolved as conscious assumptions (see Definition Disposition): Q1/Q6 discipline-and-observability tightening reclassifying no stop; Q3 decoupled reporting; Q4 unchanged safety floor; Q5 mandatory named-reason echo; Q7 actionable halt detail; Q2 deferred as out of scope (would relax an existing hard stop). Continuity and the round-trip invariant handed to defined Feature 010 rather than re-owned. Staged spec/plan/tasks (five all-open canonical units); plan carries zero active ObjectiveRegistry regions.
- 2026-07-30 UTC - re-defined (unchanged-intent refresh): user-controlled `## Idea`, `## Open Questions` (answers still blank), and `## Assumptions` are byte-unchanged; `status: defined` and exact `spec_path:` preserved. Reconciliation re-verified against unchanged intent — all five canonical tasks kept one-to-one with durable keys preserved and states left `[ ]` (kept T001@6b656570; T002@6e616d65, T003@73616665, T004@646f6373, T005@72657677 kept with a non-semantic auditability refinement); zero dropped, zero new. Normalized `tasks.md` traceability breadcrumbs by adding explicit `(US… → FR…)` trace suffixes to T002–T005 to match T001, changing no scope, key, or state. spec.md and plan.md unchanged (plan still carries zero active ObjectiveRegistry regions). Feature 010's opposite-ownership prose is in its own package and untouched; Feature 013's handoff wording verified accurate against Feature 010 US1/US3 and left as-is. Generated board fence left for coordinator regeneration (dependency-gated T002–T005).
- 2026-07-30 UTC - coordinator regenerated the derived board fence in `.dude/specs/013-unattended-work-continuity/tasks.md` deterministically via `dude-lightweight-execution/board.mjs render`, refreshing the stale T001@6b656570 board entry to match its canonical unit. Verified against the renderer contract that dependency-gated open tasks (T002@6e616d65 through T005@72657677) correctly appear in no bucket: `Ready Now` lists only dependency-satisfied open tasks and `Blocked` is reserved for `[!]`. No task glyph, task metadata, or execution state changed; the lane remains Definition Only with all five tasks `[ ]`.
- 2026-08-05 UTC - `@dude ship unattended-work-continuity`: intake resolved the existing defined package (sole owner, zero resolver diagnostics) and handed it to Work with the autonomous Ship policy `{overall:unlimited, recovery:unlimited, recover:true, untilBlocked:false, mode:autonomous}`. Work detected lane = Lightweight Execution (no Beads import); baseline green (recovery.test.mjs 441 pass, dude-lint 0/0). Claimed T001@6b656570 `[~]` in-progress and rendered the derived board; dispatching the Coder to implement the Phase 1 continuity discipline.
- 2026-08-05 UTC - T001@6b656570 closed `[x]` (Lightweight). Coder implemented the loop-level `endsUnattendedLoop(outcome)` predicate in `recovery.mjs` (reuses the frozen `OUTCOME_REASON_CLASSES` closed stop set; autonomous keeps working on no-reason/progress/authorized/completed, halts on any closed-set stop, fail-closed on malformed input; `mayContinueAutonomously`/`classifyOutcomeReason` and default/`guarded` paths byte-unchanged), added six FR-001/FR-002/SC-006/SC-007/fail-closed tests to `recovery.test.mjs`, and documented the discipline in `SKILL.md` `## Stops` as the single detailed owner. Tester verified fresh: `node --test recovery.test.mjs` 447 pass / 0 fail. First Code Reviewer returned a REJECT whose blocking "tests missing" finding was adjudicated false (it misread the pre-existing Feature 005 `T005a–h` and Feature 009 `T005` sections); the real defect it surfaced — the new tests mislabeled `T005 A–F`, colliding with those sections — was accepted and fixed by the Tester (renamed to `Feature 013 T001 A–F`, titles-only, re-verified 447 pass / 0 fail). Independent Code Reviewer re-review: APPROVE. Coordinator applied the `[x]` glyph and regenerated the derived board. No new stop reason, lane, board, command, or store; definition artifacts and unrelated features preserved.- 2026-08-05 UTC - T002@6e616d65 closed `[x]` (Lightweight). Coder added the deterministic named + actionable halt reporter to `src/skills/dude-work/recovery.mjs` as one purely additive 190-line hunk (sole new export `describeUnattendedHalt(outcome, inspection)`, plus module-private `HALT_NEXT_ACTIONS`, `HALT_STATE_CONDITIONS`, `learningGovernanceCondition`, `unattendedHaltSubject`), gated on T001's `endsUnattendedLoop` so the halt predicate keeps one owner. Three return shapes: `null` (not a halt), `{halted:true, resolved:false, unresolved}` (fail-closed, carrying no reason/target/subject), and the resolved descriptor (`reason`, `stopClass`, `target`, `subject`, `nextAction`, `evidenceHash`). Subject derivation runs Inspection-rederived blockers first and requires `code === reason`, `evidenceHash` binding, and `subject !== reason`; `OUTCOME_REASON_CLASSES` and `BLOCKER_CODES` are byte-identical to HEAD, so no new stop reason, lane, board, command, or store exists. Seven `Feature 013 T002 A–G` tests added; `SKILL.md` `## Stops` extended by one paragraph as the single detailed owner. Tester verified fresh and empirically: baseline 447/447 re-measured by reverting the three files to HEAD, post-change `node --test recovery.test.mjs` 454 pass / 0 fail (+7), diff 190 added / 0 deleted; three mutants killed (fail-closed return deleted, off-by-one partial leak, reason-echo guard removed) and the fourth proven an equivalent mutant by a 1656-row byte-identical differential, resolving the Coder's concern (c) as unkillable rather than uncovered. Independent Code Reviewer: APPROVE, no blocking findings, purely-additive property corroborated by a constant +249 line offset against the not-yet-rebuilt `.github/` copy. Coordinator applied the `[x]` glyph and regenerated the derived board. Deferred non-blocking findings carried forward, not lost: T005 must prove the `## Stops` discipline is exercised end-to-end because `describeUnattendedHalt` still has no production call site (same posture as T001's predicate and the settled Feature 005 exports); T003 must consciously decide whether the six always-fail-closed reasons (`no-progress`, `prior-no-progress`, `evidence-drift`, `invalid-mode`, `no-action`, `invalid-action`) satisfy SC-003, and whether an "unattended" reporter describing a guarded outcome needs a policy confinement; backlog holds the target-unbound `learningGovernanceCondition` probe and a pre-existing `[object Object]` splice at `recovery.test.mjs:654`. The `scripts/current-format-contract.test.mjs` 2 failures were independently confirmed pre-existing (identical with T002 reverted) and stay owned by T004's `build-dev` rebuild.
- 2026-08-05 UTC - T003@73616665 closed `[x]` (Lightweight). Tester authored 852 lines of safety-preservation regression coverage in `src/skills/dude-work/recovery.test.mjs` (`Feature 013 T003 A–H`) and made zero changes to `recovery.mjs` (sha256 `86836eec…` unchanged before and after, diff still +190 from T001/T002 alone). Coverage: all five spec safety-floor categories driven as real autonomous halts through the genuine `authorizeAttempt` → `collectEvidence` → `buildInspection` path, each asserting its named closed-set reason, `hard-stop` class, `mayContinueAutonomously === false`, and `nextAction === 'request-human-input'`; all eight settled hard stops given a real fixture; FR-008 proved sharply, including that resuming after a halt authorizes new work without rewriting the halt ("a continued loop is not an approved loop"); and FR-007 guarded by a six-clause total enumeration over `OUTCOME_REASON_CLASSES` compared against hand-written literals. Derived and pinned the many-to-one safety mapping: destructive operations and spending produce not only the same reason but the identical subject `current-run:approval-required`, so the runtime keeps no category distinction — which is what FR-004 requires. Tester ran six mutations, all caught. Independent Code Reviewer: APPROVE, no blocking findings; it verified the independence of `EXPECTED_OUTCOME_CATEGORIES` (so the class check is not a tautology), confirmed the mapping against `inspectionBlockers`, and confirmed both deferred rulings directly in the runtime — NB-2 accept-and-pin is correct because `no-progress`/`prior-no-progress` are structurally unreachable under autonomous (both sites return `learning-required` first), leaving only `evidence-drift` routinely opaque; NB-4 is a loose contract, not an SC-006 violation, since the function is uncalled, authority-free, and side-effect-free. The reviewer had no shell and explicitly declined to claim the criterion-4 mutation as performed; the coordinator therefore closed that execution gap with an independent run, which caught the injected `ambiguous-state` continuability with the exact message "these settled stop reasons no longer halt under the unattended policy: ambiguous-state, authorized — FR-007 forbids making any stop continuable" (T003 D, plus A and B), and restored `recovery.mjs` to its pristine hash. `node --test recovery.test.mjs` 462 pass / 0 fail (+8 over 454). Deferred to T005 acceptance, not resolved here: (1) an escalated SC-002 seam — the runtime can emit refusal reasons the closed set does not name (`interrupted`, `no-change`, `blocked`, `failed`, `pending-not-found`, `action-mismatch`, `occurrence-retention-conflict`, `learning-governance-conflict`), and such a halt echoes no closed-set reason, satisfying SC-002 only in the fail-closed sense; both specialists agree FR-006/FR-007/SC-005 still hold and T003 H pins the behavior, and the reviewer recommends reconciling the criterion rather than the code, but the coordinator has ruled nothing yet and T005 must not assert SC-002 as met without an explicit decision; (2) T005 cannot demonstrate the `## Stops` discipline through `describeUnattendedHalt`, because T003 G structurally asserts it has no call site. Backlog: `evidence-drift` subject observability, and a pre-existing stray `[object Object]` splice at `recovery.test.mjs:654` that predates this feature.
- 2026-08-05 UTC - T004@646f6373 closed `[x]` (Lightweight). Coder documented the unattended discipline once per surface — one paragraph each in `docs/commands.md`, `docs/workflow.md`, and `docs/reference.md`, deliberately worded distinctly so no two docs share phrasing — and added a 229-line, four-test T004 block to `scripts/current-format-contract.test.mjs` pinning single-detailed-ownership (`src/skills/dude-work/SKILL.md` `## Stops`), terse pointers elsewhere, a 480-byte pointer ceiling, and the absence of any new stop reason, command, flag, or concurrency. Regenerated core with `node scripts/build-dev.mjs`, never hand-editing `.github/`; `git status --porcelain -- .github` shows exactly the two intended files (`skills/dude-work/SKILL.md`, `skills/dude-work/recovery.mjs`). This also closed the two `current-format-contract` failures carried forward from T002, confirming them as the stale-bundle artifacts they were diagnosed to be. Tester verified fresh with PASS: it reconstructed the pre-build state from scratch to reproduce both failures and confirm `build-dev` alone closes them, proved build integrity by destroying and rebuilding `.github` to a byte-identical tree (55/55 core files matching `src/`), and ran a nine-mutation battery with zero survivors; it also self-reported and discarded a false pass caused by Node ESM resolving symlinks to realpath, re-running against real file copies. Independent Code Reviewer: APPROVE (conditional on execution it could not perform, having no shell). Both gates independently raised the same defect, which the coordinator ruled must be fixed before close rather than deferred: `docs/reference.md` credited "the runtime" with the fail-closed decision, but Feature 013 ships that discipline as a prompt-level contract in `## Stops` plus deterministic primitives nothing invokes, making the present-tense mechanistic attribution false. Coder revised — the clause is now agentless ("where any of that detail cannot be established from evidence, the halt is reported unresolved instead"), the antecedent-less "listed" now links to the enumerated stop set at `docs/commands.md#dude-work`, and the duplicated `CONTINUITY_RUNTIME` declaration guard was deleted as redundant against the strictly stronger `Feature 013 T003 G` assertion in its owning test file. Both regexes in `CONTINUITY_DOC_REQUIREMENTS` moved in lockstep and were mutation-proved to pin each fix independently (three mutations, zero survivors). Coder correctly declined to touch `docs/commands.md` and `docs/workflow.md`, whose paragraphs are agentless passive or resolve to the report artifact. Independent re-review: APPROVE, no blocking findings. Coordinator ran every executable gate itself, because reviewers in this environment repeatedly had no shell: `node --test` on the three script suites 117 pass / 0 fail; `dude-lint` 0 warnings / 0 failures; `build-dev` idempotent; `git diff --check` clean; repetition checker showing the same six pre-existing findings and none introduced; and — settling the reviewer's own stated scope-containment gap as proof rather than inference — pristine sha256 for both `recovery.mjs` (`86836eec…`) and `recovery.test.mjs` (`3e06fe2d…`), confirming the revision touched neither. Coordinator also closed the reviewer's remaining gaps directly: `describeUnattendedHalt` occurs exactly once across all of `src/` (its own declaration at `recovery.mjs:9498`) and is absent from the `runCommand` allow-list at `recovery.mjs:14192`, which is repo-wide and therefore stronger than the within-file occurrence bound the reviewer could establish, closing its cross-package-importer caveat; and the link anchor was verified to resolve, with the ten-item stop list confirmed present at `docs/commands.md:445-456` inside the linked section. Coordinator applied the `[x]` glyph and regenerated the derived board. Carried to T005, not resolved here: the reviewer's measured finding that the `docs/reference.md` pointer now sits at 460 bytes against the 480-byte gate (coordinator confirmed exactly; ~20 bytes of headroom, so any future sentence there breaks the test); the pre-existing divergence between the user-facing `commands.md` stop list and SKILL.md's closed set (`no ready Beads work` omitted, "recovery budget exhausted" added); and the still-unruled SC-002 seam and end-to-end `## Stops` demonstration inherited from T002/T003. The same runtime-actor framing survives at `src/skills/dude-work/SKILL.md:166` under approved T001/T002 text and is deliberately left for a T005 ruling; if it changes, the `T004 the Work owner states keep-working…` requirement clause must move in lockstep.
- 2026-08-05 UTC - T005@72657677 flagged `[!]` blocked — **`contract-mismatch: redefine-required`**. All eight engineering gates passed unconditionally and are recorded as fresh evidence: `recovery.test.mjs` 462 pass / 0 fail; the three script suites 117 / 0; the full discovered suite (44 files) 2259 discovered / 2255 pass / 0 fail with 4 skips identified by name as macOS/APFS filesystem-capability guards unrelated to this feature; `dude-lint` 0 warnings / 0 failures; `compose verify` all 16 packs `[OK]` with 0 failures and 0 leftovers; a pristine release build into a verified-empty directory (`[OK] staged 58 file(s)`) whose single `FEATURE_IDEAS_ROOT_MISSING` warning the Tester proved pre-existing by building the same release from `git archive HEAD` and observing the identical warning; the `.github` diff gate showing exactly `skills/dude-work/SKILL.md` and `skills/dude-work/recovery.mjs`, idempotent across rebuild; and both reference hashes intact (`recovery.mjs` `86836eec…`, `recovery.test.mjs` `3e06fe2d…`) with `src`↔`.github` byte parity. The whole feature is purely additive — 1648 insertions, zero deletions, one new export, zero new files — and a release-to-release diff confirms the shipped delta is exactly those two files. Six mutations run, all six killed. T005 nevertheless FAILS as chartered, and the blocker is normative rather than a quality defect in T001–T004. Two of T005's own acceptance clauses are demonstrably false and cannot be made true within the task's constraints: the loop does not end "only on a closed-set reason", and those halts echo no named reason. Eight reasons reach a halt under autonomous that the frozen 24-entry `OUTCOME_REASON_CLASSES` does not name — `interrupted`, `no-change`, `blocked`, `failed`, `pending-not-found`, `action-mismatch`, `occurrence-retention-conflict`, `learning-governance-conflict` — because `completeAttempt` assigns `let reason = normalized.outcome;` (`recovery.mjs:9204`) and overrides it for only four cases. The Tester drove six through the real `authorizeAttempt`/`completeAttempt` path and established the last two by autonomous-only branch construction; the Code Reviewer independently enumerated the same eight by site and confirmed no closed-set condition rides along in `blockers`, since `normalizeCompletionResult` accepts no `blockers` input and derives them solely from check states. Each such halt correctly returns `{halted:true, resolved:false, unresolved:['reason','subject']}`, so the behavior is right and fail-closed; the criterion is what is wrong. The review widened the defect beyond the Tester's report: `FR-001` sentence 2 is violated on the same grounds, so an amendment touching only SC-002 would leave the spec internally inconsistent. It also refuted the Tester's proposed FR-003 asymmetry — FR-003 sentence 2 and SC-002 clause 2 are the same norm reworded, and the narrow "progress-report only" reading fails on surplusage because FR-002 already covers progress reports — so the coordinator did not rely on it. Two further findings corrected inherited reasoning and are recorded so they are not re-litigated. First, the Feature 005 "ships identically unwired" precedent that justified accepting T002's missing call site is half false: `mayScheduleAfterStop` IS wired at `recovery.mjs:11754` via the live `transition`/`suspend-target` path, so the precedent rests on `mayContinueAutonomously` alone; the coordinator verified this directly, along with `describeUnattendedHalt` occurring exactly once across all of `src/` and `endsUnattendedLoop`'s sole caller being `describeUnattendedHalt`, which closes the reviewer's one blocking evidence gap. Second, and decisively, the review identified an FR-010 dilemma neither prior gate had named: wired, the predicate makes the FR-001/FR-003/SC-002 violations live; unwired, the model reading `SKILL.md` establishes the stop, which FR-010 explicitly forbids — so the feature cannot be both safe-because-unwired and delivering the required behavior. A third finding is new and belongs to a future phase rather than this amendment: `endsUnattendedLoop` halts on `action-mismatch` and `pending-not-found`, which `host-adapter.mjs:1184-1192` classifies `nonterminal: true` via `closedIncident` and which `host-adapter.test.mjs:728` pins by name as a single-correction affordance, so wiring Feature 013 as written would contradict settled host-adapter behavior with the adapter as the spec-conformant party; the reviewer refuted the parallel `learning-required` claim as a genuine layering difference between loop scopes, not a contradiction. Per the constraint in `tasks.md`, execution read but never wrote any `.dude/specs/013-*/**` artifact, and no specialist mutated definition state; the Code Reviewer had no shell and disclosed it, so the coordinator independently re-ran the full discovered suite, compose verify, the call-site greps, and the host-adapter and closed-set reads that confirm findings 2 and 3. A concrete three-part replacement wording for FR-001, FR-003, and SC-002 is proposed and awaiting the user; it extends FR-006's existing fail-closed license from missing cause detail to missing reason, introduces no new stop reason, and weakens nothing. Definition remains unamended and unowned by execution: `Next` is an explicit `define unattended-work-continuity`, at which point the Spec Lead becomes the delegated definition writer. T001–T004 stay `[x]` and their work is unaffected.
- 2026-08-05 UTC - re-defined Feature 013 (`define unattended-work-continuity`; sole owner `.dude/specs/013-unattended-work-continuity/spec.md` re-resolved via `dude-engine/feature.mjs`, zero diagnostics) after T005@72657677 stopped on `contract-mismatch: redefine-required`, and applied the definition. User-controlled `## Idea`, `## Open Questions` (answers still blank), and `## Assumptions` are byte-unchanged; `status: defined` and the exact `spec_path:` preserved; the entire prior `## Coordinator Log` preserved verbatim. The prior three staged designs threaded an `unattendedHalt` session member, a `runOperation` gate, malformed-request ordering, a no-progress lane permit/apply/receipt chain, and checkpoint/handoff/resume/session-identity/recovery-notice continuity; independent review rejected them three times (advisory-only enforcement; wrong completion seam; then lost checkpoint continuity, malformed-ordering contradiction, and the omitted no-progress lane route; then the omitted production runner, non-crash-safe lane settlement, and a dependency-note error). After the second same-finding failure the coordinator escalated to the user, who authorized an Architect-led redesign. The Architect ruled (coordinator-verified against live source) that the deterministic autonomous runner `src/skills/dude-work/host-adapter-runner.mjs` is the sole production adapter driver (`createHostAdapter`/`adapter.run`/`resumeHostAdapter`/`handoffHostWorker` each have exactly one non-test caller — the runner; the model submits no protocol operations via `dependencies.exchange`), so FR-010 is satisfied by the runner deciding continuation, and the gate/checkpoint/lane machinery guarded a non-existent adversary and an unreachable branch. The re-definition collapses the design to one enforcement locus — the runner's single terminal chokepoint `finish(row)`, which attaches the already-landed report `describeUnattendedHalt` on `hard-stop` and `null` on `ended`; `finish` is the sole terminal path (all `.terminal` values, `orphan`, and the natural end route through it), so it covers 100% of halts. Applied the settled resolved-or-explicitly-unresolved amendment to FR-001/FR-003/FR-006/SC-002 (FR-006 fail-closed extended from missing cause detail to missing reason; no new stop reason), clarified FR-004 (no out-of-set diagnostic presented as a closed-set reason), added behavior-level FR-011/SC-008, and phrased US5/FR-007/SC-005 "with its existing named reason where attribution is established" so an unbindable safety halt still halts and requests human input while reporting honestly-unresolved. Five conscious rulings recorded in spec/plan: no-progress lane disposition out of scope (the no-progress branch drives a terminal `controlled-end`; the lane chain is the success-path `task-completed` materialization, unreachable for halt reporting); the lane-ledger apply→commit crash window is pre-existing and unowned; clean `ended` settlements are not halts and carry no report; safety halts may report honestly-unresolved; and a future streaming-model driver would need adapter-level enforcement not built now. An independent Reviewer verified every architecture claim against live source and returned APPROVE with six advisory (non-blocking) findings; the coordinator applied the owned precision fixes — added US4/FR-002 to T006's trace, gave T006 fixture (e) an explicit unbindable-attribution unresolved-safety variant and re-grounded the `T003 G` purity-guard rationale on the autonomous-only property, and corrected the disposition's antecedent (no prior `## Re-definition Disposition` existed; the earlier `## Definition Disposition` is preserved) — and confirmed findings mechanically safe (dude-lint has no dependency rule and the board buckets by glyph, so a kept `[x]` T003 above reopened predecessors renders cleanly). Coordinator applied glyphs and reconciliation (coordinator-owned): kept 1 — T003@73616665 `[x]` (safety proof unchanged, deps T002); changed 4 — T001@6b656570 `[ ]`, T002@6e616d65 `[ ]` (landed predicate/reporter, contracts retuned, re-verify), T004@646f6373 `[ ]` (deps retargeted T003→T006 so docs follow the wiring), T005@72657677 `[ ]` (deps T004, reduced surface); new 1 — T006@77697265 (`wire`) `[ ]` deps T002,T003 (write set `host-adapter-runner.mjs` + `host-adapter.test.mjs` + `dude-work/SKILL.md`; `recovery.mjs`/`host-adapter.mjs` not modified); dropped 0, so no user-confirmation pause. Regenerated the board fence deterministically (Ready Now: T001; Done: T003). spec.md, plan.md, tasks.md canonical body, and this idea disposition applied; plan carries zero active ObjectiveRegistry regions; existing uncommitted T001/T002 `recovery.mjs` (+190) and tests preserved and reused; Feature 005/009/010 packages untouched. Coordinator lint run next; no readiness claimed until it reports zero failures.
- 2026-08-05 UTC - T001@6b656570 closed `[x]` (Lightweight; `@dude ship`, autonomous policy). Re-verification of the landed continuity predicate against the amended contract — no code change. Tester independently confirmed `endsUnattendedLoop` satisfies amended FR-001/FR-002/FR-009 and SC-001/SC-006/SC-007: under autonomous policy it returns false (keep working) for no-reason/authorized/completed/progress turns and true for closed-set stops with fail-closed true on unrecognized/malformed input, and default/guarded paths are byte-identical to HEAD. `node --test recovery.test.mjs` 462 pass / 0 fail; every in-scope FR/SC clause mapped to ≥1 landed `Feature 013 T001 A–F` assertion; three T001-scope mutations each caught within scope and `recovery.mjs` restored to pristine `86836eec…`. `OUTCOME_REASON_CLASSES` proven byte-identical to `git show HEAD` (SC-007, no new stop reason). SKILL.md `## Stops` remains the single detailed owner. Independent-review gate deferred to T006 (new runtime wiring) and T005 (final), since the earlier ship already reviewed this unchanged code at APPROVE and the re-definition changed only the spec. FR-009 "no new store" holds by construction in this scope; explicit enforcement owned by T006/T005. Coordinator applied the `[x]` glyph and regenerated the board.
- 2026-08-05 UTC - T002@6e616d65 closed `[x]` (Lightweight; `@dude ship`, autonomous policy). Re-verification of the landed deterministic halt reporter against the amended contract — no code change. Tester independently confirmed `describeUnattendedHalt` satisfies the amended FR-003/FR-004/FR-005/FR-006/FR-010: it returns exactly one of `null` (not a halt), a resolved descriptor (one closed-set reason + stopClass + target + subject + nextAction + evidenceHash), or `{halted:true,resolved:false,unresolved:[…]}`, failing closed to unresolved when a closed-set reason (out-of-set disposition → `classifyOutcomeReason` throws → `unresolved:['reason',…]`) or the actionable attribution cannot be established, and never presents an out-of-set diagnostic as a closed-set reason. This is the crux the re-definition formalized: the landed code already implements resolved-or-explicitly-unresolved as a strict superset of the old contract. Shared Tester run 462 pass / 0 fail; every in-scope FR/SC clause mapped to ≥1 landed `Feature 013 T002 A–G` assertion (with T003 F/H reinforcing the unresolved forms); two T002-scope mutations each caught within scope and `recovery.mjs` restored to pristine `86836eec…`. No contract mismatch found. Coordinator applied the `[x]` glyph and regenerated the board. Carried to T006: the primitives are not yet wired (`describeUnattendedHalt` has zero runtime callers, pinned by `Feature 013 T003 G`), so the load-bearing runner integration is next.
- 2026-08-05 UTC - T006@77697265 closed `[x]` (Lightweight; `@dude ship`, autonomous policy) — the load-bearing runner wiring, the only new implementation of the re-definition. Coder wired the landed `describeUnattendedHalt` into the autonomous runner's single terminal chokepoint `finish(row)` in `src/skills/dude-work/host-adapter-runner.mjs`: a `hard-stop` terminal decodes `row.stateBase64` to RunState and attaches `haltReport = describeUnattendedHalt({state, reason: row.reason}, currentInspection)`; an `ended` terminal (task-settled, controlled-end, cancelled) attaches `haltReport: null`; the decode+reporter run inside a try/catch whose fail-closed fallback is the reporter's own unresolved shape, so `finish` can never throw. Changed only the four sanctioned src files: `host-adapter-runner.mjs` (import + finish), `host-adapter.test.mjs` (6 `Feature 013 T006 A–F` fixtures driving the real `runHostAdapter`), `SKILL.md` (`## Stops` +1 sentence recording the runner-terminal wiring), and `recovery.test.mjs` (title/comment re-grounding of the `Feature 013 T003 G` purity guard onto the autonomous-only property — the count-==-1 assertion and signature regex left intact). `recovery.mjs` and `host-adapter.mjs` untouched (`recovery.mjs` sha `86836eec…` pristine, `describeUnattendedHalt` occurrence count stays 1; `host-adapter.mjs` empty diff). Independent Tester PASS: reproduced `node --test host-adapter.test.mjs recovery.test.mjs` 557 pass / 0 fail (462 recovery + 95 host-adapter, +6 new), verified the base64 round-trip and non-throwing property, ran a mutation battery (delete attach → 6/6 fixtures fail; flip hard-stop↔ended → 6/6 fail; runner restored byte-identically), and traced the reachability limitation through four independent barriers finding no missed path. Independent Code Reviewer APPROVE (no shell, disclosed): confirmed the `hard-stop`-only gate is necessary (not cosmetic) because `endsUnattendedLoop` fails closed to true for any non-authorized/non-completed reason, so calling the reporter on an `ended` row would misclassify a clean settlement; confirmed `finish` cannot throw, the extra field is schema-safe, the fixtures are genuine with a mutation sentinel, and the T003 G guard is intact. Conscious coordinator ruling — the safety-floor category reasons (approval-required, safety-or-authority, external-dependency, ambiguous-state) are not reachable as `runHostAdapter` terminals (`completionDispositionV2` emits only accepted/verification-failed/review-rejected; `currentRunCapture` hardcodes `state:'failed'`; ambiguous-state is preempted by `owner-resolution-failed`), so the exhaustive per-category safety coverage stays at the reporter level (`Feature 013 T003 A–H`, green) while T006 proves the uniform, reason-agnostic wiring on every reachable hard-stop (fixture E's `evidence-incomplete` substitute traverses the identical `request-human-input` path plus the unattributable-unresolved path); no fabricated fixture for a genuinely unreachable terminal, per the unreachable-guard lesson. Second conscious ruling — the runner's decode-failure unresolved fallback is a surviving mutation point (flipping its `resolved` literal is invisible to all tests) because it is unreachable end-to-end; both gates and the coordinator ruled it non-blocking defense-in-depth and chose not to churn the code for an untestable path. FR-010's wired-versus-unwired dilemma is now resolved: the deterministic runner establishes the halt and attaches the report; the model only phrases it. Coordinator applied the `[x]` glyph and regenerated the board; owner re-resolved with zero diagnostics before the write. Next ready: T004 (docs/prompt parity + `.github` regeneration).
- 2026-08-05 UTC - T004@646f6373 closed `[x]` (Lightweight; `@dude ship`, autonomous policy). Docs & parity, tightly scoped. Coder added exactly one prompt-contract requirement to `scripts/current-format-contract.test.mjs` (`T004 the Work owner states…` test) pinning the T006 runtime-wiring sentence in `src/skills/dude-work/SKILL.md` `## Stops` — a 2-regex AND-group matching "The deterministic autonomous runner attaches this report once, at its single terminal chokepoint `finish(row)` in `host-adapter-runner.mjs`" and "the runtime, not the model, owns whether and how a halt is reported" — so the wiring claim is contractually load-bearing (proven: deleting the sentence fails only that requirement; SKILL.md restored byte-identical). Correctly made ZERO doc edits: the three public docs (`docs/commands.md`, `docs/workflow.md`, `docs/reference.md`) already state the resolved-or-explicitly-unresolved discipline accurately as terse pointers, and the runner/`finish`/chokepoint mechanism is owner-only detail that must not leak into them per the single-owner principle (`CONTINUITY_DETAIL_MARKERS`). Rebuilt generated core with `node scripts/build-dev.mjs` (idempotent); coordinator confirmed `git status --porcelain -- .github` shows exactly `skills/dude-work/{SKILL.md, host-adapter-runner.mjs, recovery.mjs}` and src↔.github byte parity on all three, with `recovery.mjs` still pristine `86836eec…`. Verification: `node --test` on the three script suites 117 pass / 0 fail (all four `T004 …` continuity tests green including the new pin); full discovered suite 2265 tests / 2261 pass / 0 fail / 4 pre-existing skips. Coordinator ran the verification itself (deterministic contract-pin + mechanical rebuild); the comprehensive independent Tester+Reviewer gate is T005 over the complete contract. Carried non-blocking: the repetition checker reports 6 pre-existing cross-doc phrases (tracked-definition recovery, concurrency, idea-ledger, transcription-cleanup wording), none in the Feature 013 halt paragraphs and none introduced here. Coordinator applied the `[x]` glyph and regenerated the board; owner re-resolved with zero diagnostics before the write. Next ready: T005 (final full verification and independent review).
- 2026-08-05 UTC - T005@72657677 closed `[x]` (Lightweight; `@dude ship`, autonomous policy) — final full-contract verification and independent review; Feature 013 is fully executed. Coordinator ran the complete battery: full discovered suite 2265 tests / 2261 pass / 0 fail / 4 pre-existing macOS-filesystem skips; focused runner+recovery 557 / 0; prompt-contract three-suite 117 / 0; `dude-lint` 0 warnings / 0 failures; `compose verify` all 16 packs OK, 0 failures, 0 leftovers; a pristine release build into a verified-empty directory (58 files, 0 failures, its single `FEATURE_IDEAS_ROOT_MISSING` warning proven pre-existing/structural); and the `.github` diff gate showing exactly `skills/dude-work/{SKILL.md, host-adapter-runner.mjs, recovery.mjs}`, idempotent, with src↔.github byte parity and `git diff --check` clean. The same fresh evidence was routed independently to the Tester and Code Reviewer. Independent Tester PASS: reproduced every battery item with the exact expected numbers, authored its own `Feature 013 T005` probe (real `runHostAdapter`, 2/2 green, file restored byte-identical), ran two integration mutations (delete the `finish` attach → all 6 T006 fixtures fail; flip hard-stop↔ended → all 6 fail; runner and recovery hashes returned), confirmed the end-to-end contract (hard-stop → resolved-or-unresolved report; ended → `null`; progress never halts; non-terminal steps carry no report; `haltReport` transient and never persisted), and mapped every FR-001…FR-011 and SC-001…SC-008 to proved-by-test evidence (SC-002 in the amended resolved-or-explicitly-unresolved sense — explicitly not the retired "ends only on a closed-set reason" reading). Independent Code Reviewer APPROVE, materially ready to ship: verified chokepoint completeness (100% of work-loop terminals flow through `finish`), FR-010 genuine by construction (the runner is the sole production driver, requires autonomous policy, derives inspection only from runtime, and funnels every model input through validators to a runtime-chosen reason — "in fact stronger than FR-010 requires"), the reason-agnostic coverage split sound, and a per-FR/per-SC MET table. Both gates independently confirmed the two carried non-blocking items and ruled them acceptable: the decode-failure unresolved fallback at `host-adapter-runner.mjs:399` is a permanent surviving mutant (unreachable end-to-end because every reachable hard-stop decodes and the reporter returns non-null; it upholds `finish`'s never-throw contract and fails closed if it ever fired — honest to leave uncovered per the unreachable-guard lesson), and the safety-floor category reasons are not reachable as `runHostAdapter` terminals (`completionDispositionV2` emits only accepted/verification-failed/review-rejected; `currentRunCapture` hardcodes `state:'failed'`; ambiguous-state preempted by `owner-resolution-failed`), so exhaustive per-category safety coverage stays at the reporter level (`T003 A–H`) while the uniform wiring is proven on every reachable terminal (`T006`). The Reviewer additionally noted the `main()` CLI `request-invalid` terminal is emitted outside `finish` with no `haltReport`; ruled correct because it is a pre-loop request-shape rejection, not an unattended work-loop halt, so it is outside FR-011's scope. Recorded clarification: the working-tree edits to `docs/{commands,workflow,reference}.md` are the earlier ship's T004 terse-pointer work (preserved and accurate), and this re-definition's T004 correctly added only the prompt-contract pin without re-editing the docs. Coordinator re-resolved the owner with zero diagnostics, applied the `[x]` glyph, and regenerated the board (ready set now empty). Feature 013 "Unattended Work Continuity" is complete: all six canonical tasks `[x]`, no new stop reason, lane, board, command, persistent store, session member, gate, or parallel control system; no halt attribution persisted; guarded and non-unattended behavior byte-for-byte unchanged; the deterministic runner now attaches a resolved-or-explicitly-unresolved report at its single terminal chokepoint, resolving the FR-010 wired-versus-unwired dilemma. No VCS mutation performed; all changes remain uncommitted for user review.

<!-- dude:managed:end -->
