---
description: "Design Hugo sections, bundles, front matter, archetypes, taxonomies, menus, aliases, URLs, and multilingual structure for a content-heavy site."
name: "Design Hugo Content Model"
argument-hint: "Content types, example pages, URLs, languages, navigation, taxonomy needs"
agent: "Hugo Site Architect"
---
Design a Hugo content model for this site or feature:

`$ARGUMENTS`

Use Hugo's documented content model. Produce a concrete structure that covers:

- Sections and whether each needs `_index.md`.
- Leaf bundles versus branch bundles.
- Archetypes for new content.
- Front matter fields, with custom values under `params`.
- Taxonomies, menus, weights, aliases, slugs, and URL overrides.
- Page resources and image/data placement.
- Multilingual translation strategy if relevant.

If editing the workspace, implement the structure and validate with Hugo commands.
